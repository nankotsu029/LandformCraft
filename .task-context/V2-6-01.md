# V2-6-01 Release 2 placement contract

## Task Index (excerpt)

- **目的／前提:** verified Release 2をworld mutationから分離するplacement plan／journal契約を固定する。前提はV2-5 gate完了。
- **Scope／成果物:** target/bounds/anchor、capability set、tile/order、mutation/effect envelope参照、reservation/confirmation binding、operation ID、format 2 journal statesのstrict contractを実装する。
- **非Scope:** envelope算出、reservation、snapshot、apply、Undo/Recovery。
- **主変更:** `model.v2.placement`、`core.v2` plan compiler、placement plan/journal Schema、新ADR、migration／placement docs。
- **必須test／D/M/S:** strict round-trip／unknown state/version/capability／target mismatch、canonical order／locale/timezone、journal/entry budget、path/checksum/future version拒否、v1 journal codec不変。
- **Gate／docs／次／停止:** Release/target/capabilityへchecksum-boundしたimmutable planがstrict読取できれば完了し、ADR／migration／roadmapを更新して`V2-6-02`へ進める。v1 journal再解釈が必要なら停止する。

## Related docs

- ADR 0005 (v1 placement) — do not reinterpret
- migration-plan §8 Release 2 placement pipeline
- AGENTS.md §10 placement order
- ADR 0020 (new) — Release 2 placement contract

## Change candidates

- `model/v2/placement/*`
- `core/v2/placement/PlacementPlanCompilerV2.java` (or `core/v2/PlacementPlanCompilerV2.java`)
- `format/v2/LandformV2DataCodec.java` — read/write/seal/verify
- `schemas/placement-plan-v2.schema.json`, `schemas/placement-journal-v2.schema.json`
- `examples/v2/placement/*`
- `docs/adr/0020-*.md`, migration-plan, roadmap, task-index, architecture notes

## Frozen

- v1 `PlacementJournal` / `LandformDataCodec` / `FilePlacementJournalRepository`
- Release format 1, generator 3.0.0-phase6
- No envelope calculation, reservation, snapshot, apply

## Acceptance checklist

- [ ] Immutable PlacementPlanV2 sealed with release/target/capability checksum binding
- [ ] Format-2 journal states + tile entries strict contract
- [ ] Schema + examples + round-trip
- [ ] Unknown state/version/capability/path/future rejected
- [ ] Target mismatch rejected
- [ ] Canonical order / locale / timezone determinism
- [ ] Journal/entry budget
- [ ] v1 journal codec unchanged
- [ ] ADR 0020 + docs/roadmap sync
- [ ] No V2-6-02+ implementation

## Required tests

- PlacementPlanV2Test / PlacementJournalV2Test / PlacementPlanCompilerV2Test
- SchemaContractTest registration
- LandformDataCodecTest still passes (v1 journal)

## Stop conditions

- v1 journal reinterpretation required
- Need envelope calculation / apply to satisfy Acceptance
