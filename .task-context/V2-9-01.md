# V2-9-01 Surface foundation field, ownership, and transition contract

## Task Index (要約)

- Scope: version付きfoundation plan、surface class/elevation/residual/owner/transition descriptors、stable seed namespace、merge/interaction compiler、ScaleProfile admission、strict Schema/example/codec。kindをSUPPORTEDへしない。
- 非Scope: 個別形状generator、ALPINE/GLACIAL/MEANDERING変更、Paper、LARGE enable。
- 依存（方針A）: V2-6-02、V2-8-01（V2-8-02は前提にしない）。
- Acceptance: empty/minimal strict；2 synthetic ownerのwhole/tile merge同checksum。
- 拒否: ownerなしcell、同票owner、未宣言overlap、0..32外transition、seed collision、field/support/halo budget。

## Packages

- `model.v2.foundation`
- `core.v2` compiler
- `generator.v2.foundation` merge compositor

## Freeze

- v1 Schema/generator/Release1/placement/Undo
- 既存field意味のin-place変更禁止
- WorldBlueprint埋め込みは本Taskでは行わない（standalone contract）
