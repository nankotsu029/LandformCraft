# V2-6-11 Provider, manual, and image capability integration

## Task Index（正本抜粋）

- **目的／前提:** OpenAI／Anthropic/manual/import/image pathsからv2 capabilityを明示選択し同じcanonical compileへ接続する。前提はV2-2〜5 supported contractsとRelease 2。
- **Scope／成果物:** provider capability negotiation、v2 structured intent/import、manual/constraint bundle binding、reference image→soft draft/confirmation boundary、no-fallback dispatchを実装する。
- **非Scope:** new AI model feature、Web UI、画像からhard geometryの暗黙生成、Paper apply internals。
- **主変更:** `ai.spi` versioned interfaces、provider adapters、`core.v2` design service、request/provider docs、tests。
- **必須test／D/M/S:** each provider/manual/image path、unsupported model/capability/unknown version、canonical output equivalence、bounded payload/image/budget、secret redaction/path security、v1 provider default回帰。
- **Gate／docs／次／停止:** provider差がcanonical Intent後のgenerationへ影響せずunsupported capabilityをfallbackしなければ完了し、README／provider／migration／roadmapを更新して`V2-6-12`へ進める。real credentialをfixtureへ必要とする場合はcontract testで止め外部smoke Taskを追加する。

## Execution Guide必須

- 1 Taskだけ。後続（V2-6-12）へ進まない。
- Acceptanceを弱めない。v1 Schema／provider default不変。
- HARD geometryを画像から暗黙生成しない（ADR 0017）。
- full suiteはPhase gate以外でもTask close前に`./gradlew test`と`build`。

## Model

- model-assignment: Claude Sonnet 5 / High / Antigravity（本セッションはCursor Grok）

## 変更候補

- `ai.spi.v2` versioned SPI、capability catalog／negotiator
- OpenAI／Anthropic／import／fixture v2 adapters
- `core.v2.design` TerrainDesignApplicationServiceV2
- Design Package v2 publisher／Schema／example
- soft draft confirmation boundary
- docs: ai-providers、migration、roadmap、README、CHANGELOG、ADR 0029

## 凍結

- v1 TerrainDesignApplicationService／providers／Design Package format 1
- v1 Schema、generator 3.0.0-phase6、Release 1
- Paper apply internals、V2-7-02 secure file envelope（file入力はpixelハンドル経由）

## Acceptance checklist

- [ ] capability negotiation + no-fallback
- [ ] openai/anthropic/import/fixture/manual/image paths
- [ ] soft draft never HARD without explicit promotion path
- [ ] canonical Intent equivalence across providers
- [ ] budget／secret／path security
- [ ] v1 default regression
- [ ] docs/roadmap sync

## 必須テスト

- TerrainDesignApplicationServiceV2Test（全path）
- DesignCapabilityNegotiatorV2Test
- HttpTerrainDesignProviderV2Test（fake HTTP）
- v1 TerrainDesignApplicationServiceTest回帰

## 停止条件

- real credentialがfixture必須 → contract testで止め外部smoke提案
- hard geometry暗黙昇格が必要 → 停止
