# V2-6-07 Bounded settle and full verify

## Task Index全文

- 状態／Phase／優先度: 未着手／V2-6／P0。
- 目的／前提: apply後にbounded multi-tick settleしeffect envelope全体をexact verifyする。前提は`V2-6-06`。
- Scope／成果物: settle policy、scheduler-sliced full scan、canonical expected resolver、late update reconciliation、surface foundation／marine underwater column／surface-volume entrance／underground fluid／overlay continuity metric、journal evidenceを実装する。
- 非Scope: sampled verify、rollback実行、Undo、performance support declaration。
- 主変更: `core.v2.placement.verify`、Paper gateway、verify policy Schema、tests／operations docs。
- 必須test／D/M/S: delayed update/timeout/mismatch/server shutdown/tile checkpoint false-positive、solid/air/fluid順、tile seam river/volume連続性、scan order/thread不変、slice/timeout/queue budget、effect外 update/unknown settle policy拒否。
- Gate／docs／次／停止: settle後effect envelope全体のexact block-state streamを比較しfailureをcanonical分類できれば完了し、placement／operations／roadmapを更新して`V2-6-08`へ進める。sample verifyへ弱める必要があれば停止する。

## Task Execution Guide必須事項

- 1 Taskだけを閉じ、`V2-6-08`へ進まない。
- sample verifyへ弱めない。tile checkpointをsettle後exact verifyと誤認しない。
- `paper_apply`を`SUPPORTED`へ昇格しない。
- 対象test、`./gradlew test`、`./gradlew build`、`git diff --check`をTask close前に実行する。

## 関連docs／契約要点

- ADR 0024: 全tile apply後も`APPLYING`。terminal `APPLIED`はsettle／full verify後。
- generation-pipeline／migration: settle → full verify in bounded scheduler slices。
- journal states `SETTLING`／`VERIFYING`／terminal `APPLIED`（tiles `VERIFIED`）はSchema version 1で既定義。
- rollbackはV2-6-08。verify失敗は`RECOVERY_REQUIRED`へ分類。

## 変更候補ファイル

- new: `model.v2.placement.PlacementSettleVerifyPolicyV2`／`PlacementVerifyEvidenceV2`
- new: `core.v2.placement.verify.*`
- mod: `PlacementWorldGatewayV2`／`PaperPlacementWorldGatewayV2`（settle tick／verify read slice）
- mod: `PlacementJournalV2`（SETTLING／VERIFYING／terminal APPLIED invariants）
- new: Schema／example／ADR 0025
- docs: operations／roadmap／task-index／migration／limitations／CHANGELOG

## 凍結対象

- v1 Schema／placement／Undo／Release 1
- V2-6-01〜06 checksum契約の意味変更
- rollback／Undo／Recovery実行、production `SUPPORTED`

## Acceptanceチェックリスト

- [ ] settle policy version固定、unknown policy拒否
- [ ] APPLYING(all APPLIED) → SETTLING → VERIFYING → terminal APPLIED(all VERIFIED)
- [ ] effect envelope全体のexact stream比較（sample不可）
- [ ] tile checkpoint aloneでは成功にしない
- [ ] delayed update／timeout／mismatch／shutdown → canonical failure／RECOVERY_REQUIRED
- [ ] effect外 update拒否
- [ ] solid/air/fluid expected順、tile seam continuity metrics
- [ ] scan order／thread／locale／timezone不変
- [ ] slice／timeout／queue budget
- [ ] docs／roadmap同期、test／build／diff check

## 必須テスト

- `PlacementSettleVerifyServiceV2Test`
- SchemaContract／example round-trip
- Paper gateway settle／verify slice（unit）
- close: `./gradlew test`、`./gradlew build`、`git diff --check`

## 停止条件

- sample verifyが必要、snapshot外副作用をverify成功扱いにする、v1意味変更、clean build不能。
