# V2-6-06 Release 2 apply transaction orchestration

## Task Index全文

- 状態／Phase／優先度: 未着手／V2-6／P0（新地形拡張より優先）。
- 目的／前提: `validate → reserve → confirm → snapshot-all → apply` の順序を破れないapplication serviceを実装する。前提は`V2-6-01`〜`V2-6-05`（roadmap／Task Indexで完了）。
- Scope／成果物: operation state machine、canonical tile apply、surface／volumeのsolid→air carve→fluid pass、child-plan／volume-overlayのexplicit ordinal、cross-tile river／volume継続、gateway completion/close tracking、scheduler dispatch contract、failed future propagation。FeatureKindごとのPaper adapterは作らず、Release 2のcanonical surface／solid／air／fluid block streamだけを入力にする。
- 非Scope: settle/full verify、rollback完成、Undo/Recovery、production `SUPPORTED` enable。
- 主変更: `core.v2.placement` application service、Paper/worldedit v2 gateway、journal transitions、tests／architecture docs。
- 必須test／D/M/S: illegal order/late completion/timeout/cancel/shutdown/tile failure/main-thread assertion、surface-only／cave carve／sky solid／waterfall overlay／underground fluid、tile正逆／thread不変、bounded scheduler slices/queue/admission、confirm/envelope/checksum mismatch拒否、v1 service回帰。
- Gate: apply開始前invariantが強制され、preview/exportと同じcanonical streamをFeature別分岐なしで適用し、受理済みoperationを観測timeoutだけで取消扱いにしなければ完了。Feature別配置ロジックまたはsnapshot外mutationが必要なら停止。

## Task Execution Guide必須事項

- 1 Taskだけを閉じ、`V2-6-07`へ進まない。
- Release 2 Paper apply開始条件としてV2-5 gate、offline self-verify、3D read-back、corruption validationとV2-6-01〜05を再確認する。
- strict verify／binding／resource admissionをworld mutation前に完了する。
- `paper_apply`を`SUPPORTED`へ昇格しない。
- 対象test、`./gradlew test`、`./gradlew build`、`git diff --check`をTask close前に実行する。

## 関連docs／契約要点

- ADR 0020〜0023: v1と分離したplan/journal、effect envelope、atomic reservation＋one-time confirmation、snapshot-all、CONTAINED evidenceがapplyの必須前提。
- migration／operations: scheduler受理済みoperationをobserver timeoutで取消済みにせず、late completionをjournalへ反映する。
- volumetric terrain: final resolverのcanonical streamを共通入力とし、solid→air carve→fluidの順序、明示ordinal、全envelope snapshot、containmentを維持する。
- `PlacementJournalV2`: `SNAPSHOT_COMPLETE`後に`APPLYING`へ進み、tileはcanonical prefixだけ`APPLIED`。settle／verify状態は後続Taskが所有する。
- `PlacementWorldGatewayV2`: world accessはPaper scheduler経由、bounded sliceごとにcompletionとresource closeを証明する。

## 変更候補ファイル

- new: `core/v2/placement/apply/*`（service、canonical source/pass、limits、failure／receipt、journal store contract）
- mod: `model/v2/placement/PlacementJournalV2.java`（APPLYING invariant）
- mod: `core/v2/placement/snapshot/PlacementWorldGatewayV2.java`（async bounded apply slice contract）
- new/mod: `paper/*V2*`、`worldedit/v2/*`（scheduler／public WorldEdit adapter）
- new: `src/test/.../core/v2/placement/apply/PlacementApplyTransactionServiceV2Test.java`
- docs: README、architecture、operations、security、migration、implementation-roadmap、placement README、task-index、roadmap、CHANGELOG／ADR index（実装に応じる）

## 凍結対象

- v1 Schema、generator `3.0.0-phase6`、Release 1 verifier／allowlist、v1 `PlacementApplicationService`／gateway／journal／Undo／Recovery。
- Release 2 artifact format／capability set、V2-6-01〜05 checksum契約、snapshot artifact、containment policy。
- settle/full verify、rollback、Undo、Recovery、production support catalog。

## Acceptanceチェックリスト

- [x] strict Release／plan／envelope／reservation／snapshot／containment／confirmation-consumed preflight
- [x] `SNAPSHOT_COMPLETE → APPLYING`とcanonical APPLIED-prefix journal checkpoint
- [x] Feature非依存canonical solid→air carve→fluid apply、explicit ordinal
- [x] canonical tile order、cross-tile continuity
- [x] bounded scheduler slice／queue／worker admission
- [x] gateway accepted/completed/resource-closed tracking、main-thread assertion
- [x] illegal order／binding/checksum mismatchをfirst apply前に拒否
- [x] timeoutで内部operationをcancelせずlate completionをjournalへ反映
- [x] cancel／shutdown／tile failureを成功扱いせず、mutation後は`RECOVERY_REQUIRED`
- [x] surface/cave/sky/waterfall/underground-fluid fixtures
- [x] tile入力順／1・4 thread／locale／timezone決定性
- [x] v1 placement service回帰
- [x] docs／roadmap同期、targeted test、test、build、diff check

## 必須テスト

- 編集前: V2-6-01〜05対象test（成功、2026-07-18）。
- 対象: `PlacementApplyTransactionServiceV2Test`＋必要なgateway/model tests。
- 互換: `PlacementApplicationServiceTest`、`V1CompatibilityGoldenTest`、Release 1 verifier対象test。
- close: `./gradlew test`、`./gradlew build`、`git diff --check`。

## 停止条件

- FeatureごとのPaper adapter、snapshot外mutation、sample verify、private WorldEdit/FAWE API、unbounded queue／slice、v1意味変更が必要。
- Release/snapshot/containmentをstrictに再検証できない、またはclean buildを維持できない。

## 現在の関連差分と失敗テスト要約

- 作業開始前からV2-6-01〜05のmodel/core/schema/example/docs/testが未追跡または変更済み。これらを既存変更として保持する。
- README／architectureにはV2-6-03以前の古い進捗記述が残るが、roadmap／Task Indexと実装から一意に同期可能。
- 編集前のV2-6-01〜05 targeted testsは`BUILD SUCCESSFUL`。失敗testなし。
- 実装中にstrict verifierがfinal confirmed planをenvelope作成時planへ直接比較する段階差を検出し、unbound→envelope-bound→confirmed planをcanonical再構成して各checksum linkを検証するよう修正した。
- V2-6-01〜06＋Paper gateway＋v1 placement／Release 1／v1 golden targeted regression、`./gradlew test`（5分30秒）、`./gradlew build`、`git diff --check`はすべて成功。
- persisted Schema shapeは既存version 1で足りるため変更なし。canonical APPLIED-prefixはJava record invariant、stream例はplacement READMEへ同期した。
