# Task capsule: V2-9-14 Terrain foundation integration and Phase gate

## Task Index全文（正本: docs/design-v2/task-index.md）

- 状態／Phase／優先度: 未着手／V2-9／P0 gate。
- 目的／背景: `V2-9-01`〜`V2-9-13`を統合し、基礎Featureとcontractのoffline support能力だけを確定する。
- Scope／成果物: representative surface/coast/island/marine/connection/macro/river scenarios、capability matrix、strict Release、determinism/resource/security/compatibility audit。
- 非Scope: V2-10 feature、LARGEの無条件support、Paper個別実装、未完能力の昇格。
- 依存／変更面: 全V2-9 Task。`V2-6-19` Phase gateの完了は前提としない（回帰対象は着手時点でのV2-6実装状態）。audit、roadmap、taxonomy、README/limitations、support catalog。
- 個別要件: full positive/corruption/tamper、whole/tile/thread/module/candidate/locale/timezone、1000角＋ScaleProfile admission、cancel/cleanup、v1/Release1/V2-2〜V2-6回帰を統合する。
- Acceptance／停止: 証拠が揃った能力だけ`SUPPORTED`へし、Paper能力はV2-6 evidenceを継承できる共通stream compatibilityとして記録する。欠損、cycle、budget、compatibility regressionがあれば新V2-9 Taskを追加し親Phaseを未完のまま停止する。

## 前提確認

- V2-9-01〜V2-9-13: roadmap上すべて完了（13/14）。
- 依存 `V2-6-01`／`V2-6-02`／`V2-8-01`: 完了済み。V2-6は05まで完了（回帰対象は現状のV2-6実装）。
- Phase gate TaskのためFull suite（`./gradlew test` / `build`）を実行する。

## 現状の事実（コード）

- 全V2-9 dedicated module（generator.v2.foundation.Landform*ModuleV2）は`EXPERIMENTAL`かつ`BuiltInLandformModuleCatalogV2`未登録（diagnostic binding）。WorldBlueprint checksum凍結のため。
- Release 2 capability list（natural order）: `[environment-fields, hydrology-plan, sparse-volume, surface-2_5d]`。foundation専用capabilityは存在しない。
- 全foundation planはstrict codec read/write＋`canonicalChecksum`を持つ。examples/v2/foundation/に22 plan example。
- ScaleAdmissionV2（V2-8-01）がSurfaceFoundationPlanCompilerV2のadmission。LARGE生成は未実装。

## 実装成果物

1. `src/test/java/.../integration/v2/FoundationPhaseGateV2Test.java`
   - foundation plan example portfolio（22 plan）のstrict read＋canonical checksumが正逆読取順／1・4 thread／locale（tr-TR）／timezone（Pacific/Chatham）で不変。
   - 代表統合scenario: plain-hill slice compile → SurfaceFoundationMergeのwhole／tile checksum一致（SMALL）。
   - 1000角: 2 synthetic ownerのmerge whole／tile一致＋ScaleAdmission経由。oversized profile拒否。
   - false-promotion検査: V2-9系FeatureKindのcatalog dispatchがDIAGNOSTIC＋EXPERIMENTALのまま、dedicated module EXPERIMENTAL、Release capability list不変（foundation capabilityなし）。
2. `docs/design-v2/audits/v2-9-phase-gate.md`（capability matrix含む監査報告）
3. docs同期: roadmap.md、task-index.md、terrain-feature-taxonomy.md、README.md、docs/limitations.md、docs/design-v2/current-limitations.md、CHANGELOG.md（該当あれば）

## 能力判定方針（Acceptance準拠、証拠ベース）

- offline_generate／validation／preview（plan-level）: SUPPORTED（各Taskでcontract＋generator＋validator＋preview＋positive/corruption fixture＋決定性＋budget testが完備）
- intent_compile／standalone_usage: PARTIAL（typed Intent＋strict Schemaはあるがdiagnostic binding経由のみ・専用module catalog未登録）
- export: PARTIAL（sealed canonical plan JSON／streaming tile checksumのみ。Release 2 foundation capability未定義のためRelease経由exportなし）
- paper_apply以降: UNSUPPORTED（V2-6 gate待ち。共通canonical stream互換をauditへ記録しV2-6 evidenceを継承可能にする）
- LARGEはsupported表現しない。WorldBlueprint埋込もしない。

## 凍結対象

- v1 Schema／generator `3.0.0-phase6`／Release format 1／placement／Undo
- WorldBlueprint checksum（module catalog登録は行わない）
- 既存V2-2〜V2-6／V2-7-01／V2-8-01 fixtureとchecksum
- 全foundation plan exampleのchecksum（in-place変更しない）

## 停止条件

- full suiteで欠損・cycle・budget・compatibility regressionを検出（→新V2-9 Task提案し停止）
- clean build不能
- 能力昇格に必要な証拠の欠落を発見

## テスト

- `./gradlew test --tests "*FoundationPhaseGateV2Test"`（対象）
- `./gradlew test` / `./gradlew build`（full suite、Phase gate要件）
- `git diff --check`

## 関連差分

- 作業開始時、branch `fix/all` に他Trackの大量の既存差分あり（V2-6-01〜05、V2-7-01、V2-8-01、V2-9-01〜13等）。保持し上書きしない。
