# Task capsule: V2-12-01

> 補助資料。正本は `docs/design-v2/task-index.md` / `docs/roadmap.md` / `AGENTS.md`。Git管理外。

## 1. Task Index 全文（V2-12-01 v1 retirement governance ADR and compatibility policy update）

- **状態／Phase／優先度:** 未着手／V2-12／P1。
- **目的／前提:** v1凍結（AGENTS.md §6）を段階的に解除するgovernance ADRを作り、削除・改名の範囲と順序を正本化する。前提は`V2-11-01`。
- **Scope／成果物:** ADR（削除対象v1機能の一覧と根拠、維持するv1資産（参照用golden等）の扱い、名称変更方針 — Javaクラス等の`V2` suffix改名は自由、contract ID／Schema `$id`／checksumへ影響する識別子はADRが明示承認した場合のみ変更、`/lfc r2`→正式command名、deprecation window）、AGENTS.md §6／roadmap「継続する製品境界」／task-execution-guideの改訂。コード変更なし。
- **非Scope:** 実コードの削除・改名、catalog変更、Schema変更。
- **主変更:** `docs/adr/`、AGENTS.md、roadmap、task-execution-guide。
- **必須test／D/M/S:** なし（正本間の矛盾検査のみ）。
- **Gate／docs／次／停止:** ADRと改訂正本が矛盾なく揃い人間承認が得られれば完了し、`V2-12-05`以降のv1変更Taskを解禁する。v2が未カバーのv1機能が削除対象に含まれる場合は停止し、先行カバレッジTaskを提案する。

Phase前文（task-index §12）: AGENTS.md §6のv1凍結は`V2-12-01`のADR承認までは全Taskで有効。`V2-12-05`以降は人間承認必須。

## 2. Task Execution Guide 必須事項

- 1回の作業で1 Taskのみ。後続（`V2-12-02`以降）へ進まない。
- 親Phaseを閉じられるのは`V2-12-07`のみ → V2-12は`進行中`のまま。
- Acceptanceを弱めない。未完を完了と記録しない。
- 停止条件: 正本間の矛盾で一意解決不能、v1 golden／Release 1／placement互換を破る必要、hard constraint推測が必要。

## 3. 直接関係するdocs／ADRの要点

- `AGENTS.md` §6: v1 Schema／generator `3.0.0-phase6`／Release format 1／v1 placement／v1 Undo／既存checksum・goldenを変更しない。v2はversion dispatchで並設。
- `docs/roadmap.md` L59「継続する製品境界」（Web UI等の恒久非Scope）と L21（v1凍結はADR承認まで有効）。
- `docs/design-v2/task-execution-guide.md` §2（D/M/S共通のv1凍結）、§8（実機依存／寸法evidence）。
- ADR 0032（FAWE 2.15.2 standalone smoke profile）、ADR 0034（R2 Paper application lifecycle、`/lfc r2` 分離の根拠）。
- `V2-11-06`: published dimension limit = 1000×1000（FAWE 2.15.2 evidence）。WorldEdit 7.3.19単独runtimeは64×64のまま。

## 4. 変更候補ファイル

- 新規 `docs/adr/0035-v1-retirement-governance.md`
- `docs/adr/README.md`（index追記）
- `AGENTS.md` §6
- `docs/roadmap.md`（「継続する製品境界」＋V2-12行のADR参照）
- `docs/design-v2/task-execution-guide.md` §2
- `docs/design-v2/task-index.md`（V2-12-01状態、Phase前文のADR参照）
- `CHANGELOG.md`

## 5. 凍結対象（本Taskでは一切変更しない）

- 全ソース（`src/`）、`schemas/`、`examples/`、`src/main/resources/`。
- v1 Schema、generator `3.0.0-phase6`、Release format 1、v1 placement／Undo、既存checksum／golden。
- feature support catalog、Release capability集合、contract version。

## 6. Acceptance チェックリスト

- [x] ADRに削除対象v1機能の一覧と根拠がある
- [x] 維持するv1資産（参照用golden等）の扱いが定義されている
- [x] 名称変更方針（`V2` suffix自由／contract ID・`$id`・checksum影響識別子はADR明示承認のみ）
- [x] `/lfc r2` → 正式command名の決定
- [x] deprecation windowの定義
- [x] AGENTS.md §6／roadmap／task-execution-guideを矛盾なく改訂
- [x] コード変更ゼロ
- [x] 正本間の矛盾検査（rev.3でTask Index §1／`V2-12-03`〜`07`を同期）
- [x] **人間承認**（2026-07-20、承認者 nankotsu029。ADR Status: Proposed → Accepted）

**Task状態: COMPLETE（2026-07-20）。次は`V2-12-02`（本Taskからは開始しない）。**

## 7. 必須テスト

Task Index上「なし（正本間の矛盾検査のみ）」。実施するのは:
- `./gradlew test` / `./gradlew build`（docs変更の非破壊確認、`DocsLinkConsistencyTest`が相対link／anchorを検査するため必須）
- `git diff --check`、`git status --short`

## 8. 停止条件（Task固有）

- v2が未カバーのv1機能を削除対象へ含めそうになったら停止し、先行カバレッジTaskを提案する。
  → 本Taskの調査結果: custom asset catalog（`structure/`、`CustomAssetService`、`custom-asset-*.schema.json`）にv2等価物が存在しない。**削除対象から除外**しRETAINへ分類することで停止を回避した。

## 9. 現在の関連差分と失敗テスト要約

作業開始時の`git status --short`はV2-11-04〜06の既存変更（modified 30件＋未追跡`.task-context/V2-11-06.md`）。これらはユーザーの既存変更として保持し、上書きしない。失敗テストなし。
