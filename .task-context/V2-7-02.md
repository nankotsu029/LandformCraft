# V2-7-02 Secure extraction input envelope

## Task Index (全文要約)

- 目的: sanitized画像fileから抽出coreへのARGB供給路を固定する。前提は V2-7-01（完了）。
- Scope: v1 ReferenceImageProcessorと同等のpath/symlink/hardlink/magic/byte/pixel/frame/EXIF検査を持つ抽出専用envelope、decode/working budget、cancel、source checksum連鎖。
- 非Scope: Request Schema変更、draft artifact保存、AI Provider接続。
- 主変更: format.v2.constraint.extract、fixtures、image-constraint-maps docs。
- 必須test/D/M/S: path/link/TOCTOU/bomb/multi-frame拒否、byte/pixel/decode/working budget、checksum連鎖、thread/locale不変、cancel cleanup。
- Gate: 実PNG/JPEGからdraftまでの経路がstrictに通れば完了。v1画像境界の意味を変える必要があれば停止。
- 次: V2-7-03（本Taskでは着手しない）。

## Execution Guide 必須事項

- 1 Taskだけ。後続へ進まない。Acceptance弱めない。
- EXPERIMENTALのまま。SUPPORTED昇格しない。
- ./gradlew 対象test → test → build。docs/roadmap同期。

## 関連正本要点

- ADR 0017: 抽出coreは純関数；ファイルは専用secure envelope経由のみ。
- image-constraint-maps §2/§15: ReferenceImageProcessor同等の安全境界を共有し、decodeは抽出用ARGBへ。
- security.md Phase 5: PNG/JPEG、8MiB/4Mpx/4096/aspect32、NOFOLLOW、TOCTOU、EXIF向き補正後metadata除去。

## 変更候補

- format.v2.constraint.extract 新規: SecureImageExtractionEnvelopeV2、limits/failure、SanitizedArgbImageV2、file→draft facade
- tests + fixtures
- docs/design-v2/image-constraint-maps.md、docs/roadmap.md、task-index、CHANGELOG

## 凍結

- v1 Schema/generator/Release1、V2-1 numeric PNG経路、Request Schema、draft artifact (V2-7-03)、昇格 (V2-7-04)

## Acceptance

- [ ] path/symlink/hardlink alias/TOCTOU拒否
- [ ] magic/extension不一致、corrupt、multi-frame拒否
- [ ] byte/pixel/dimension/aspect/decode working budget
- [ ] EXIF orientation適用、metadataを意味に使わない（ARGBへcopy）
- [ ] source SHA-256 → ExtractedMaskDraftV2.sourceChecksum → semanticChecksum連鎖
- [ ] PNG/JPEG → draft happy path
- [ ] cancel、thread/locale決定性
- [ ] docs/roadmap同期、CLI/Paper/Request未接続のまま

## 必須テスト

SecureImageExtractionEnvelopeV2Test（上記全経路）

## 停止条件

v1画像境界の意味変更が必要、暗黙hard化が必要、Request Schema変更が必要
