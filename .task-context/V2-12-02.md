# Task capsule V2-12-02 — Production Release 2 export path

補助資料。正本は `docs/design-v2/task-index.md` §12。

## 1. Task Index 全文（V2-12-02）

- 状態／Phase／優先度: 未着手／V2-12／P1。2026-07-20全体再監査の残課題「P1 — V2通常利用経路」の前半。
- 目的／前提: v2のrequest→design→generate→preview/exportをstrict Release 2 publisherへ接続し、production export経路（core application service／API）を成立させる。前提は`V2-11-01`。v1不変のため`V2-12-01`とは独立に実装できる。
- Scope／成果物: `TerrainDesignApplicationServiceV2`〜generation〜preview/export〜Release 2 publisherの正式接続、staging→strict read-back→atomic publish、export成果物を`V2-6-20` verified canonical block sourceで開きplacement planを作れるまでのoffline E2E、CLI／Paperから呼べるapplication API。
- 非Scope: Paper／CLI command routing（`V2-12-03`）、v1変更、新capability／artifact format、world mutation。
- 主変更: `core.v2`のapplication orchestration、`format.v2.release` publisher接続（共有領域のため直列実行）、tests、architecture／artifact-format docs。
- 必須test／D/M/S: request fixture→R2 strict verify→placement eligibilityのoffline E2E、同Blueprint同checksumのdeterminism、cancel／cleanup、budget admission、v1 golden不変。
- Gate／docs／次／停止: 正式APIから`ReleasePlacementEligibilityVerifierV2`を通るstrict R2を再現的にpublishできれば完了し`V2-12-03`へ進める。新artifact formatや推測fallbackが必要になれば停止する。

## 2. Execution Guide 必須事項

- 1回1 Task、後続へ進まない。Acceptanceを弱めない。
- v1凍結は全Taskで有効（ADR 0035 D1第2条件未成立）。v1資産を削除・改名しない。
- 新capability／artifact formatを追加しない。既存 `surface-2_5d` だけを使う。
- 完了前に `./gradlew test` と `./gradlew build`。

## 3. 直接関係する既存契約

- `TerrainDesignApplicationServiceV2`（design → `DesignArtifactsV2`）
- `DiagnosticBlueprintCompilerV2` / `DiagnosticCompileRequestV2`（Intent → Blueprint）
- coastal generators／`CoastalTransitionCompositorV2`／`CoastalValidatorV2`／`CoastalDiagnosticPreviewRendererV2`
- `LfcGridWriterV1`／`ConstraintFieldIndexCodecV2`（constraint field sidecar）
- `OfflineTileSchematicWriterV2`／`OfflineTileArtifactCodecV2`（Sponge v3 tile）
- `SurfaceReleaseSourceV2` → `ReleaseSurfacePublisherV2`（staging→strict read-back→atomic publish）
- `ReleasePlacementEligibilityVerifierV2`／`VerifiedReleaseCanonicalBlockSourceV2`／`PlacementPlanCompilerV2`

## 4. 変更候補ファイル

- 新規 `core/v2/export/`: `Release2ExportApplicationServiceV2`, `Release2ExportRequestV2`,
  `Release2ExportResultV2`, `CoastalSurfaceExportPipelineV2`, `CoastalSurfaceFieldsV2`
- 新規 test: `core/v2/export/Release2ExportApplicationServiceV2Test`
- docs: `docs/design-v2/pipeline-and-architecture` 系、`artifact-format`、`task-index`、`roadmap`、CHANGELOG

## 5. 凍結対象

v1 Schema／generator `3.0.0-phase6`／Release format 1／v1 placement／Undo／既存checksumとgolden。
Release 2 manifest／capability catalog／artifact type・version。

## 6. Acceptanceチェックリスト

- [ ] 正式API（core application service）だけでrequest→R2 publishが完走する
- [ ] publish後に `ReleaseSurfaceVerifierV2` と `ReleasePlacementEligibilityVerifierV2` が通る
- [ ] verified canonical block sourceでopenしplacement planをcompileできる
- [ ] 同一入力で同一 blueprint／manifest／tile checksum（再現性）
- [ ] cancelでstaging残骸ゼロ、publish済みディレクトリなし
- [ ] budget admission（disk上限超過を事前拒否）
- [ ] v1 golden不変

## 7. 必須テスト

`Release2ExportApplicationServiceV2Test`（E2E／determinism／cancel／budget）、
`V1CompatibilityGoldenTest`、`./gradlew test`、`./gradlew build`。

## 8. 停止条件

新artifact format、推測fallback、hard constraint補完、v1変更が必要になった場合。

## 9. 現在の関連差分

作業前から `V2-11-xx`／`V2-12-01` の未commit差分が存在する（保持する）。
