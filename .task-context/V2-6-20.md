# V2-6-20 task capsule

## Scope

- strict verify済みRelease format 2 directory／ZIPからrestartable `PlacementCanonicalBlockSourceV2`を構築する。
- surface capabilityはsurface tile、sparse-volume capabilityはfinal volume tileを使用し、X-fastest／Z／Yでworld座標へ変換する。
- source bindingはmanifest checksum、canonical capability prefix、overlay ordinal、immutable fingerprintへ固定する。
- bounded Sponge v3 decode、cursor close／reopen、ZIP staging cleanupを実装する。

## Frozen

- v1 Schema／Release 1／placement／Undo。
- Release format 2 manifest、artifact type/version、Sponge v3 format、既存Schema。
- Paper／WorldEdit／FAWE、command routing、world mutation（V2-6-21）。

## Acceptance

- [x] directory／ZIPが同じbindingとblock streamを返す。
- [x] surface／sparse-volumeのfinal tileを正しく選ぶ。
- [x] missing／extra／duplicate／case collision／traversal／symlink／future version／checksum／semantic tamperをstrict verifier経由で拒否する。
- [x] palette／VarInt／block state／dimension budgetをbounded decodeで拒否する。
- [x] tile／overlay順、locale／timezone／charsetに依存しない。
- [x] cursor close／reopen、source close、ZIP staging cleanup、cancelを検証する。
- [x] targeted test、test、build、diff checkが成功する。

## Stop

- 新artifact format、unbounded dense decode、Paper／WorldEdit依存が必要なら停止する。
