# V2-6-21 Task capsule (re-opened 2026-07-20)

## Task contract

- Scope: Release 2 Paper application and explicit command lifecycle only.
- Purpose: strict Release 2 sourceからPaper lifecycleを安全に接続し、durable store、Undo、Recovery、command securityをproduction経路で閉じる。
- Prerequisites: V2-6-01〜13、V2-6-20（正本上は完了）。
- Non-scope: WorldEdit／FAWE実機smoke、500／1000実測、新Release artifact／Schema、support catalog昇格、v1 command意味変更。
- Frozen: v1 Schema、generator 3.0.0-phase6、Release 1、v1 placement／Undo／Recovery。
- Order: strict verify → plan/envelope → reserve/confirm → snapshot-all → containment → apply → settle → full verify; failureはreverse rollbackまたは明示Recovery。
- Model/Effort: Codex Sol Max／Max。独立reviewはClaude Opus 4.8 XHigh＋Antigravity＋人間。

## Re-open evidence

- P0 durability: confirm／applyのdurable contextとjournalが別writeで、write間fail／restart時にterminal journalと必要contextが不整合になり得る。
- P0 Undo reservation: apply直後の同placement Undo予約について、自己置換・第三者overlap・baseline完全復元のproduction-store証拠が不足。
- P0 command security: R2 planがv1 world policyを通らず、非Player senderをCONSOLEへ昇格し、execute actor bindingとR2 completion回帰が不足。
- Safety until closure: R2をproduction `SUPPORTED`として扱わず、後続V2-11-01へ進めない。

## Candidate files

- `core/v2/placement/Release2PlacementApplicationServiceV2.java`
- `core/v2/placement/Release2PlacementOperationStoreV2.java`
- `core/v2/placement/verify/PlacementSettleVerifyRequestV2.java`
- `core/v2/placement/verify/PlacementSettleVerifyServiceV2.java`
- `paper/PaperRelease2PlacementServiceV2.java`
- `paper/LandformCraftCommand.java`／`LandformCraftSuggestions.java`
- corresponding production-store／Paper security tests
- ADR 0034、commands/admin/operations/security、Task Index、roadmap

## Acceptance checklist

- [x] every operation-store write failpoint is fail-closed and restart classifies safely
- [x] terminal `APPLIED` is never published before apply-complete journal＋verify evidence are durable
- [x] confirm terminal state is never published before snapshot＋containment context is durable
- [x] restart permits production-store Undo or conservative Recovery without guessing
- [x] Undo reservation replaces the same placement lease, rejects third-party overlap, and restores the complete baseline
- [x] R2 plan enforces startup-captured world allow／deny policy and missing world is a stable domain error
- [x] command block／ambiguous sender is rejected; plan／confirm／execute／Undo／Recovery actor binding is preserved
- [x] R2 tab completion is permission-aware and never exposes tokens
- [x] explicit `/lfc r2` remains separate; v1 command／golden／Release 1 regressions pass
- [x] no new Release artifact／Schema version; examples only if an existing contract changes
- [x] targeted tests、`./gradlew test`、`./gradlew build`、`git diff --check`

## Close evidence

- 全12 `WritePointV2`×`BEFORE_WRITE`／`AFTER_PUBLISH`（24通り）をproduction `Release2PlacementOperationStoreV2`へ注入し、pre-mutation mutation 0、post-mutation exact rollback、Recovery plan retry、restart strict loadを確認。
- confirm後／APPLIED後にapplication serviceを再生成し、同じfile storeからexecute／Undoを継続。第三者overlapを拒否し、同placement Undoは成功、tracked effect-envelope blockを全てsnapshot baseline `minecraft:stone`へ復元。
- operator Player／CONSOLE／RCON actor、非operator／command block拒否、world allow／deny、missing world `LFC-NOT-FOUND`、execute actor mismatch、permission-aware completion／token非補完を確認。
- Targeted placement／Undo／Recovery／Paper／Schema／v1 golden suite: SUCCESS。
- `./gradlew test`: SUCCESS（9m01s）。
- `./gradlew build`: SUCCESS。
- `git diff --check`: SUCCESS。
- Schema／example／Release artifact変更なし。support catalog昇格なし。後続`V2-11-01`未着手。

## Stop conditions

- private WorldEdit／FAWE API、新Release format／Schema、snapshot外副作用の容認、v1互換破壊が必要。
- production-store restartをmockだけで完了扱いする必要がある。
