# V2-5-17 Release 2 `sparse-volume` capability

## Task (Task Index 正本)
- 目的: VolumePlan／index／validation／3D tileをRelease 2へstrict追加する。前提は`V2-5-15`〜`V2-5-16`とV2-4 Release capability(environment-fields)。
- Scope: `sparse-volume`必須artifact/version、AABB/operator/palette/block checksum binding、directory／ZIP publisher/verifier。
- 非Scope: Paper placement、feature lifecycle最終昇格。
- 主変更: `format.v2.release` catalog、volume artifact Schema／examples、artifact／migration docs。
- 必須test: earlier capability regression、complete volume release、missing/extra/version/operator/AABB/tile checksum改変、directory/ZIP parity、operator/index/expand/palette budget、future capability拒否。
- Gate: 全過去capability setとsparse-volume releaseをstrict verifyしfallbackなしなら完了→artifact/migration/roadmap更新→`V2-5-18`。format core意味変更が必要なら停止。

## 設計（surface→hydrology→environmentのnestを踏襲）
- capability list: `SPARSE_VOLUME_WITH_ENVIRONMENT = [sparse-volume, environment-fields, hydrology-plan, surface-2_5d]`。
- 追加artifact（`volume/`配下）:
  - `volume/sdf-primitive-plan.json` type `volume-sdf-primitive-plan-v2` v1
  - `volume/csg-plan.json` type `volume-csg-plan-v2` v1
  - `volume/aabb-index-plan.json` type `volume-aabb-index-plan-v2` v1
  - `volume/validation.json` type `volume-validation-artifact-v2` v1
  - `volume/tiles/<id>.json` type `volume-offline-tile-artifact-v2` v1（新type: surface tileのofType集計を汚さない）
  - `volume/tiles/<id>.schem` type `volume-sponge-schematic-v3` v1
- binding検証: csg.requirePrimitivePlan(sdf)、aabb.requireCsgPlan(csg)、validation.sourcePlanChecksum==csg.canonicalChecksum && passesHardValidation、tile strict read-back(semantic checksum)、tile.sourceBlueprintChecksum==blueprint.canonicalChecksum。
- 既存type `offline-tile-artifact-v2`/`sponge-schematic-v3`は再利用しない（surface verifierの`ofType`集計と衝突する）。

## 変更ファイル
- ReleaseArtifactCatalogV2: SPARSE_VOLUME定数/list、KNOWN_CAPABILITIES、6 artifact登録、verifyManifest dispatch、verifyKnownKeys依存規則。
- EnvironmentReleaseCapabilityVerifierV2: `verifyEnvironmentStack(root,index,token)->WorldBlueprintV2`をpackage-privateで公開（surface+hydrology+env payload、requireNoUnexpectedはcaller）。
- ReleaseEnvironmentPublisherV2: inspectEnvironment/copyEnvironment/EnvironmentSnapshotをpackage-privateへ（logic不変）。
- 新規: SparseVolumeReleaseCapabilityVerifierV2、ReleaseSparseVolumePublisherV2、SparseVolumeReleaseSourceV2、ReleaseSparseVolumeArtifactsV2、ReleaseSparseVolumeVerifierV2。
- schemas: 既存volume plan schema再利用。examples: release-sparse-volume README。
- docs: artifact-format.md、migration-plan.md、roadmap、task-index。

## Acceptance チェックリスト
- [ ] sparse-volume release directory/ZIPがstrict verify（parity）
- [ ] surface/hydrology/environment releaseが依然strict verify（regression）
- [ ] missing/extra/version/operator/AABB/tile checksum改変/future capability拒否
- [ ] cancel cleanup
- [ ] budget（operator/index/expand/palette）
- [ ] v1 golden / Release1 / 既存Release2回帰不変
- [ ] docs/example/roadmap/task-index同期

## 停止条件
- format core意味変更（offline-tile schema拡張等）が必要 → 停止
