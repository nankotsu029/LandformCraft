# Task capsule V2-12-05 — v2 default switchover and v1 deprecation

補助資料。正本は `docs/design-v2/task-index.md` と `docs/adr/0035-v1-retirement-governance.md`。Git管理外。

## 1. Task Index 対象Task全文（要約転記）

- 状態／Phase／優先度: 完了（2026-07-21、人間承認 nankotsu029）／V2-12／P1。
- 目的／前提: CLI／Paperの既定経路をv2へ切替え、v1を明示flag付きdeprecatedへ移す。前提は`V2-12-01`〜`V2-12-04`、および`V2-11-06`。
- Scope／成果物: default routing切替（Paperは`/lfc <verb>`既定＋`/lfc v1 <verb>` opt-in、CLIは位置非依存global option `lfc --v1 <verb>`）、v1のdeprecation表示、v1→v2機能カバレッジ監査（ADR 0035 D2a-R5のprovider同等性検査を含む）、ADR 0035 D10 command inventory（削除verbと維持verbの両方）、D10維持verbのv2 backend接続、user／admin／README／Quickstart全面更新、CHANGELOG。tagged releaseが無い場合はD5条件1の代替として「v1経路にしか存在しない利用可能機能はゼロ」を証拠付きで確認する。
- 非Scope: v1コード削除（`V2-12-06`）、catalog昇格、新機能。
- Gate: 既定操作が全てv2で完走し、v1がopt-in化され、カバレッジ監査で劣化なし（またはD6で明示承認済み）、D10 command inventoryが揃い、人間承認が得られれば完了。**未承認のカバレッジ劣化があれば停止**し、ADR 0035をamendするまで`V2-12-06`へ進めない。

## 2. Execution Guide 必須事項

- 1回1 Task。後続Taskへ進まない。Acceptanceを弱めない。
- 停止条件（§9）: 「予定外の大規模subsystemが必要になる」「Task contractと矛盾し互換性を保った一意な解決ができない」。
- 停止時は未完を完了と書かず、blocking evidence／影響／追加Task案だけを報告する。

## 3. 直接関係するADR要点

- D2a-R4: v1 design／generation orchestration（`PaperWorkflowService` 等）は削除対象。
- D3-K3: custom asset catalogはv2等価物が無いため削除対象から**明示除外**。
- D3-K3末尾: 「`V2-12-05`で他の未カバー機能が判明した場合は**停止し、本ADRをamendする**。」
- D5: deprecation window条件1（tagged release）の代替＝V2-12-05での「v1経路にしか存在しない利用可能機能ゼロ」確認。tagは0件（`git tag`）。
- D6: 明示承認済みの劣化は**WorldEdit単独runtimeの寸法のみ**。範囲外の劣化は停止＋amendment。
- D10: command inventory（削除verbと維持verbの両方）。

## 4. 変更ファイル

- `src/main/java/.../paper/LandformCraftCommand.java`（default routing、`v1` opt-in、deprecation表示）
- `src/main/java/.../cli/LandformCraftCli.java`（`--v1` global option）
- `src/main/resources/plugin.yml`、`config.yml`
- `docs/commands.md`、`docs/admin-guide.md`、`README.md`、`CHANGELOG.md`

## 5. 凍結対象

v1 Schema、generator `3.0.0-phase6`、Release format 1、v1 placement／Undo、既存checksum／golden。本Taskはv1の**意味**を変更しない（routing既定とdeprecation表示のみが許可範囲）。

## 6. Acceptanceチェックリスト

- [x] 既定操作が全てv2経路で完走
- [x] v1がopt-in化（`/lfc v1`、`lfc --v1`）
- [x] v1→v2カバレッジ監査を現在のコードから再実施
- [x] D10 command inventoryを作成
- [x] カバレッジ監査で未承認劣化なし（F1〜F3解消、F4はD6承認範囲内）
- [x] D10維持verbのv2 backend接続
- [x] 人間承認（nankotsu029）

## 7. 必須テスト

default routing E2E、v1 opt-in回帰、deprecation表示、v1 golden不変、full `test`／`build`、diff check。

## 8. 停止条件

初回は未承認のカバレッジ劣化で停止した。ADR 0035 rev.4と`V2-12-08`〜`V2-12-10`完了後の再監査で解消済み。再監査証拠は`docs/design-v2/audits/v2-12-05-v1-to-v2-coverage-audit-rerun.md`。

## 9. 現在の関連差分と失敗テスト要約

default routing／v1 opt-in／deprecation／helpと関連docsを変更。Schema、Release format、catalog、v1 generator／placement／Undoは変更なし。
