# V2-6-16 — CANCELLED (2026-07-19)

Task IDs V2-6-16 / V2-6-17 are disabled (not renumbered).

## Why
Typical 8 GiB hosts cannot close full E2E containment → settle → full verify → Undo for 500×500 Paper placement within acceptable wall-clock/memory. Unmeasured sizes must not become catalog `SUPPORTED`.

## Cleanup done
- Measurement runner, Gradle measurement server tasks, FiveHundred fixture/exporter removed
- `measured-candidate-max-width/length` default = 64 (smoke-sized)
- Docs/roadmap/task-index: Next = `V2-6-18`
- Keep: settle verify slice budget fix, `saveApplied` ordering fix

## Do not
- Start `V2-6-18` in this change
- Revive these Task IDs; add a new ID if re-measurement is needed
