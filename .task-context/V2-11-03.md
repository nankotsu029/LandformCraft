# V2-11-03 Docs／Schema／example consistency sync

## 1. Task（task-index.md §11 より）

- **状態／Phase／優先度:** 未着手／V2-11／P2。2026-07-20全体再監査で追加。
- **目的／前提:** 監査で検出した文書・Schema整備不足を実装と一致した表現へ同期する。前提なし（`V2-6-19`完了済み）。
- **Scope／成果物:** Quickstartの`sandy-coast-001`生成→`rocky-coast-001`配置の誤記修正、console confirmation token説明の追記、roadmapの古いanchor参照3件の修正、Schema 2件へのtitle・139件へのdescription追加（validation挙動不変）、feature-support catalog Schemaの共通Schema検証への追加、Schema／exampleのinventory-driven検証（disk列挙とtest対象の一致検査）、任意でdocs anchor checker。
- **非Scope:** Schema semantic変更、契約version変更、新機能docs、roadmap進捗状態の変更、64×64以外を`SUPPORTED`と表示する記述の追加。
- **主変更:** `docs/`（Quickstart／user／admin／roadmap anchor）、`schemas/`のtitle／description、`SchemaContractTest`等の共通検証、（任意）`scripts/` checker。
- **必須test／D/M/S:** 全Schemaのtitle／description presence検査、inventory完全性検査、既存example round-trip・checksum不変、v1 golden不変。
- **Gate／docs／次／停止:** presence／inventory検査が通り、既存checksum・example検証が全て不変なら完了。description追加がstrict検証やchecksumへ影響する場合は停止して影響範囲を報告する。

## 2. Task Execution Guide 必須事項

- 1回1 Task。後続（`V2-11-04`）へ進まない。
- Acceptance gateを弱めない。未完を完了と書かない。
- 作業後に対象test、`./gradlew test`、`./gradlew build`、`git diff --check`、`git status --short`。
- `.task-context/`はcommitしない。

## 3. 調査で確定した現状（実装との差分）

1. `docs/quickstart.md` §2 は `examples/sandy-coast` から `sandy-coast-001` を生成するが、§3 は `rocky-coast-001` を配置している（誤記）。
2. §3 の confirmation token 説明にCONSOLE運用（playerまたはCONSOLE、click copyのみ）の記述が薄い。
3. `roadmap.md#terrain-generation-v2` 参照が3件（`docs/architecture.md:222`、`docs/limitations.md:28`、`docs/terrain-design-guide.md:109`）。roadmapの見出しは `## Terrain Generation v2（5トラック）` へ変更済みで、この anchor は解決しない。
4. `schemas/` は141件。`custom-asset-catalog-entry.schema.json` と `custom-asset-metadata.schema.json` に `title` 無し、141件中139件に `description` 無し。
5. `schemas/feature-support-catalog-v2.schema.json` だけが `StructuredDataValidator.BUNDLED_SCHEMAS`（140件）に無い。そのため drift しており、`support.paper_apply` 他5列が `const: UNSUPPORTED` のままで、`V2-11-01` が昇格した4 entry（`SANDY_BEACH`／`BREAKWATER_HARBOR`／`HARBOR_BASIN`／`ROCKY_CAPE`）を持つ sealed example が **125件のschema違反**になる。
6. `examples/` 配下の`.json`／`.yml` 178件のうち14件がどのsource／docsからも参照されていない（inventory 欠落）。

## 4. 変更候補ファイル

- `docs/quickstart.md`、`docs/architecture.md`、`docs/limitations.md`、`docs/terrain-design-guide.md`
- `schemas/*.schema.json`（title／description、feature-support-catalog の5列）
- `src/main/java/.../validation/StructuredDataValidator.java`（bundle追加＋inventory公開）
- `src/main/java/.../format/v2/catalog/FeatureSupportCatalogCodecV2.java`（共通schema検証の適用）
- `src/test/java/.../format/SchemaContractTest.java`（presence／inventory／example coverage）
- `docs/roadmap.md`、`docs/design-v2/task-index.md`（本Taskの完了記録）

## 5. 凍結対象

- v1 Schema、generator `3.0.0-phase6`、Release format 1、v1 placement／Undo、既存checksumとgolden。
- feature support catalog の canonical checksum と昇格範囲（64×64／`surface-2_5d` 4 entry）。
- 契約version（`contractVersion`／`catalogVersion`）と全schemaのvalidation semantics。

## 6. Acceptanceチェックリスト

- [ ] Quickstart §2→§3 のrelease ID一致と token 説明追記
- [ ] anchor参照3件が解決する
- [ ] 全141 schemaに非空の `title` と `description`
- [ ] feature-support catalog schema が共通検証へ入り、sealed example が通る
- [ ] schema inventory（disk＝bundle＝packaged resource）検査
- [ ] example inventory（disk列挙＝参照済み）検査
- [ ] 既存 example round-trip／checksum 不変、v1 golden 不変

## 7. 必須テスト

- `./gradlew test --tests '*SchemaContractTest*'`
- `./gradlew test --tests '*FeatureSupportCatalog*'`
- `./gradlew test --tests '*V1CompatibilityGoldenTest*'`
- `./gradlew test` / `./gradlew build`

## 8. 停止条件

- description／title 追加が strict 検証、canonical JSON、checksum のいずれかへ影響する場合。
- feature-support catalog schema の同期に semantic 変更（契約version変更や昇格範囲拡大）が必要になる場合。
- 64×64 以外を `SUPPORTED` と表現する必要が生じた場合。

## 9. 現在の関連差分と失敗テスト要約

- 作業開始時の `git status --short` は `.task-context/` と `.agents/skills/` の未commit追加のみ（本Taskの変更前）。
- 既知の未解決（本Task外、`V2-11-02` 記録）: `PlacementPhaseGateV2Test.release2LifecycleAppliesVerifiesAndUndoesDeterministically` が full suite で約4回に1回 `RejectedExecutionException: I/O concurrency limit reached` で落ちる既存 race。
