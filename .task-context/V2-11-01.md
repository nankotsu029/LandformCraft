# Task capsule: V2-11-01 Paper apply capability promotion and Track D／E connection

## Task Index全文（正本: docs/design-v2/task-index.md）

- 状態／Phase／優先度: 未着手／V2-11／P1。2026-07-20全体再監査で、catalog／testに本Task相当の64×64能力昇格が既に含まれる一方、正本（roadmap／task-index）は未着手のままという矛盾を検出した。既存昇格コードを正式成果として完成させるか未昇格へ差し戻すかを確定する。
- 目的／前提: smoke実測済み範囲（64×64、WorldEdit 7.3.19／FAWE 2.15.2）のRelease 2 placementについて、catalogの`paper_apply`／`post_apply_validation`／`snapshot`／`rollback`／`restart_recovery`を証拠へbindして昇格し、Track D（V2-9）／E（V2-10）の共通canonical stream互換を能力表へ反映する。前提は`V2-6-19`と`V2-6-06`〜`V2-6-10`（すべて完了）。
- Scope／成果物: catalog data／sealed exampleの更新（dimension limit 64×64のまま）、evidence link、false-promotion corpusの更新、README／user docs／taxonomy／limitations同期。
- 非Scope: 500／1000寸法の昇格、新featureのPaper adapter、Release format変更、Beta hardening項目の完了化。
- 必須test／D/M/S: catalog consistency verifier、sealed example round-trip、昇格entryのevidence link照合、未測定寸法／未接続featureの昇格拒否回帰、v1回帰。
- Gate／停止: 昇格が実機evidenceの範囲に限定され、catalog検証とfull suiteが通れば完了。evidenceのない昇格が必要になれば停止する。

## 前提確認

- roadmap Track A `次のTask` = `V2-11-01`、task-index前提 `V2-6-19`／`V2-6-06`〜`V2-6-10` = 完了。矛盾なし。
- `V2-6-21`（P0再オープン）は2026-07-20に再完了済みで、catalogを昇格していない。

## 着手時点の事実（コード調査）

再監査の指摘どおり、実装・test側はすでに昇格済みだった。

- `BuiltInFeatureSupportCatalogV2`: `PROMOTION_DECISION_ID = v2-11-01-paper-capability-promotion`、`PAPER_SMOKE_EVIDENCED_CAPABILITIES = {surface-2_5d}`、`REQUIRED_RUNTIME = paper-1.21.11+worldedit-7.3.19|fawe-2.15.2`。
- sealed example（`examples/v2/catalog/feature-support-catalog-v2.json`、71 entry）: `paper_apply` SUPPORTED 4／EXPERIMENTAL 21／UNSUPPORTED 46。SUPPORTEDはSANDY_BEACH／BREAKWATER_HARBOR／HARBOR_BASIN／ROCKY_CAPE（全て`surface-2_5d`）。
- `FeatureSupportCatalogConsistencyVerifierV2`: SUPPORTED Paper列にsmoke-evidenced prefix／宣言runtime／`smoke`を含むevidence linkを必須化し、Release capability path無しのPaper主張を拒否。dimension limit 64×64固定と65拒否を回帰。
- `FeatureSupportCatalogV2Test#refusesChildOnlyStandaloneAndPaperFalsePromotion`: false-promotion corpus（`PLAIN`／`MEANDERING_RIVER`の構造的拒否、偽装evidence付き`MEANDERING_RIVER`のverifier拒否、child-plan standalone拒否、500寸法limit拒否）。
- `PlacementPhaseGateV2Test#paperCapabilitiesMatchSmokeEvidenceAndHardLimitStaysSmokeMeasured`: prefix別の期待levelとhard limit 64×64／500・1000拒否。

## 判断

**既存昇格を正式成果として確定する**（差し戻さない）。根拠は`V2-6-14`（WorldEdit 7.3.19）／`V2-6-15`（FAWE 2.15.2）の64×64実機smokeと [V2-6 Phase gate audit](../docs/design-v2/audits/v2-6-phase-gate.md) が、`surface-2_5d`×64×64の範囲でTask contractのevidence要件を満たすため。範囲外（他prefix、foundation entry、500／1000）は一切昇格しない。

## 本Taskの作業（docs／正本同期）

コード・testは要件を満たしていたため変更なし。正本と利用者向けdocsを昇格後の事実へ同期した。

1. `docs/roadmap.md` — Track A表（次は`V2-11-02`）、V2-11進捗（1/6）、Phase表、`V2-11-01`証拠段落を追加。
2. `docs/design-v2/task-index.md` — `V2-11-01`を完了・判断根拠・gate証拠へ更新、Phase表の次Taskを`V2-11-02`へ。
3. `docs/design-v2/terrain-feature-taxonomy.md` — Paper 5能力列の正本記述を昇格後へ。
4. `docs/design-v2/current-limitations.md`／`docs/limitations.md`／`docs/commands.md`／`README.md`／`docs/beta-release-checklist.md`／`docs/design-v2/migration-plan.md`／`examples/v2/catalog/README.md`／`CHANGELOG.md`。

## 検証

- `./gradlew clean build --rerun-tasks`: BUILD SUCCESSFUL、924 tests／0 failures／0 errors（skipped 4）。
- `git diff --check`: clean。
- Schema／example checksum、Release format、v1 golden、Release 1 verifierは不変。

## 次

`V2-11-02`（R2 placement dimension guard and measurement profile、P0）。本Taskからは開始しない。
