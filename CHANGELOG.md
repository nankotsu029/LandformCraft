# Changelog

## Unreleased — 2026-07-17 redesign (offline v2 track only)

### Fixed

- V2-6-21 P0再オープン（2026-07-20）: Release 2 operation stateをprepared／confirmed／appliedのcommit marker付きwrite-ahead順序へ変更し、fsync・atomic publish・strict read-backと全12 write point×before／after failpointを固定。confirm／apply間restart、Undo reservationの同placement置換と第三者overlap拒否、失敗時のapply lease復元、Recovery retry、baseline exact restoreをproduction file storeで検証した。R2 commandはoperator Player／CONSOLE／RCON限定、actor bindingとworld allow／deny・missing worldをstable domain error化し、permission-aware completionからdeny world／tokenを除外した。v1、Release format、Schema、example、support catalogは不変。

### Changed

- V2-12-06 v1 removal and renaming — **完了（人間承認 nankotsu029、2026-07-21）**。[ADR 0035](docs/adr/0035-v1-retirement-governance.md) rev.5の未公開project限定例外により30日条件だけを免除し、D8、R1〜R8限定、D2b／D3維持境界、識別子凍結、Acceptance testを維持した。D8 preflightは2つの既知runtime rootをbyte-exact neutral archiveへ非破壊保存し、strict read-back／inventory SHA-256／unresolved 0でPASS（[evidence](docs/design-v2/audits/v2-12-06-v1-retirement-evidence.md)）。D2cの汎用`deleteTree`を`FileTreeOperations`へ抽出後、D7順でv1 command、design／generation orchestration、provider adapter、placement／Undo／recovery、Release 1／design-v1 publisher、generator `3.0.0-phase6`、V2-0 spineを削除した。`/lfc r2`／`/lfc v1`／`--v1`／`landformcraft.r2.*`を削除し、default v2と恒久的な`v2`明示aliasを維持。v1専用Schema／fixtureはactive inventoryから`src/main/resources/legacy/v1/`へ移設し、packaged JARでv1 intent／design package／Release 1のread／verify／migrateが継続する。v1 goldenはimmutable archive＋v2 migration/equivalence testへ変換。custom asset、version中立基盤、`asset`／`doctor`／`job`／`recovery`／`version`／`help`は維持。Release 2 contract／semantic checksum／catalog supportは不変。targeted regressionとfull `./gradlew test`（985件、6 skip）が通過。次は`V2-12-07`（未着手）。

- V2-12-05 v2 default switchover and v1 deprecation — **完了（人間承認 nankotsu029、2026-07-21）**（[ADR 0035](docs/adr/0035-v1-retirement-governance.md) D5）。**既定経路をv2へ切替えた。** Paperは`/lfc <verb>`＝`/lfc v2 <verb>`、CLIは`lfc <verb>`＝`lfc v2 <verb>`。v1は明示opt-inのみ: Paper `/lfc v1 <verb>`、CLIは既存parserに合わせた位置非依存global option `lfc --v1 <verb>`。v1／r2経路とv1挙動は**不変**で、実行時にdeprecation警告を出す（削除は`V2-12-06`）。version中立の維持verb（`help`／`version`／`doctor`／`ops`／`asset`／`selection`）はversionを問わず既定面に残す。**再開の前提**として、task-index指示どおりv1→v2カバレッジ監査を最初からやり直し（[再監査](docs/design-v2/audits/v2-12-05-v1-to-v2-coverage-audit-rerun.md)）、前回STOPの原因F1〜F3が`V2-12-08`〜`V2-12-10`で解消、F4がADR 0035 D6 rev.4で明示承認済みであることを確認した。`git tag`は0件のためD5条件1の代替判定「v1経路にしか存在しない**退役対象**機能はゼロ」を証拠付きで確認した（`asset`＝K3、`verify`／`design-verify`＝D2b legacy readerは維持対象であり退役対象ではない）。ADR 0035 D10 command inventoryの最終形（削除verbと維持verbの両方、Paper／CLI全verb）を再監査§7へ記録した。routerは1 verbが直接形とoperation形を併せ持つ場合に対応済み（`V2-12-09`）。CLI `--v1`をParsedInvocationのglobal optionへ追加し、Paper `onCommand`とCLI `run`をversion中立verb→v1 opt-in→v2明示→**既定v2**の順へ再構成した（Paperは`handleLegacy`、CLIは`legacy()`へv1 dispatchを分離）。Paperのtab completionと両surfaceのhelpを既定v2へ更新した。**破壊的変更**: 既定面の文法がv2になったため、旧v1 verbは`/lfc v1`／`--v1`が必要になる（v1固有の`validate`／`verify`／`design-verify`、v1文法の`export`／`generate`等）。既存の破壊対象testはopt-in形（`--v1`／`/lfc v1`）へ更新して回帰を維持した（`LandformCraftCliTest`、`PluginArtifactTest`、`LandformCraftCliV2Test`）。catalog昇格・新機能・v1コード削除は行っていない（削除は`V2-12-06`）。docs全面更新: README、`docs/quickstart.md`（v2既定flowへ書換）、`docs/commands.md`（既定v2節＋v1→既定対応表）、`docs/admin-guide.md`、`docs/operations.md`。test: 上記回帰更新に加え`LandformCraftCliV2Test`へ既定surface routing／`--v1` opt-in／v1固有verbが既定面に無いことのE2E 4件を追加。full `./gradlew test`／`./gradlew build`が通り、v1 golden・Release format・Schema・placement contract・support catalogは不変。次は`V2-12-06`（本Taskからは開始しない。人間承認を要する）。

### Added

- V2-7-07 Multi-source reconciliation + Phase gate — **完了（2026-07-21）**。`MultiSourceReconciliationServiceV2`（優先順位凍結・HARD/SOFT peer fail closed・source-to-result差分／preview）と[V2-7 Phase gate audit](docs/design-v2/audits/v2-7-phase-gate.md)。抽出経路はSUPPORTED候補、`EXPERIMENTAL`・CLI／Paper／Request未接続・Release capability追加なし。Track B完了。
- V2-7-06 Zone and sketch label extraction — **完了（2026-07-21）**。`ImageZoneLabelExtractorV2`（固定palette距離量子化→zone draft、ambiguous／遠色／alpha UNKNOWN）と`ExtractedZoneLabelPromotionServiceV2`（明示閾値＋V2-1 `ZONE_LABEL_MAP`経路）を追加。Schema／`examples/v2/extract/`を同期。CLI／Paper／Request未接続、`EXPERIMENTAL`のまま。次は`V2-7-07`。
- V2-7-05 Height guide extraction — **完了（2026-07-21）**。`ImageHeightGuideExtractorV2`（integer-only輝度→U8 draft、no-data／254 clamp）と`ExtractedHeightGuidePromotionServiceV2`（3種`HeightValueMeaning`明示＋V2-1 decoder／residual一貫）を追加。Schema／`examples/v2/extract/`を同期。CLI／Paper／Request未接続、`EXPERIMENTAL`のまま。次は`V2-7-06`。
- V2-7-04 Explicit promotion to constraint map — **完了（2026-07-21）**。`ExtractedMaskPromotionServiceV2`がconfidence閾値とUNKNOWN処理（`REJECT`／`MAP_TO_WATER`／`MAP_TO_LAND`／`MAP_TO_NODATA`）の明示指定を必須とし、U8 grayscale `land-water.png`をstaging→`SecureConstraintMapSourceLoader`＋`NumericPngDecoder`再検証→`extracted-mask-promotion-v2.json` provenanceとatomic publishする。Schema／`examples/v2/extract/`を同期。自動／暗黙昇格・AI補完・CLI／Paper／Request／Release未接続、`EXPERIMENTAL`のまま。次は`V2-7-05`。
- V2-7-03 Draft artifact and confidence preview — **完了（2026-07-21）**。`ExtractedMaskDraftArtifactPublisherV2`（`classes.u8`／`confidence.u8`＋strict index、staging→read-back→atomic publish）と`ExtractedMaskDraftPreviewRendererV2`（class／confidence／unknown固定palette PNG、1枚ずつbounded render、strict preview index）を追加。Schema `extracted-mask-draft-v2.schema.json`／`extracted-mask-draft-preview-index-v2.schema.json`と`examples/v2/extract/`を同期。昇格・Release・CLI／Paper／Request未接続、`EXPERIMENTAL`のまま。次は`V2-7-04`。
- V2-7-02 Secure extraction input envelope — **完了（2026-07-21）**。`format.v2.constraint.extract`へ`SecureImageExtractionEnvelopeV2`／`SanitizedArgbImageV2`／`ImageExtractionInputLimitsV2`を追加。retired `ReferenceImageProcessor`と同等のportable path／symlink／hardlink alias／magic／extension／byte／pixel／aspect／APNG multi-frame／EXIF orientation／TOCTOU検査とtrusted ceiling decode／working budgetを実装し、`loadAndExtractLandWater`で実PNG／JPEG→`ExtractedMaskDraftV2`のstrict経路とraw SHA-256 checksum連鎖を固定。`SecureImageExtractionEnvelopeV2Test`で拒否・決定性・cancelを検証。Request Schema・draft artifact・CLI／Paper／AI Provider未接続、`EXPERIMENTAL`のまま。次は`V2-7-03`。
- V2-12-10 v2 operational verb wiring — **完了**（`V2-12-05`のカバレッジ監査が検出したF2（v1の`cleanup`／`journal-verify`／`recovery` read-onlyに相当するv2運用機能がcommandへ未接続）を、[ADR 0035](docs/adr/0035-v1-retirement-governance.md) rev.4 D11のとおり接続で解消した。retention policyそのものは変更していない。**retention（Paper、v1 `cleanup`後継）**: 既存の`core.v2.operations.Release2RetentionServiceV2`を`/lfc v2 retention plan|execute|status`へ接続した。`plan <placement-id>`はplacementのapplied journalからplan＋journalを読み（`release2Placements.status`）、recovery plannerがretention window内と判定したsnapshotのbyte総量を示す1回限りの確認token（10分失効、actor束縛）を発行、`execute <placement-id> <plan-id> <token>`がtokenを消費して削除、`status`がjournal stateとsnapshot byte使用量を表示する。削除対象はrecovery cleanup planが列挙したwindow外terminal snapshotだけで、この判定は`V2-6-13`と同一である。本番配線として、placement統合が有効なとき`Release2PlacementApplicationServiceV2.retentionCleanupPort()`（＝`RetentionCleanupPortV2.from(recovery)`）を`Release2RetentionServiceV2`へ渡すよう`Landformcraft`を変更した（従来のdeferred portはthrowするだけだった。統合が無ければ削除対象も無いためdeferredのまま）。retentionは実行中serverのplacement stateを読みsnapshot state（world blockではない）を削除するためPaper専用で、新permission `landformcraft.v2.retention`を要する。**journal-verify（CLI、v1同verb後継）**: `/lfc v2 journal-verify <placement-journal-v2.json>`がv2 placement journalをstrict検証する。**recovery inspect（CLI read-only、v1 `recovery status|diagnose`のread-only視点）**: `/lfc v2 recovery inspect <journal|plan> <artifact.json>`がjournal state（`RECOVERY_REQUIRED`含む）またはrecovery planのclassificationをread-onlyで表示する。両者は明示artifact fileだけを読みworldを変更しないためCLI専用（Paperからは安定code`V2_CLI_ONLY`）で、v2 recoveryの**実行系**`recover diagnose|plan|execute`（Paper専用）とはverb token（`recover`／`recovery`）で区別される。CLIに`recovery list`（全placement列挙）は設けていない。列挙はoffline store走査を要し、v2の明示path・非推測方針（`v2 verify`／`v2 preview`と同じく明示artifactを取る）に反するため、Paper起動時のrestart-state点検が列挙相当を担う。permission `landformcraft.v2.retention`をplugin.ymlへ追加。test: `Release2RetentionExpiryV2Test`（確認token失効で削除ゼロ、削除対象がrecovery plannerのscopebyte総量だけ。actor束縛・wrong-token拒否は既存`OperationalOperationsV2Test`が担保）、`V2CommandRouterV2Test`追加3件（retention Paper専用、read-only verb CLI専用、`recover`／`recovery` token区別）、`LandformCraftCliV2Test`追加5件（journal-verify positive／corruption、recovery inspect journal／plan、unknown kind、retention CLI拒否）。v1 `cleanup`／`journal-verify`／`recovery`、v1 command、v1 golden、Release format、placement contract、catalogは不変。次は`V2-12-05`の再開（本Taskからは開始しない）。ただし`V2-12-08`〜`V2-12-10`が揃ったため、`V2-12-05`の再開条件（ADR 0035 rev.4 D11）は成立した）
- V2-12-09 v2 job／candidate／export lifecycle — **完了**（`V2-12-05`のカバレッジ監査が検出したF3（v1の非同期job→candidate→確認付きexportにv2等価物が無い）を、[ADR 0035](docs/adr/0035-v1-retirement-governance.md) rev.4 D11のとおりv2実装で解消した。新package `core.v2.job`／`format.v2.job`／`model.v2.job`。`ExportJobServiceV2`が`Release2ExportApplicationServiceV2`を**包む**形で非同期lifecycleを提供し、既存の単発同期`v2 export`／`v2 generate`は一切変更していない。cancelはAGENTS.md §11のとおり連動する: `cancel`がexport pipelineの参照するcancel tokenを立て、`CompletableFuture`をinterrupt付きでcancelし、終端stateを永続化する。publisherはcommit point（atomic move）直前までtokenを観測するため、**取り消したjobはReleaseを公開しない**（testで公開物ゼロを確認）。state は`QUEUED`→`RUNNING`→`PUBLISHED`／`CANCELLED`／`FAILED`で、遷移ごとにsnapshotを書くため再起動後も`job status`が答えられる。終端jobへの再cancelは冪等。新Schema `generation-job-v2.schema.json`＋example `examples/v2/job/export-job-v2.json`（v1 `generation-job.schema.json`は凍結のまま。拡張も再利用もしない）。`ExportJobSnapshotV2`はjobIdをcanonical lowercase UUIDに、requestId／releaseIdをportable slugに、progressを整数millionthsに、updatedAtをcanonical UTC instantに制限する。**candidate列挙**はRelease format 2 manifestがrequest identityを持たない（＝format変更が停止条件）ため、job snapshotの`requestId`を正本にして「そのrequestの公開済みjob」をjob ID順で返す。`ExportPlanStoreV2`がv1の`export plan`→`export create`に相当するreservation-bound confirmation tokenを提供する: plan時にfree diskをadmitし、SHA-256 hashだけを保持（平文token非保持）、constant-time比較、単回使用・10分失効・actor束縛・未確認plan 256件上限で、**plan後にrequestかintentが編集されるとdigest不一致で拒否**する。verbは`v2 export plan`／`v2 export create`（Paper専用。planは実行中processに存在し確認したjobも同processで走るため）、`v2 job status|cancel|list`と`v2 candidate list|info`（両surface。job storeが永続的なためCLIからも読める。CLIの`job cancel`は別processのworkerには届かずdurable snapshotを`CANCELLED`にするだけで、v1 CLIと同じ性質）。routerは1つのverbが直接形とoperation形を併せ持つ場合（`export` と `export plan`／`create`）をoperation名**かつ**arity一致で判定するよう拡張したため、`plan`という名前のfile引数は直接形へ正しく routing される。permission `landformcraft.v2.job`／`.candidate`をplugin.ymlへ追加。job store上限4096件はjobをqueueする前にadmitする。`ExportBudgetV2.requireFreeDisk`をpublicにした（plan時予約のため。挙動は不変）。test: `ExportJobServiceV2Test`（非同期export E2E＝PUBLISHED＋ZIP有無、mid-flight cancelで公開物ゼロ、cancel冪等、再起動相当の durable status、unknown／traversal／非canonical job ID拒否、candidate列挙の決定性、store上限admission、終端state再遷移拒否、snapshot契約拒否、injected clock）、`ExportPlanStoreV2Test`（単回消費、偽造・他plan token・actor不一致・inputs変更・失効・上限・token一意性）、`V2CommandRouterV2Test`追加5件、`LandformCraftCliV2Test`追加4件（durable store読取、malformed job ID、Paper専用拒否、同期export回帰）、`SchemaContractTest`のsealed example読取。v1 job store、v1 command、v1 golden、Release format 2、placement contract、catalogは不変。次は`V2-12-10`）
- V2-12-08 v2 request authoring — **完了**（`V2-12-05`のカバレッジ監査が検出したF1（v1にはあるrequest authoringがv2に無い）を、[ADR 0035](docs/adr/0035-v1-retirement-governance.md) rev.4 D11の方針どおり**劣化を承認せずv2実装で**解消した。新しいBukkit非依存の`core.v2.command.V2RequestStoreV2`が、v2 requestのcreate／bounds／constraint-map宣言／prompt／listを担う。`GenerationRequestV2`契約は不変で、各操作は現在のrequestを読み1 fieldだけ差し替えて`LandformV2DataCodec`経由で書き戻すため、strict schema検証とatomic publishを毎回通り中途状態が残らない。verbは`v2 request create <id>`（既定128×128／y -32..160／water 62／seed 0／tileSize 64、既存IDは上書きせず失敗）、`bounds <id> <w> <l> <min-y> <max-y> <water>`（waterは新範囲へclamp）、**Paper専用**`selection <id>`（現在のWorldEdit選択範囲からwidth／length／min-y／max-yを取り込み、保存済みwater levelはclampして維持。CLIからは安定code`V2_PAPER_ONLY`）、`constraint-map <id> <slug> <file> <sha256> <w> <l>`、`prompt <id>`（Paperは次のchat 1件を取込。5分失効・`cancel`取消）／`prompt <id> <text...>`（CLI）、`list`。`validate`／`info`の意味とpath引数は不変。`constraint-map`は`surface-2_5d` exportが「宣言済みconstraint map source ちょうど1件」を要求するため必要で、これが無いとauthoringした request が `v2 export` へ到達できない。canonical形（U8 grayscale・north-west原点・east/south軸・pixel中心・回転／反転なし・全面crop・`0=water` `1=land`・no-data禁止）に固定し、それ以外の形式は推測せず手書きJSONのままとする。routerは1つのoperation tokenがsurfaceごとに異なるarityを持つ場合（`prompt`）にcaller surface→arityの順で解決するようになり、各surfaceが自分のusageを受け取る。permissionは`landformcraft.v2.request.create`／`.edit`をplugin.ymlへ追加（read系は既存の`.request`）。promptはv1 CLIと同じcredential filterを**store側**に持つため、v1 Paperのchat取込より厳しい。request IDはlowercase portable slugのみで、pathもtraversalもsymlinkも受け付けない。test: `V2RequestStoreV2Test`（既定値／上書き拒否／bounds clamp／selection water保持／contract違反時の無変更／constraint map canonical形・置換・unsafe path・digest・aspect拒否／prompt secret拒否／list決定性／2 root間のbyte一致determinism／slug・traversal・symlink・誤名・corruption拒否）、`V2CommandRouterV2Test`（新verb routing、surface別prompt解決、`selection`のPaper専用、operation重複排除、completion）、`LandformCraftCliV2Test`（create→bounds→constraint-map→prompt→list、**authoringしたrequestが`v2 export`で`placementEligible: true`まで到達するE2E**、重複・unsafe ID・secret prompt・arity・surface拒否）。v1 request store、v1 command、v1 golden、Release format、catalogは不変。次は`V2-12-09`）
- V2-12-04 v1→v2 migration tool — **完了**（新package `core.v2.migration`／`format.v2.migration`／`model.v2.migration`。`LegacyMigrationApplicationServiceV2`が、operatorが明示した1資産（v1 `terrain-intent.json`／v1 design package directory／Release format 1 directory・ZIP）をstrict readし、v2 design package＋変換reportからなるmigration bundleへ変換する。CLIは`lfc v2 migrate inspect`（dry-run）と`lfc v2 migrate apply ... <strict|accept-lossy>`。**写すのは`schemaVersion`と`theme`だけ**である: `TerrainIntentV2.Feature`はnormalized geometryを必須とし、v1のzoneは`preferredArea`（8方位＋CENTER＋ANY）と`areaShare`しか持たないため、topology／seaSides／landRatio／relief／coastline／water／zone／structureからv2 featureやconstraintを作ることは位置・形状の発明にあたる。これらは`migration-report-v2.json`へ1件ずつ理由付きで列挙し、v2 intentには入れない（task-index `V2-12-04` Scope「欠落はdraft／未指定のまま」、AGENTS.md §6）。Release 1のtile schematic／structures／assets／previewも、v2 Blueprintのmodule・stage・field descriptorを持たないため非対応elementとして報告し、移行後のintentから`lfc v2 export`で作り直す。したがって実質すべてのv1資産がlossyであり、`apply`は`strict`（1件でも非対応があれば拒否）か`accept-lossy`の明示を必須とし、既定でlossy変換が通ることはない。ADR 0035 D9の10条件を実装で満たす: sourceは読むだけ（非破壊）、dry-run提供、既存bundleを上書きせず失敗、同一sourceから同一checksum（job UUIDをsource digestから導出、timestampはv1 audit由来かepoch）、report出力、元version＋checksum記録（canonical checksumを持たないsourceはbyte digest、Release 1は`checksums.sha256`のdigest）、staging→strict read-back→atomic move→published read-backで部分publishなし、unknown version／未定義fieldをstrict codecでfail closed拒否、corrupted artifact拒否、結果をRelease 2 design strict verifier（`DesignArtifactVerifierV2`）＋bundle checksumsで検証。ADR 0035 D2b／R7の先行整備として、v1 contract schema 8件のimmutable copyを`src/main/resources/legacy/v1/contracts/`へ配置し、`StructuredDataValidator`を`/schemas/` → `/legacy/v1/contracts/` のfallback解決へ変更（active copyは削除せず、削除は`V2-12-06`）。移行intentは`provenance.source = UPGRADED_V1`／`UNCONFIRMED`、design packageは`pathKind = IMPORT`。routing契約へ`Surface.CLI`と安定code`V2_CLI_ONLY`を追加し、`migrate`はPaperのroutingにもtab completionにも現れない。新Schema `migration-report-v2.schema.json`、example `examples/v2/migration/`（sealed reportと移行済intent）。v1 Schema・generator・Release format 1・v1 placement／Undo・v1 commandは不変で、catalogの能力・寸法昇格も行っていない。test: `LegacyMigrationApplicationServiceV2Test`（dry-run／strict拒否／publish＋R2 strict verify／sealed example一致／determinism／非破壊・非overwrite／unknown version・未定義field・corruption／mislabelled source／v1 design package／Release 1／改竄bundle拒否）、`LegacyMigrationPackagedJarV2Test`（ADR 0035 D2b Acceptance 6: active v1 schemaとv1 orchestration classを隠すClassLoader下でのpackaged-JAR migration＋legacy contract copyのbyte一致）、router／CLI回帰。**未解決**: `docs/design-v2/migration-plan.md` §6「自動変換できるもの」は推測値を`SOFT`＋provenanceで書き出す方針であり、本実装の保守方針と差異がある。現行`TerrainIntentV2`契約ではgeometryにstrengthが無く§6は実装不能なため、Scope／Gate／AGENTS側に従った。方針決定が必要（同§に記録）。次は`V2-12-05`）
- V2-12-03 CLI／Paper v2 command routing and E2E — **完了**（実装・自動test・実機64×64 smokeすべて合格。[evidence](docs/design-v2/audits/v2-12-03-v2-command-path-evidence.md)）。新package `core.v2.command` にBukkit非依存の共有routing契約（`V2CommandVerbV2` verb表、`V2CommandRouterV2`、`V2CommandRouteV2`、`V2CommandErrorCodeV2`、offline verb backendの`V2WorkflowServiceV2`）を追加し、`/lfc v2 <verb>`（Paper）と`lfc v2 <verb>`（CLI）が同一文法・arity・permission nodeを共有する。ADR 0035 D5どおり`/lfc r2 <op>`は同一semanticのdeprecated aliasとしてrouterがcanonical形へ翻訳し実行時に警告を出す（placement lifecycleの実装は1本のまま）。verbは request／design／generate／export／preview（offline、CLI＋Paper。design＝`V2-6-11`、export＝`V2-12-02`のproduction service）と place／status／undo／recover（Paper専用、CLIからは安定code`V2_PAPER_ONLY`で拒否）。`generate`は`export`のZIP無し版で、baseline `<land|water> <land-y> <water-y>` は非推測のため必須。Paperは`PaperV2WorkflowServiceV2`が`data/v2/`配下へoperator pathをsandbox化（絶対path・`..`・backslash・symlink拒否）し、bounded executorで実行してmain threadを塞がない。permissionはADR 0035 D4の明示承認どおり`landformcraft.v2.*`へ改名し、旧`landformcraft.r2.*`をdeprecation window中の同権限として受理（削除は`V2-12-06`）。plugin.ymlへv2 node 10件を追加。tab completionはpermission／surface／world policyを尊重し、全失敗経路に安定error codeと`v2CorrelationId`が付く。v1 commandのrouting・出力・permissionは未変更。64×64 coastal fixture `harbor-cove-64` をexampleへ追加（Azureのnormalized geometryを保持しblock単位parameterのみ再調整）。test: `V2CommandRouterV2Test`（canonical routing／alias 1対1翻訳／permission改名／surface拒否／arity・unknown tokenの安定code／correlation ID／completion）、`LandformCraftCliV2Test`、`V2CommandPathE2EV2Test`（fake gateway E2E: request→export→64×64 place plan/confirm/execute→status→undo→`UNDONE`＋寸法gate拒否）。実機smoke（2026-07-20、Paper 1.21.11＋WorldEdit 7.3.19単独、64×64）: production `lfc v2 export`のReleaseを`/lfc v2 place plan→confirm→execute`で`APPLIED`（settle＋effect envelope全体のexact verify成功）、`/lfc v2 status`／`/lfc r2 status`（deprecation警告）、`/lfc v2 undo plan→execute`で`UNDONE`。本実行で判明したrunnerの欠陥3件（fill guardが`No blocks were filled`をfalse positiveで通す、RCON応答を証拠化していない、deprecation警告の二重スラッシュ）を修正した。v1 command不変のlive証拠は未取得で、自動testで担保している。併せて、full suiteで間欠的に出ていた`I/O concurrency limit reached`を根本解決した: `DesignResponseCacheTest`が`ioConcurrency=2`、`PlacementPhaseGateV2Test`のrun-bが`ioConcurrency=1`で、fail-fast admissionに対して過小だった（`GenerationExecutors.create`の第1引数はioConcurrency）。determinismの軸であるgenerationParallelismは変えずI/O admissionだけを引き上げ、紛らわしかった`runLifecycle`の引数名も実際の並びへ修正した。production契約は不変。次は`V2-12-04`）
- V2-12-02 Production Release 2 export path — **完了**（新package `core.v2.export`。`Release2ExportApplicationServiceV2`がCLI／Paperから呼べる唯一の正式export APIで、sealed `GenerationRequestV2`とdesign stageの`TerrainIntentV2`（`Release2ExportRequestV2.fromDesign(...)`で`TerrainDesignApplicationServiceV2`の出力から直接構築可）だけを入力に、Blueprint compile → coastal generation／transition composition → constraint field sidecar（desired／actual／residual land-water `LFC_GRID_V1`＋strict index）→ intentのLAND_WATER_MASK rebindとBlueprint再compile（coastal幾何が変われば fail closed）→ field-only `CoastalValidatorV2`（HARD違反はexport不可）→ 固定11 diagnostic preview → `TilePlanV2`幾何のSponge v3 tile → `ReleaseSurfacePublisherV2`（staging→strict read-back→atomic publish）を実行。publish後に`ReleaseSurfaceVerifierV2`でdirectory／ZIP manifest一致を再確認し、`ReleasePlacementEligibilityVerifierV2`を通過した場合だけ`Release2ExportResultV2`をseal。tile幾何を`TilePlanV2`正本に合わせたため、publish済みReleaseは`V2-6-20`の`VerifiedReleaseCanonicalBlockSourceV2`でそのまま開いて`PlacementPlanCompilerV2`のplacement planをcompileでき`requirePlanMatches`が通る（world mutationなし）。coastal featureが所有しないcellのland-water／地表高は推測せず`SurfaceBaselineV2`で呼び出し側が宣言し、`ExportBudgetV2`がtile数とdense descriptor working setをartifact生成前にadmit。LARGE（1024超）、4 coastal contributor未満の構成、複数LAND_WATER_MASK bindingはfail closedでreject。新capability・新artifact format・Schema変更・v1変更はゼロ。CLI／Paper command routingは`V2-12-03`。証拠: `Release2ExportApplicationServiceV2Test`（E2E＋placement plan／design→export接続／determinism／mid-pipeline cancel cleanup／tile・resident budget拒否）。次は`V2-12-03`）
- V2-12-01 v1 retirement governance ADR／互換性policy改訂 — **完了**（[ADR 0035](docs/adr/0035-v1-retirement-governance.md) をrev.3で `Accepted`（2026-07-20、承認者 nankotsu029）。コード・Schema・example・catalogの変更はゼロ。D1: v1凍結の解除にはADR `Accepted`とD2前提Taskの両方が必要で、v1契約に触れてよいのは`V2-12-05`／`V2-12-06`のみ。D2: 削除可能なv1資産をR1〜R8（generator `3.0.0-phase6`、Release format 1、v1 placement／Undo／recovery、design／generation orchestration、v1 provider adapter、v1 command、v1専用Schema／example、V2-0互換spine）へ限定し各行にv2カバレッジと前提Taskを対応付け。D3: 維持対象K1〜K5を定義し、**custom asset catalogはv2等価物が無いため削除対象から明示除外**してGateの停止条件を成立させない。D4: Java識別子の`V2` suffix除去は自由、Schema `$id`／schemaファイル名／capability名／artifact type・version／module・stage ID／named seed／contract ID／generator version／error code／metric labelは凍結し、明示承認する識別子変更はpermission node `landformcraft.r2.*`→`landformcraft.v2.*`とcommand名の2件のみ。D5: `/lfc r2`→`/lfc v2 <verb>`（`V2-12-03`で正式化）→`/lfc <verb>`既定（`V2-12-05`）→v1経路・alias削除（`V2-12-06`）の4段階と、tagged release／30日／未承認劣化ゼロ／人間承認の4条件からなるdeprecation window。D6: 既知の唯一のカバレッジ劣化＝WorldEdit 7.3.19単独runtimeの64×64を寸法限定で明示承認。D7: 削除順序と例外条件。D8: v1 job／journal／Undo／recovery／cleanup stateのdrain gate（fail closed、preflight証拠必須）。D9: migration toolの品質条件10項目。rev.2（第1回レビュー反映）でD2をD2a（writer削除）／D2b（legacy compatibility reader維持）／D2c（構造問題）へ分割し、Release format 1 verifierを削除対象から外してmigration専用readerへ縮退。deprecation window条件1の代替判定を`V2-12-07`→`V2-12-05`へ移して`V2-12-06`開始条件の循環を解消。削除順序を静的依存実測（`core`のv1 orchestrationが`ai.spi`をimport、`DesignArtifactPublisherV2`が`ReleaseVerifier.deleteTree`を使用）に基づきR6→R4→R5→R3→R2→R1+R8→R7へ修正。v1 goldenはR1削除前にimmutable archive＋v2 equivalence testへ変換。rev.3（第2回レビュー反映）でD0（正本関係: ADRはgovernance境界、Task Indexは実行Scope、矛盾時は厳しい側を適用し停止）とD10（command inventory。`asset`／`doctor`／`job`／`recovery`／`version`／`help`をR6削除対象外と明示）を追加。**Task Index側の矛盾を解消**（§1の無条件v1凍結へADR例外、`V2-12-06` Scope／非ScopeをD2a／D2b／K1へ同期、`V2-12-03`／`04`／`05`へD5／D9／D2b／D10条件、`V2-12-07`の「v2単独構成」を「v2 production-only構成」へ再定義）。D8のdrain対象へ完了済みでUndo可能なplacement・job history・cleanup期限前snapshot・監査保持journal・未migration Release 1を追加（実測: v1 Undoは`APPLIED` state対象、`SnapshotCleanupService`は`retentionDays`保持）。D2bへv1 intent／design package readerを明示追加（実測で`DesignArtifacts`／`DesignArtifactVerifier`／`DesignVerification`が未分類だった）。R7を削除からimmutable legacy contract resourceへの移設へ変更。K1のequivalenceをRelease 1→R2（完全一致）とintent／design→v2（構造対応、生成結果一致は非保証）へ分割。R5前提を`V2-6-11`＋`V2-12-03`へ訂正。AGENTS.md §6へ「v1凍結の解除条件」、roadmap「継続する製品境界」へv1退役方針、task-execution-guide §2へADR参照を同期。承認によりD1の第1条件は成立したが、第2条件（対象行のD2a前提Task完了）が未成立のため、`V2-12-02`〜`V2-12-04`が完了するまでv1凍結は全Taskで有効であり、現時点でv1資産の削除・改名はできない。承認はv1削除の開始許可ではなくgovernance境界の発効を意味する。次は`V2-12-02`）
- V2-11-06 実測寸法のcatalog昇格 — **完了**（`V2-11-04`（500×500）／`V2-11-05`（1000×1000）のFAWE 2.15.2実測evidenceだけを根拠に、published `PlacementDimensionLimitV2`を64×64→**1000×1000**（`MEASURED_MAXIMUM`）へ昇格し、sealed exampleとschema（`maximum` 64→1000）を同期。昇格は寸法のみで、feature集合（`surface-2_5d`の4 entry×5 Paper能力列）、contract version、Release formatは不変。上位寸法evidenceがFAWE単独のため`Release2MeasuredDimensionGateV2.measuredCeilingFor(...)`がruntime別production ceilingを返し（FAWE 2.15.2=1000、WorldEdit 7.3.19=64）、plugin起動時に検出したintegrationをgateへbind。配布configの`measured-candidate-max-*`既定値は保守的に64のまま。consistency verifierがpublished limit＝実測最大／500・1000のadmit／1001以上の拒否／昇格entryのmeasurement evidence link／runtime別ceilingとevidence分割の一致を強制し、false-promotion corpusが1000超のcatalog宣言とmeasurement link欠落の昇格を拒否。3072等のLARGEとWorldEdit単独runtimeでの64超は未実測のまま拒否。次は`V2-12-01`）
- V2-11-05 FAWE 1000×1000 Paper再実測 — **完了**（無効化済み`V2-6-17`の後継。`run-fawe/` paperclip単独JVM＋RCON＋PID telemetry、FAWE 2.15.2のみ／WorldEdit禁止、measurement profile ceiling 1000×1000。Pass1／Pass2とも`APPLIED`→full effect-envelope verify→`UNDONE`（wall ≈ 6342／6436 s、peak RSS ≈ 6.02 GiB、Xmx 8G／Xms 2G）。≤256-chunk strip forceload、journal `SNAPSHOT_COMPLETE`待ち、MEDIUM envelopeの`Map.copyOf` MapN欠陥を`Collections.unmodifiableMap`へ修正、disk admission超過なし、plan→SIGKILL recovery、v1 help／doctor。この計測自体はcatalogを昇格せず、昇格は`V2-11-06`が行った。証拠: [audit](docs/design-v2/audits/v2-11-05-fawe-1000-measurement.md)／[evidence](docs/design-v2/audits/v2-11-05-fawe-1000-measurement-evidence.md)。次は`V2-11-06`）
- V2-11-04 FAWE 500×500 Paper再実測 — **完了**（無効化済み`V2-6-16`の後継。`run-fawe/` paperclip単独JVM＋RCON＋PID telemetry、FAWE 2.15.2のみ／WorldEdit禁止、measurement profile ceiling 500×500。Pass1／Pass2とも`APPLIED`→full effect-envelope verify→`UNDONE`（wall ≈ 621／627 s、peak RSS ≈ 6.08 GiB、Xmx 6G）。stone-seal＋block-column `forceload`、Pass間Paper再起動、plan→SIGKILL recovery、v1 help／doctor。この計測自体はcatalogを昇格せず、昇格は`V2-11-06`が行った。証拠: [audit](docs/design-v2/audits/v2-11-04-fawe-500-measurement.md)／[evidence](docs/design-v2/audits/v2-11-04-fawe-500-measurement-evidence.md)。次は`V2-11-06`（`V2-11-05`完了済み））
- V2-11-03 Docs／Schema／example consistency sync — **完了**（2026-07-20再監査のP2指摘を実装側に合わせて同期。Quickstartの`sandy-coast-001`生成→`rocky-coast-001`配置の誤記を修正し、CONSOLE／RCONではconfirmation tokenをchatへ出さずowner-onlyの`data/confirmations/<key>.command`（10分有効・1回用）へ保存しpathだけを表示する実運用を追記。`roadmap.md#terrain-generation-v2`参照3件を明示anchorで復旧し、`DocsLinkConsistencyTest`が全Markdownの相対link・anchorを検査。Schema 2件へ`title`、139件へ`description`を追加（validation semantics不変）。共通検証から唯一漏れていた`feature-support-catalog-v2.schema.json`をbundleへ加え、drift（5 Paper能力列の`const: UNSUPPORTED`が`V2-11-01`昇格後のsealed exampleと125件矛盾）を`supportLevel` enumへ同期、`FeatureSupportCatalogCodecV2.read`から共通strict検証を通す。昇格evidenceの強制は引き続き`FeatureSupportCatalogConsistencyVerifierV2`が正本。inventory検査としてschemaのdisk＝bundle＝packaged resource一致と`examples/`178 documentの参照必須を固定し、未参照だった14 exampleをstrict読取corpusへ追加。契約version・canonical checksum・昇格範囲は不変。次は`V2-11-04`）
- V2-11-02 R2 placement dimension guard／measurement profile — **完了**（2026-07-20再監査 判定#5の解消。`Release2MeasuredDimensionGateV2.production(...)`が`placement.release2.measured-candidate-max-*`をcatalogの実測上限（当時64、`V2-11-06`以降はruntime別）へクランプし上限超の設定値を起動時に拒否、`Release2PlacementDimensionPolicyV2`が唯一のadmission pointとしてworld mutation・journal書込・reservation取得より前に上限超layoutを拒否。再実測専用`Release2MeasurementProfileV2`は既定無効で明示flag＋隔離world＋CONSOLE／RCON operatorの3条件必須、能力昇格は伴わない。`disk.minimum-free-bytes`／`disk.safety-margin-bytes`を`Release2DiskBudgetV2`としてR2 reservation floorへ初反映（従来は固定1 MiB）。`FilePlacementSafetyStoreV2.rebuild`をplan target箱からsealed effect envelope基準へ修正し、consumed confirmationのanti-replay保持を固定。新規寸法hard-codeなし。次は`V2-11-03`）
- V2-11-01 Paper apply capability promotion／Track D・E connection — **完了**（2026-07-20再監査で検出した「catalog／testは昇格済み、正本は未着手」の矛盾を、既存昇格の正式確定で解消。`paper_apply`／`post_apply_validation`／`snapshot`／`rollback`／`restart_recovery`をsmoke実測済み`surface-2_5d`のSANDY_BEACH／BREAKWATER_HARBOR／HARBOR_BASIN／ROCKY_CAPEの4 entryだけ64×64以内で`SUPPORTED`化し、runtime `paper-1.21.11+worldedit-7.3.19|fawe-2.15.2`と`V2-6-14`／`15` smoke＋[V2-6 Phase gate audit](docs/design-v2/audits/v2-6-phase-gate.md)へbind。`hydrology-plan`／`environment-fields`／`sparse-volume`の21 entryは`EXPERIMENTAL`、Release capability未接続のV2-9／V2-10 foundation entryは`UNSUPPORTED`維持。consistency verifierがsmoke-evidenced prefix／runtime／evidence link／Release capability path無しの昇格を拒否し、false-promotion corpusで偽装evidence昇格・500寸法limitを回帰固定。dimension limitはこの時点で64×64のままとし、寸法昇格は`V2-11-06`が実施した。次は`V2-11-02`）
- V2-6-19 Release candidate audit／V2-6 Phase gate — **完了**（`PlacementPhaseGateV2Test`：catalog Paper 5能力列非昇格・hard limit 64×64・sealed example checksum・capability list／5 valid prefix不変・placement example portfolio決定性・R2全lifecycle（plan→confirm→snapshot-all→containment→apply→settle→full verify→Undo）のrepeat／thread／locale／timezone同一block-state map；full clean test／build 909 tests；監査発見のlocale依存`toLowerCase()` 6箇所を`Locale.ROOT`へchecksum不変修正；能力昇格なし、昇格Taskとして`V2-11-01`を登録；[audit](docs/design-v2/audits/v2-6-phase-gate.md)。V2-6完了 19/21、`V2-6-16`／`17`無効化）
- V2-6-11 Provider／manual／image v2 capability integration（`ai.spi.v2` capability negotiation、OpenAI／Anthropic／import／fixture／manual／soft-draft paths、`TerrainDesignApplicationServiceV2`、Design Package v2、ADR 0029；no-fallback、v1 default不変；次は`V2-6-12`）
- V2-6-12 Release 2 cross-capability hardening（capability dependency matrix、cross-version reader policy、artifact limits catalog、placement eligibility、shared placement input contract、ADR 0030；全valid prefix directory／ZIP＋tamper corpus；Release 1 allowlist未緩和；次は`V2-6-13`）
- V2-6-13 Operational metrics／diagnostics／retention（`core.v2.operations`、closed-label metrics、redacted diagnostics、audit JSONL、actor-bound retention、Paper `/lfc ops`、ADR 0031；自動削除defaultなし；次は`V2-6-15`）
- V2-6-14 WorldEdit 7.3.19 Release 2 smoke — **完了**（`/lfc r2` E2E `APPLIED`→`UNDONE`、JAR／WE／plan／envelope／snapshot／verify checksum；[evidence](docs/design-v2/audits/v2-6-14-worldedit-smoke-evidence.md)；default water property read-back正規化；次は`V2-6-15`）
- V2-6-15 FAWE 2.15.2 standalone Release 2 smoke — **完了**（`runFaweServer`／`run-fawe`、`/lfc r2` E2E `APPLIED`→`UNDONE`、FAWE `2.15.2+e9ed0d1`、WE非併用、canonical stream=`V2-6-14`と同値；[evidence](docs/design-v2/audits/v2-6-15-fawe-smoke-evidence.md)）
- V2-6-16／V2-6-17 500×500／1000×1000 Paper実測 — **無効化（CANCELLED）**（典型開発ホストではcontainment／settle／verifyの壁時計とメモリが許容外。未測定寸法をcatalog `SUPPORTED`へ上げない）
- V2-6-18 Final supported catalog — **完了**（`FeatureSupportCatalogV2`／71 entry／13能力×role、placement hard limit 64×64、consistency verifier、Schema／sealed example、false-promotion corpus；Paper列と500／1000は非SUPPORTED；次は`V2-6-19`）
- V2-6-21 Release 2 Paper application／command lifecycle（strict source bind、plan→confirm→snapshot-all→containment→apply→settle/full verify、reverse rollback、operation-bound Undo／Recovery、restart inspection、`/lfc r2`、bounded worker shutdown、ADR 0034；v1 command／Release format／Schema不変；`V2-6-14`再開後に完了）

- V2-6-09 Release 2 Undo（`PlacementUndoPlanV2`／`PlacementUndoPrepareCompilerV2`／`PlacementUndoServiceV2`／`PlacementUndoApplicationServiceV2`／`PaperPlacementUndoServiceV2`、ADR 0027、UNDO confirm＋world-drift preflight＋逆順restore＋baseline full verify→`UNDONE`、force禁止、snapshot保持；startup Recoveryは`V2-6-10`）
- V2-6-07 bounded settle／full verify（`PlacementSettleVerifyPolicyV2`／`PlacementSettleVerifyServiceV2`／`PlacementExpectedBlockResolverV2`／`PlacementVerifyEvidenceV2`、ADR 0025、`SETTLING → VERIFYING →` terminal `APPLIED`、effect envelope exact stream、continuity metrics、`RECOVERY_REQUIRED` classification；rollback／Undo／Recovery／public command未接続）
- V2-10-09 Deferred terrain Phase gate（`DeferredTerrainPhaseGateV2Test`、21 V2-10 sealed example portfolio決定性、14 FeatureKind／12 dedicated module false-promotion検査、17非kind名不在、protected host checksum回帰、1000角ScaleProfile admission、full suite。offline plan-level generate／validation `SUPPORTED`、preview はpreview index証拠のあるsliceのみ`SUPPORTED`（`MORAINE_FIELD`／`OUTWASH_PLAIN`は`EXPERIMENTAL`）、intent／standalone／export `PARTIAL`、Paper以降`UNSUPPORTED`。[audit](docs/design-v2/audits/v2-10-phase-gate.md)）
- V2-10-11 EXPERIMENTAL `OXBOW_LAKE` foundation（`OxbowLakePlanV2`、`FoundationOxbowLakeSliceCompilerV2`、HARD `ORIGINATES_AT` parent `RIVER`／`MEANDERING_RIVER` + HARD `SUPPORTED_BY` host、FIXED `CLOSED` stagnant basin、cutoff reach bind、wetland handoff metrics、catalog未登録、open-spill `LAKE`／sealed river fixtures不変）
- V2-10-10 EXPERIMENTAL surface `SPRING` foundation（`SpringPlanV2`、`FoundationSpringSliceCompilerV2`、HARD `SUPPORTED_BY` surface host + HARD `EMPTIES_INTO` general `RIVER`、`RiverPlanV2` SOURCE bind、`HydrologyPlanV2.NodeKind.SPRING` id、outflow continuity metrics、catalog未登録、`KARST_SPRING`／sealed river fixtures不変）
- V2-10-08 `BARRIER_ISLAND`／`ATOLL` COMPOSITE_PRESET（`BarrierIslandPlanV2`／`AtollPlanV2`、`FoundationAdvancedIslandReefSliceCompilerV2`、`AdvancedIslandReefCatalogContractV2`、child `SINGLE_ISLAND`／`CORAL_REEF`＋`LAGOON`／`REEF_PASS` reuse、catalog候補5種、`FLOATING_REEF` deferred、FeatureKind／module未導入）
- V2-10-07 EXPERIMENTAL `LAVA_TUBE` foundation（`LavaTubePlanV2`、`FoundationLavaTubeSliceCompilerV2`、HARD `WITHIN`→`VOLCANIC_CONE`、HARD `ORIGINATES_AT` caldera/lava-flow provenance、`SweptSpline`＋`CARVE_SOLID` only、roof/support/entrance plan fields、catalog未登録、dynamic lavaなし）
- V2-10-06 EXPERIMENTAL `ESCARPMENT`／`PLATEAU` foundation（`EscarpmentPlanV2`／`PlateauPlanV2`、`FoundationEscarpmentPlateauSliceCompilerV2`、HARD `OVERLAPS` transition、`DryLandModifierContractV2` for dune/badlands/salt-flat modifiers、`MESA`／`BUTTE`は`PlateauProfile`のみ、catalog未登録、surface `CANYON`不変）
- V2-10-05 advanced river／lake **contract／split only**（`AdvancedRiverLakeSplitContractV2`、first slices `SPRING`→`V2-10-10`／`OXBOW_LAKE`→`V2-10-11`、他5種deferred、FeatureKind／generator未導入、V2-10 Task数 9→11）
- V2-10-04 EXPERIMENTAL `ABYSSAL_PLAIN`／`SEAMOUNT` additional-marine foundation（`AbyssalPlainPlanV2`／`SeamountPlanV2`、`FoundationAdditionalMarineSliceCompilerV2`、HARD `WITHIN`→`OCEAN_BASIN`、basin containment／depth-relief／slope／transition metrics、catalog未登録。`OCEAN_TRENCH`／`MID_OCEAN_RIDGE`／`SUBMARINE_VOLCANO`はcatalog評価のみ）
- V2-10-03 EXPERIMENTAL `SINKHOLE`／`KARST_SPRING` karst hydrology graph foundation（`SinkholePlanV2`／`KarstSpringPlanV2`／`KarstHydrologyGraphPlanV2`、`CenotePlanV2` COMPOSITE_PRESET、host `CAVE_NETWORK` checksum bind、loss/spring static balance、catalog未登録、`KARST_CAVE_SYSTEM`／`CENOTE` FeatureKind化なし）
- V2-10-02 EXPERIMENTAL `MORAINE_FIELD`／`OUTWASH_PLAIN` glacial-deposition foundation（`MoraineFieldPlanV2`／`OutwashPlainPlanV2`、glacial parent geometry bind、integer raster whole／tile export、`PermafrostPlainProfileV2` on `PLAIN`＋cold climate、catalog未登録）
- V2-10-01 EXPERIMENTAL `VALLEY_GLACIER`／`ICE_CAP`／`ICE_SHEET` glacial-ice foundation（共通`GlacialIcePlanV2`、bounded sparse `ADD_SOLID`、cold climate＋snow、`IceFjordPlanV2` COMPOSITE_PRESET、catalog未登録）
- V2-9-14 Terrain foundation Phase gate（`FoundationPhaseGateV2Test`、22 foundation plan example portfolio決定性、1000角ScaleProfile admission、false-promotion検査、full clean suite。offline plan-level generate／validation／preview `SUPPORTED`、intent／standalone／export `PARTIAL`、Paper以降`UNSUPPORTED`。[audit](docs/design-v2/audits/v2-9-phase-gate.md)）
- V2-9-13 river graph roles／`WATERFALL_CHAIN` preset（`RiverPlanV2` role extension、`WaterfallChainPlanV2`、FeatureKindなし、legacy hydrology fixtures不変）
- V2-9-12 macro land-water topology contract（`MacroLandWaterTopologyPlanV2`、manual mask／zone compile、FeatureKindなし、whole／tile／thread freeze）
- V2-9-11 EXPERIMENTAL `UNDERGROUND_RIVER`／`FLOODED_CAVE` flooded-volume connection（`UndergroundRiverPlanV2`、carve→単一`ADD_FLUID`、frozen cave＋lake checksum bind、catalog未登録）
- 3トラックroadmap（A: コア地形 / B: 画像忠実性 V2-7 / C: スケール V2-8）と実行モデル割当の正本 `docs/design-v2/model-assignment.md`（ADR 0015）
- scale契約 `model.v2.scale`（`ScaleClassV2`／`ScaleProfileV2`／`TilePlanV2`）と `core.v2.scale.ScaleAdmissionV2`（`scale-admission-v1`、ADR 0016）— `EXPERIMENTAL`、生成経路へ未接続
- 決定論的land-water抽出 `format.v2.constraint.extract`（`image-land-water-extract-v1`、`ExtractedMaskDraftV2`、`SecureImageExtractionEnvelopeV2`、`ExtractedMaskDraftArtifactPublisherV2`、draft preview、ADR 0017）— `EXPERIMENTAL`、CLI／Paper／Requestへ未接続
- Minecraft palette adapter `model.v2.minecraft`／`format.v2.minecraft`（`MinecraftPalettePlanV2`、`minecraft-palette-resolver-v1`、ADR 0018）— V2-4 offline `SUPPORTED`、Paper apply未接続、v1 palette不変
- Mangrove regional shaping `generator.v2.landform.mangrove`（`MangroveWetlandPlanV2`、Stage 6 `MANGROVE_TIDAL_LINK`、`kind-feature-constraint-v2`）— V2-4 offline `SUPPORTED`、Paper未接続
- Coral reef bathymetry `generator.v2.landform.reef`（`CoralReefPlanV2`、Stage 6 `REEF_LAGOON_PASS`、`kind-feature-constraint-v3`）— V2-4 offline `SUPPORTED`、Paper未接続
- Sparse ecology placement `model.v2.ecology`／`generator.v2.ecology`（`EcologyPlanV2`、`ecology-placement-contract-v1`、habitat／assemblage／density-spacing）— V2-4 Release componentとしてoffline `SUPPORTED`、WorldBlueprint／Paper未接続、cave／entity非対応
- Volcanic／canyon feature material overlay `model.v2.material.feature`／`generator.v2.material.feature`（`FeatureMaterialProfilePlanV2`、basalt／tuff／ash・strata／talus／sediment）— V2-4 Release componentとしてoffline `SUPPORTED`、shape generator不変、Paper未接続
- Environment validator／preview `validation.v2.environment`／`preview.v2`（`EnvironmentValidatorV2`、10-layer diagnostic bundle、`environment-validation-artifact-v2`／`environment-preview-index-v2`）— V2-4 offline `SUPPORTED`
- Release 2 `environment-fields` capability（ADR 0019、`ReleaseEnvironmentPublisherV2`、palette／ecology／snow／material exact set）— V2-4 offline `SUPPORTED`、Paper未接続
- V2-4 Phase gate audit（5 scenario、order／thread／locale／timezone determinism、1000角budget、tampering／cancel、v1／Release 1／V2-2／V2-3回帰）
- Volume SDF primitives `model.v2.volume`／`generator.v2.volume.sdf`（`volume-sdf-primitive-contract-v1`、`volume-sdf-fixed-v1`／`volume-sdf-q-v1`、6 analytic primitives）— `EXPERIMENTAL`、CSG／feature／Release未接続
- Ordered volume CSG `model.v2.volume.VolumeCsgPlanV2`／`generator.v2.volume.csg`（`volume-csg-contract-v1`、`volume-csg-ordered-v1`、ADD_SOLID／CARVE_SOLID／ADD_FLUID）— `EXPERIMENTAL`、feature／Release未接続
- Volume AABB spatial index `model.v2.volume.VolumeAabbIndexPlanV2`／`generator.v2.volume.index`（`volume-aabb-index-contract-v1`、`volume-aabb-index-v1`、halo query／ordinal order）— `EXPERIMENTAL`、feature／Release未接続
- Volume 3D tile cache `model.v2.volume.VolumeTileCachePlanV2`／`generator.v2.volume.cache`（`volume-tile-cache-contract-v1`、`volume-tile-cache-v1`、LRU／intervals／admission／dense detector）— `EXPERIMENTAL`、feature／Release未接続
- Volume TerrainQuery composition `generator.v2.volume.query`（`terrain-query-volume-v1`、base＋ordered CSG、intervals／ceiling／surface）— `EXPERIMENTAL`、feature／Paper未接続、v1 adapter不変
- Cave network `model.v2.volume.CaveNetworkPlanV2`／`generator.v2.volume.cave`（`cave-network-contract-v1`、`cave-network-v1`、graph→CARVE_SOLID）— `EXPERIMENTAL`、lake／sea cave／Release未接続
- Lush cave `model.v2.volume.LushCavePlanV2`／`generator.v2.volume.cave.lush`（`lush-cave-contract-v1`、`lush-cave-v1`、WITHIN／REACHABLE_FROM／wet surfaces／ecology hooks）— `EXPERIMENTAL`、full ecology／lake／lighting／Release未接続
- Underground lake `model.v2.volume.UndergroundLakePlanV2`／`generator.v2.volume.water`（`underground-lake-contract-v1`、`underground-lake-v1`、CARVE→ADD_FLUID／rim／air cavity）— `EXPERIMENTAL`、sea／Paper fluid／Release未接続
- Sea cave `model.v2.volume.SeaCavePlanV2`／`generator.v2.volume.seacave`（`sea-cave-contract-v1`、`sea-cave-v1`、cliff opening／static ADD_FLUID）— `EXPERIMENTAL`、dynamic tide／Paper／Release未接続
- Overhang `model.v2.volume.OverhangPlanV2`／`generator.v2.volume.overhang`（`overhang-contract-v1`、`overhang-v1`、SUPPORTS_FROM／ADD_SOLID＋CARVE recess）— `EXPERIMENTAL`、natural arch／Paper gravity／Release未接続
- Natural arch `model.v2.volume.NaturalArchPlanV2`／`generator.v2.volume.arch`（`natural-arch-contract-v1`、`natural-arch-v1`、two-pier＋through carve）— `EXPERIMENTAL`、bridge asset／sky island／Release未接続
- Sky island group `model.v2.volume.SkyIslandGroupPlanV2`／`generator.v2.volume.skyisland`（`sky-island-group-contract-v1`、`sky-island-group-v1`、multipoint＋underside carve）— `EXPERIMENTAL`、ecology／Paper／Release未接続
- Waterfall volume `model.v2.volume.WaterfallVolumePlanV2`／`generator.v2.volume.waterfall`（`waterfall-volume-contract-v1`、`waterfall-volume-v1`、fall checksum binding／column＋behind＋plunge fluid）— `EXPERIMENTAL`、dynamic fluid／Paper／Release未接続
- Post-volume local environment `model.v2.environment.local.VolumeLocalEnvironmentPlanV2`／`generator.v2.environment.local`（`volume-local-environment-contract-v1`、`volume-local-environment-v1`、surface／wetness／sparse moss-root）— `EXPERIMENTAL`、lighting／Paper／Release未接続
- Volume validator／preview `validation.v2.volume`／`preview.v2`（`VolumeValidatorV2`、5-layer diagnostic bundle、`volume-validation-artifact-v2`／`volume-preview-index-v2`）— `EXPERIMENTAL`、Release／`SUPPORTED`未接続
- 再設計調査記録 `docs/audits/redesign-2026-07-current-state.md`、LARGE実行モデル設計 `docs/design-v2/scale-and-streaming.md`
- Offline 3D volume tile read-back `format.v2.tile.VolumeTileBlockResolverV2`（`terrain-query-volume-v1`合成→canonical block state、cave air／fluid／floating solid、strict inspector＋WorldEdit 7.3.19 read-back、127/128 VarInt 3D境界）— V2-5-16
- Release 2 `sparse-volume` capability（`ReleaseSparseVolumePublisherV2`／`ReleaseSparseVolumeVerifierV2`、SDF／CSG／AABB／validation／volume tileのexact set、CSG→SDF／AABB→CSG binding、directory／ZIP parity）— V2-5-17
- V2-5 Phase gate audit（`VolumePhaseGateV2Test`、lifecycle／capability順、13 example strict read安定性、whole／tile／XYZ seam scene合成、dense allocation拒否・1000角admission、full suite・v1／Release 1／V2-2〜V2-4回帰）と `docs/design-v2/audits/v2-5-phase-gate.md` — V2-5-18
- Release 2 placement contract `model.v2.placement`／`core.v2.placement`（`PlacementPlanV2`、`PlacementJournalV2`、`release-2-placement-contract-v1`／`release-2-placement-journal-v1`、ADR 0020）— V2-6-01、envelope／reservation／apply未接続、v1 journal不変
- Release 2 mutation／effect envelope `PlacementEnvelopePlanV2`／`PlacementEnvelopeCompilerV2`（`release-2-placement-envelope-contract-v1`、physics policy、AABB union、ADR 0021）— V2-6-02、reservation／snapshot／apply未接続
- Surface foundation contract `model.v2.foundation`／`core.v2.foundation`／`generator.v2.foundation`（`SurfaceFoundationPlanV2`、`surface-foundation-field-contract-v1`、ScaleAdmission、owner/transition merge）— V2-9-01、`EXPERIMENTAL`、kind SUPPORTEDなし、WorldBlueprint／Paper／LARGE未接続
- PLAIN／HILL_RANGE vertical slices `PlainPlanV2`／`HillRangePlanV2`／`FoundationPlainHillSliceCompilerV2`（microrelief＋groundwater handoff、ridge／saddle budget＋plain transition、validation／preview／sealed export）— V2-9-02、`EXPERIMENTAL`、`BACKSHORE_PLAINS` ID維持、WorldBlueprint／SUPPORTED未接続
- MOUNTAIN_RANGE／VALLEY vertical slices `MountainRangePlanV2`／`ValleyPlanV2`／`FoundationMountainValleySliceCompilerV2`（derived peak／saddle／spur／pass／foothill、V／U cross-section、floor／shoulder／connection role、validation／preview／sealed export）— V2-9-03、`EXPERIMENTAL`、ALPINE／GLACIAL／FJORD seed不変、WorldBlueprint／SUPPORTED未接続
- General RIVER foundation `RiverPlanV2`／`FoundationRiverSliceCompilerV2`（source→mouth reach graph、bank／bed／discharge／floodplain handoff、orphan／self-loop拒否）— V2-9-04、`EXPERIMENTAL`、`MEANDERING_RIVER` serialization／SUPPORTED不変、WorldBlueprint未接続
- FLOODPLAIN／MARSH hydrologic foundation `FloodplainPlanV2`／`MarshPlanV2`／`FoundationFloodplainMarshSliceCompilerV2`（river adjacency、groundwater／hydroperiod／wetness、open-water fluid／solid ownership）— V2-9-05、`EXPERIMENTAL`、`MANGROVE_WETLAND`不変、WorldBlueprint未接続
- ROCKY_COAST／SEA_CLIFF foundation `RockyCoastPlanV2`／`SeaCliffPlanV2`／`FoundationRockyCoastCliffSliceCompilerV2`（rock shelf／cliff face／talus／notch、coast transition、sea-cave／overhang host AABB handoff）— V2-9-06、`EXPERIMENTAL`、catalog未登録、`ROCKY_CAPE`／SeaCave／Overhang checksum不変、WorldBlueprint未接続
- SINGLE_ISLAND／ARCHIPELAGO／VOLCANIC_CONE foundation `SingleIslandPlanV2`／`ArchipelagoPlanV2`／`VolcanicConePlanV2`と各slice compiler（island mass／shore／drainage／apron、group spacing／saddle、cone／crater）— V2-9-07、`EXPERIMENTAL`、catalog未登録、`VolcanicIslandConeAdapterV2` suggested-params only、`VOLCANIC_ARCHIPELAGO` checksum／hook不変、WorldBlueprint未接続
- OCEAN_BASIN／CONTINENTAL_SHELF／CONTINENTAL_SLOPE bathymetry `OceanBasinPlanV2`／`ContinentalShelfPlanV2`／`ContinentalSlopePlanV2`と3 slice＋coast-to-basin transect（depth／ownership／streaming underwater export、dense 3Dなし）— V2-9-08、`EXPERIMENTAL`、catalog未登録、WorldBlueprint未接続
- SUBMARINE_CANYON bathymetric carve `SubmarineCanyonPlanV2`／`FoundationSubmarineCanyonSliceCompilerV2`（shelf／slope／basin host relations、centerline depth carve、whole／tile＋streaming underwater export）— V2-9-09、`EXPERIMENTAL`、surface CANYON未変更、catalog未登録、WorldBlueprint未接続
- CAVE_ENTRANCE surface-volume connector `CaveEntrancePlanV2`／`FoundationCaveEntranceSliceCompilerV2`（`CaveEntranceParameters`、HARD ENTRANCE_OF＋SUPPORTED_BY、frozen CaveNetworkPlan checksum bind、opening／approach carve、seamless query／export）— V2-9-10、`EXPERIMENTAL`、cave-network checksum不変、catalog未登録、WorldBlueprint未接続
- Release 2 region／disk reservation／bound confirmation `PlacementReservationPlanV2`／`PlacementSafetyStateV2`／`FilePlacementSafetyStoreV2`／`PlacementConfirmationBinderV2`（ADR 0022）— V2-6-03、snapshot／apply未接続、v1 reservation不変
- Release 2 snapshot-all `core.v2.placement.snapshot`（`PlacementWorldGatewayV2`／`PlacementSnapshotFileCodecV2`／`PlacementSnapshotAllCompilerV2`、`PlacementSnapshotPlanV2`＝`release-2-placement-snapshot-v1`、2回走査world drift拒否、staging→strict read-back→atomic publish、`SNAPSHOT_COMPLETE`＝apply-ready）— V2-6-04、apply／settle／rollback未接続、v1 snapshot不変
- Release 2 fluid／gravity／neighbor containment preflight `core.v2.placement.safety`／`format.v2.minecraft.PlacementBlockPhysicsCatalogV2`（`PlacementContainmentPolicyV2`／`PlacementContainmentEvidenceV2`、ADR 0023）— V2-6-05、apply／settle未接続、snapshot外検出非依存
- Release 2 canonical apply transaction `core.v2.placement.apply`／`paper.PaperPlacementWorldGatewayV2`／`worldedit.v2.WorldEditBlockMutationAccessV2`（strict prerequisite chain、feature-neutral X→Z→Y stream、明示solid→air carve→fluid／overlay順、bounded executor／queue／slice、late completion reconciliation、atomic `APPLYING`／canonical tile prefix journal、ADR 0024）— V2-6-06、settle／full verify／rollback／Undo／Recovery／public enable未接続、v1 placement不変
- Skill 5件追加（survey-repository / assign-task-model / change-schema-or-ir / add-image-constraint / sync-docs-roadmap）

### Changed

- AGENTS.md、task-execution-guide、task-index、implementation-roadmap、README、architectureを3トラック体制・新境界へ同期
- V2-4対象module／featureと`environment-fields` lifecycleをoffline `SUPPORTED`へ昇格
- V2-5 Phase gate（V2-5-18）でvolume plan compiler群・waterfall volume・local environment・`sparse-volume`と、deferredだった`WATERFALL` kind／moduleをoffline `SUPPORTED`へ昇格し、Track Aの次Taskを`V2-6-01`へ更新
- v1 Schema、generator `3.0.0-phase6`、Release format 1、既存checksum、公開CLI／Paper挙動は一切変更なし

## 0.9.0-beta.1 — Unreleased release candidate (2026-07-14 audit)

### Added

- Paper request、design、generate、job、candidate、export、version、doctor、Recovery、cleanup、asset commands
- 機能単位permissionと権限対応completion
- stable public error code、correlation ID、safe message契約
- actor domain identityとactor-bound／一回用confirmation
- world UUID＋inclusive boundsのatomic placement reservationとoverlap拒否
- snapshot worst-case disk見積もり、FileStore別永続予約、実使用量journal
- retention dry-run／確認付きcleanup／checksum再検査／audit
- Recovery診断分類、rollback、full-match限定accept、audit
- Paper OpenAI／Anthropic設定、環境変数key、model／policy hard range
- CLI common option、doctor、request、job、candidate、asset、read-only recovery
- 制限付きSponge v3 custom asset catalogとsecurity検査
- beta向けQuick Start、User／Admin Guide、仕組み、障害対応、制限、release checklist

### Changed

- versionを `0.9.0-beta.1`へ更新
- WorldEdit／FAWEが無い場合も非placement Paper commandを利用可能に変更
- OpenAI requestへ決定論的client request IDを付加
- placement journal／Schemaへactor、confirmation時刻、disk reservation／usageを追加
- READMEとroadmapをBeta stabilizationへ再構成
- Phase 3 terminal journalを`SYSTEM:LEGACY`として保守的に読めるupgrade互換loaderを追加
- preferred zone fallbackをmanifest／warning／previewへ明示し、柵の4方向connection stateを決定論的にmaterialize
- Paper jobの即時statusと単調な永続化、request別candidate一覧／完全性検証へ強化

### Removed

- 独自Web UI／Phase 7 UI／Web専用workerを将来計画から削除
- broad apply／undo permissionをplan／execute／status／recoveryへ分割

### Security

- 別actor、overlap領域、disk不足、symlink asset、RECOVERY_REQUIRED snapshot削除をworld変更／削除前に拒否
- API key値、Provider response本文、internal exception、absolute pathを利用者向けerrorへ出さない契約を追加

### Compatibility

placement journalとpermission名がbeta前snapshotから変わります。upgrade前にworldとdata rootをbackupしてください。downgradeは保証しません。
