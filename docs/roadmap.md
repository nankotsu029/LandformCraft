# 開発ロードマップ

このファイルを進捗の正本とします。過去のPhase 0〜5監査は [phase-0-5-audit.md](audits/phase-0-5-audit.md)、現在の実装監査は [phase-6-beta-audit.md](audits/phase-6-beta-audit.md)、2026-07-17再設計の調査記録は [redesign-2026-07-current-state.md](audits/redesign-2026-07-current-state.md)、地形別Gapの履歴資料は [terrain-feature-gap-audit-2026-07-18.md](audits/terrain-feature-gap-audit-2026-07-18.md)、現行の全地形inventoryと実装計画は [terrain-catalog-full-audit-2026-07-22.md](audits/terrain-catalog-full-audit-2026-07-22.md)、macro foundation／intent conformanceの実測監査は [macro-foundation-conformance-audit-2026-07-22.md](audits/macro-foundation-conformance-audit-2026-07-22.md) です。v2の詳細設計とPhase別gateは [Implementation Roadmap v2](design-v2/implementation-roadmap.md)、Task Scopeの正本は [Task Index](design-v2/task-index.md)、実行体制とモデル割当は [Model Assignment](design-v2/model-assignment.md)、採用判断は [ADR 0010](adr/0010-adopt-versioned-terrain-v2-roadmap.md) と [ADR 0015](adr/0015-adopt-v2-foundation-three-track-roadmap.md) を参照してください。

## 現在地

**現在: 0.9.0-beta.1 release candidate / Beta stabilization。2026-07-17再設計で3トラック体制へ移行し、2026-07-18にV2-9/V2-10をTrack D／Eとして独立させ5トラック体制へ拡張した。**

| Track | 対象 | 状態 | 次のTask |
|---|---|---|---|
| A コア地形 | V2-4 Environment → V2-5 Sparse volume → V2-6 Placement → V2-11 Capability promotion → V2-12 V2正式経路化 → V2-18 Macro foundation／intent conformance → V2-17 Paper evidence | V2-12まで完了。**2026-07-22 macro foundation監査（[audit](audits/macro-foundation-conformance-audit-2026-07-22.md)）でV2-18を12 Task登録した**。V2-18は全13 Task完了（13/13。`V2-18-11`の実測で確定した非適合により`V2-18-13`を追加登録・同日完了、`V2-18-08`の [ADR 0038](adr/0038-macro-foundation-contract.md) は2026-07-22の人間承認で`Accepted`、**`V2-18-10`のfail-closed昇格は2026-07-23の人間承認で完了**、**Phase gate `V2-18-12`は2026-07-23の人間承認で完了し親Phaseを閉じた**）。`DiagnosticGateContractV2`（`diagnostic-gate-contract-v1`）でBlueprint diagnostic issueを`GATING`／`NON_GATING`へ分類し、production接続済みkind（surface-2.5D coastal 4種）への一律`v2.unsupported-capability` ERRORを廃止、現在無視されるERROR一覧をgolden化した。fail-closed化はせず（全rule `NON_GATING`）、field／tile semantic checksumは不変。`V2-18-02`は`IntentContributionCoverageV2`（`intent-contribution-coverage-v1`、report-only）を追加し、単一export runの`CoastalSurfaceFieldsV2`owner-index走査から**active contributor被覆**と**surface foundation owner被覆**を別metricとして直接集計（現状は数値一致、macro foundation owner追加後に分岐する設計）、未消費HARD要素一覧（HARD `MetricRangeConstraint`／`EdgeClassificationConstraint`、非production-connected kind宛HARD relation、coastal経路が読まないHARD `LAND_WATER_MASK`）、および`contractOnlyKinds`をCLI `export`／`generate`のJSON summaryへ表示する。reportはBlueprint／manifestへ一切書き込まれず（[audit](audits/macro-foundation-conformance-audit-2026-07-22.md)がevidenceとした73.0%の正式集計はここで確定）、field／tile／block semantic checksumと既存Release checksumは不変。`V2-18-03`は`HardPreflightGateV2`をproduction export spine（surface／hydrology／environment／sparse-volumeの4 Application Service、dispatch選択後・生成前）へ配線し、評価器なきHARD `EDGE_CLASSIFICATION`／`METRIC_RANGE`（rule `v2.preflight.hard-constraint-unevaluated`）、consumerなきHARD relation（非production-connected端点、`v2.preflight.hard-relation-unconsumed`）、未解決HARD `LAND_WATER_MASK`（実在・digest・寸法、`v2.preflight.map-reference-unresolved`）をartifact生成前に`HardPreflightRejectedV2`（IOException）でfail-closed拒否する。SOFTは同経路で警告に留め、override flagは持たない。map解決は既存`SecureConstraintMapSourceLoader`（secure resolve＋digest）＋IHDR寸法照合で行い、pixel decode／canonical field登録（V2-18-06）は含まない。既存coastal fixture（azure-coast／harbor-cove-64／coastal-fishing-map）はHARD意図を保持したnot-yet-honorable negative fixtureとして拒否され、gate通過の作業経路は新規clean fixture `coastal-honored-400`／`harbor-cove-64-honored`（実在mask PNG＋実digest、not-yet-enforceable宣言をSOFT化）へ移した。coastal-fishing-mapのダミーdigest（`dddd…`）と欠落mask PNGは実在の400×400 categorical maskへ是正した。field／tile／block semantic checksumと既存Release checksumは不変。`V2-18-04`は`ValidationTargetV2`を消費するtarget-driven validation framework（`validation.v2.target`：`TargetDrivenValidatorV2`／`ValidationTargetEvaluatorV2`／`ValidationFieldSamplerV2`／`TargetEvaluationV2`／`TargetValidationReportV2`）と`EdgeClassificationEvaluatorV2`（rule `v2.edge-classification`、evaluator version 1）を新設し、coastal surface生成経路（hydrology／environment／sparse-volumeも`generateWithFields`再利用で継承）で生成後・publish前にEDGE targetを測定、HARD違反を`IOException`でexport拒否する。edge band測定領域は「外周`max(1, 垂直方向/32)`行／列×平行方向全幅」をversion 1契約として固定（内部海岸線推測なし、land=1/water=0を`intent.land-water-mask`から読む）。EDGEにevaluatorが付いたため`HardPreflightGateV2`はHARD/SOFT EDGE constraintをpreflightで拒否・警告しなくなり（`EVALUATED_CONSTRAINT_RULES`は`TargetDrivenValidatorV2.BUILT_IN_EVALUATED_CONSTRAINT_RULES`を参照）、`IntentContributionCoverageV2`もEDGEをunconsumedから除外した。`CoastalValidatorV2`既存metricとfield／tile／block semantic checksumは不変、EDGE以外のevaluator（`v2.metric-range`等）は後続Task。honored fixtureのEDGEはmacro foundation（`V2-18-09`）未実装のため充足不能でSOFTのまま維持。`V2-18-05`は`CoastalValidatorV2`の`coastal.breakwater.clear-opening` metricを恒真（`actual=expected=plan.actualClearOpeningWidthMillionths()`）から実測へ修復し、生成fieldのcrest region（`BREAKWATER_REGION_FIELD_ID`）を8連結成分に分解して2 arm間の最小crest間距離を計測、期待値を`[planClear, planClear+6block]`の片側band（下側tolerance 1block）とした。この片側化は計測が常にplan clear widthより数block大きくなる（crest cell中心が構造edgeの内側にある）ためで、opening閉塞（成分数≠2→計測0）やnarrow化は下限で失敗し、恒真ではなくなった。他の自己参照metricを点検し、`coastal.land-water.residual-cells`／`coastal.height.residual-max`が`CoastalValidationInputV2(blueprint, fields, fields)`によりdesired＝actualで常時0（監査item 3）であることを確認したが、これはdesired／actual分離を扱う`V2-18-07`のScopeであり本Taskでは修正しない。beach幅／harbor深度／cape露出／transition conflict metricは既にfield実測で自己参照ではない。変更はcoastal-validation.json（diagnostic容器）のbyteとRelease manifest checksumを動かすが、terrain field／tile／block semantic checksumは不変、絶対checksumをpinするtestは無く、v1 goldenは無関係でADR承認範囲外の変更はない。`V2-18-06`は再利用可能constraint-map binding（`core.v2.binding`：`ConstraintMapFieldBindingV2`／canonical field facade`BoundConstraintFieldV2`／暫定coastal adapter`ProvisionalCoastalLandWaterSourceV2`）を新設し、secure resolve→digest→decode→canonical field登録（normalized XZ registration）の恒久共通部品を既存`SecureConstraintMapSourceLoader`＋`NumericPngDecoder`＋`CanonicalConstraintRasterV2`の再利用で構成、coastal生成への`HardLandWaterSourceV2`注入だけを暫定adapterとして分離した（coastal専用loader化はせず、恒久consumerは`V2-18-09`のmacro foundation stage）。寸法不一致・HARD no-data・bit depth・legend未登録・symlink path traversal・digest mismatch・decode memory budgetのnegativeとthread／locale／timezone非依存のdeterminismを固定。実maskの一律注入はbaseline領域（feature非所有cell、実測73.0%）でcoastal contributor不在となり`CoastalTransitionCompositorV2`が拒否するため、production coastal生成へは未配線とし、field／tile／block semantic checksumとRelease checksumは不変に保った（実配線は`V2-18-09`のfoundation owner追加後）。`V2-18-07`はconformance契約（`validation.v2.conformance`：`ConformanceTargetKindV2`／`ConformanceProvenanceV2`／`ConformanceTargetV2`（sealed：DesiredRaster／AggregateMetric／Topology／Geometric）／`ConformanceTargetSetV2`＋versioned `ConformanceTargetClassifierV2`／`ConformanceResidualV2`（sealed：RasterResidual／ScalarMetricResidual／TopologyPassFail／UnconsumedTarget／ToleranceViolation）／`ConformanceMeasurementsV2`／`ConformanceResidualEvaluatorV2`）を新設して異種target集合とresidual種別を定義し、desired rasterはoptional（maskがある場合のみ、部分desiredはNO_DATA cellを比較対象外）とした。desired sourceが未解決（provenance `SELF_DERIVED`）なら raster residual を false 0 ではなく `UnconsumedTarget` として表現し、監査item 3（residual恒等0で適合扱い）を契約側で是正した。あわせて`CoastalSurfaceExportPipelineV2`／`FoundationSurfaceExportAdapterV2`の`withLandWaterBinding`を、生成field checksumへの自己再束縛から入力mask digest（`request.constraintMaps().expectedSha256()`）への束縛へ置換し、constraint field indexのcanonicalArtifactIdは従来どおりdesired field checksumで content-address する（`SurfaceReleaseCapabilityVerifierV2.verifyIntentBindings`はintent binding＝宣言input digest、field index＝field checksumを別々に検証するよう分離）。terrain field／tile／block semantic checksumは不変、intent／blueprint canonicalChecksum／tile metadata `sourceBlueprintChecksum`／Release manifest checksum（容器byte）は変化する。CoastalValidatorのproduction self-reference（`CoastalValidationInputV2(blueprint, fields, fields)`）自体の解消は実mask解決を伴う`V2-18-09`のScope。`V2-18-08`は [ADR 0038](adr/0038-macro-foundation-contract.md)（全surface XZ cellへeffective foundation ownerを要求するmacro foundation契約）を著述し、同日の第一レビュー（Changes Requested）を反映した改訂第2版へ更新した。D1 candidate（0..N）／background（macro base、`UNDECLARED_OVERLAP`対象外）／effective（merge後ちょうど1、宣言的replacement）のowner 3概念と全cell被覆契約、D2 foundation入力契約（normative＝`TerrainIntentV2.mapReferences`のHARD `LAND_WATER_MASK`＋`ConstraintMapFieldBindingV2`解決、elevationは`HEIGHT_GUIDE`またはmedium別宣言値のみ、明示LAND/OCEAN polygon region型はSchema home不在のため契約要件だけ固定し導入は新Task＋amendment、`MacroLandWaterTopologyPlanV2`はlabelから境界を新規生成しない表現・検証限定、EDGE等からの内部境界推測禁止）、D3 単一role enumを不採用とし`CompositionProfileV2`（foundationEligible＋`CompositionStageV2`集合`FOUNDATION`／`SURFACE_MODIFICATION`／`VOLUME_OPERATION`／`FLUID_OPERATION`＋`ParentPolicyV2`、ADR 0036 canonical carrierへのlookup規則、要否＝要）、D4 60 kind対応表を**NORMATIVE 6**（coastal 4＝modifier＋`PLAIN`／`HILL_RANGE`＝foundation-eligible）／**PROVISIONAL 54**（配線Taskのfield監査で確定）の2段階化（foundation-eligible計17、`ABYSSAL_PLAIN`はHARD `WITHIN`前提のためmodifierへ再分類、`BACKSHORE_PLAINS`等aliasはcarrier継承、`WATERFALL`／`SINKHOLE`／`CAVE_ENTRANCE`／`UNDERGROUND_RIVER`は多段stage）、D5 foundation resolver（exactly-one、background拡張）とmodifier compositor（0..N、taxonomy §5 interaction合成、既存`CoastalTransitionCompositorV2`が具現）の**分離**を含む昇格差分、D6 MEDIUM ceiling内の既存budget再利用、D7 検証3層分担（kernel不変条件＝生成時常時fail-closed／`V2-18-10`＝owner coverageのみ／`V2-18-11`＝coastal precondition・形状conformance）、D8 `SurfaceBaselineV2` deprecation方針（受理継続＋`v2.cli.surface-baseline-deprecated` NON_GATING警告、削除は`V2-18-12`後の独立Task＋amendment）、D9 凍結と可変域を確定した。新capability／artifact type／Release format無し、コード変更無し（docs同期のみ、terrain field／tile／block semantic checksumと全既存test不変）。ADR 0038は改訂第2版に対する2026-07-22の人間承認で`Accepted`となり、`V2-18-09`（依存08）の開始gateは満たされた。`V2-18-09`はmacro foundation production stage（`MacroFoundationStageV2`／`MacroFoundationV2`、`core.v2.export`）を新設し、明示foundation入力 — HARD `LAND_WATER_MASK`（V2-18-06 bindingで恒久解決）＋request新設optional `foundationBaseLevels`（medium別provisional base elevation宣言。Schema追加、absent時のrequest checksumは不変） — からbackground candidate（全cellのland-water medium＋provisional elevation）を解決、coastal 4種を`CoastalTransitionCompositorV2`のsurface modifierとしてfoundation上へ再接続した（maskがcompositorのHARD source、feature非所有cellはbackground所有で`SurfaceBaselineV2`充填を置換）。kernel不変条件（`OWNERLESS_CELL`／`UNDECLARED_OVERLAP`／medium有効性）はADR 0038 D7-1どおり生成時常時fail-closed、maskとfeature形状の矛盾はexport拒否。`CompositionProfileV2`／`CompositionStageV2`／`ParentPolicyV2`（`model.v2.composition`）と`CompositionProfileRegistryV2`（`core.v2.catalog`、60 kind、NORMATIVE 6／PROVISIONAL 54、canonical carrier継承、foundation-eligible 17）をD3/D4どおり登録。foundation経路ではdesired sidecar／validation desired samplerが入力mask由来となりdesired＝actual自己参照（監査item 3 production側）を解消、residual sidecarは実測差分。coverage報告のfoundation owner metricは`foundation.owner-index` namespaceから集計されfoundation経路で100%（activeContributorと設計どおり分岐、legacy経路は正直に0）、HARD maskはunconsumed一覧から除外。`SurfaceBaselineV2` CLI引数はfoundation入力完備requestで無視＋NON_GATING警告`v2.cli.surface-baseline-deprecated`（D8-1）、foundation入力なしのlegacy requestは従来baseline経路をbyte不変で維持（D8-2）。honored fixture 2種は`foundationBaseLevels`を宣言し、maskをcomposed出力∪macro構図（北=本土・南=海）へ再生成（request `expectedSha256`更新）。暫定`ProvisionalCoastalLandWaterSourceV2`は恒久consumerへ置換のうえ削除。coastal容器checksum変化はADR 0038 D9承認範囲内で、terrain semantic checksumの意味論・v1 golden・hydrology／environment／sparse-volume shared pipelineの回帰はfull `./gradlew test`／`build`でPASS。`V2-18-10`（2026-07-23**完了**、人間承認済み）はsurface foundation owner gateをfail-closedへ昇格した（ADR 0038 D7-2／D8-2）。`SurfaceFoundationOwnerGateV2`（`surface-foundation-owner-gate-v1`、rule `v2.export.foundation-owner-coverage-incomplete`）が`V2-18-02`のfoundation owner metricを唯一の計測源として読み、被覆<100%を`SurfaceFoundationOwnerRejectedV2`（IOException）で拒否する。配線先はcoastal surface生成stage（surface／hydrology／environment／sparse-volumeの4 Application Serviceが`generateWithFields`再利用で継承）と`FoundationSurfaceExportAdapterV2`で、後者の従来checkは`covered++`が無条件で常に真の恒真判定だったため、実際にowner index>0のcellを数える実測へ是正した。gate位置はconstraint field index verify直後・diagnostic preview／offline tile書き出し前で、公開物は一切生成されない。Scopeはsurface foundation ownerのみ（modifier＝active contributor被覆、volume／material被覆、request domain外は対象外）。coverage分母はrequest domain全cellでno-dataを除外せず、no-dataはbinding（`INVALID_NO_DATA`）／kernel不変条件（`OWNERLESS_CELL`）が生成前・生成時に拒否するためgateへcovered扱いで到達できない。owner候補は0..N許容・effective ownerは各cellちょうど1（ADR 0038 D1）で、override flagは設けない（D8-2どおりbaseline引数でも回避不能）。これによりlegacy baseline経路のsurface exportは終了した。fixture移行として、authoring surfaceへ`v2 request foundation-base-levels <id> <land-y> <water-bed-y>`と`v2 request generation <id> <seed> <tile-size>`を追加した（両方`request.edit`権限、CLI／Paper共通）。前者は明示foundation入力の宣言、後者はmaskが`V2-18-09`以降seedの合成形状に束縛されるためで、これが無いとauthoringからexportへ到達できる経路が消えるため本Taskの移行対象とした。V2-14-01のimage fidelity chain fixtureは、抽出元画像をhonored maskから生成する形へ移行し、extract→promote→declare→exportのevidenceを維持した。terrain field／tile／block semantic checksum、Release manifest checksum、v1 goldenは不変（gateは計測のみで何も書かない）。full `./gradlew test`／`build` PASS。`V2-18-11`はintent-conformance E2E portfolio（test scopeの`integration.v2.conformance`：`IntentConformancePortfolioV2`＋`IntentConformancePortfolioV2Test`）を新設し、監査item 5（地形形状をどの層でもassertしない）を閉じた。portfolioは**公開済みRelease**（`source/terrain-intent.json`／`blueprint/world-blueprint.json`／ACTUAL land-water sidecar）だけを入力とし、in-process generator状態を読まない。測定は3系統である: (1) **EDGE実測** — published blueprintの`v2.edge-classification` targetを production の`EdgeClassificationEvaluatorV2`（version 1 edge band）で再評価し、充足と実測値をpinする。(2) **beach↔backshore land連続性** — beachがeffective owner（compositor owner index）であるFORESHORE／BACKSHORE cellが全てland、単一land component、mainland（最大4連結land component）所属で、宣言`BACKSHORE_PLAINS` polygonも全cell landかつ同一mainlandに載ること。NEARSHORE cellは全てwater。(3) **breakwater両armのland接続** — 宣言arm（`armId`）ごとにfootprintがbreakwater以外のmainland landへ8近傍接触し、宣言landfall endpoint cellがmainlandにあること。あわせて`coastal-honored-400`／`harbor-cove-64-honored`のEDGE_CLASSIFICATIONを、`V2-18-03`が「evaluator／consumer未実装」を理由に暫定SOFT化した状態から監査fixtureの原意図であるHARD（north-is-land ≥0.85、south-is-sea ≥0.90）へ戻した。V2-18-04のevaluatorとV2-18-09のmacro foundationが揃ったことで両fixtureとも実測`1.000000`であり、EDGE非適合はproduction exportが拒否する。negativeはpublished rasterのnorth edge band浸水（同一評価経路で不充足を検出）、宣言hinterland浸水（連続性metricの非空虚性）、合成rasterのisolated blob（mainland判定）、および`coastal-honored-400`東armの実測非適合。決定性はlocale／timezone（tr-TR／Pacific/Chatham）とRelease複製後の再測定で固定した。**portfolioは計測専用でproduction経路へgateを追加せず**（Phase gate昇格は`V2-18-12`）、terrain field／tile／block semantic checksumとv1 goldenは不変、fixtureのEDGE強度変更によりintent／blueprint canonicalChecksumとRelease manifest容器byteは変化する。本Taskの実測で **`coastal-honored-400`の東breakwater armが宣言landfall（0.61, 0.47）でrocky cape西端（0.62）へ届かず、arm全体（2163 cell）が孤立land componentになる**非適合を確定した。fixture geometry是正には`V2-18-09`のland-water mask再生成が必要で1 Taskの規模を超えるため、隠蔽せず新ID`V2-18-13`へ分離し、portfolioには現状（西arm接続・東arm非接続）を明示pinした。full `./gradlew test`／`build` PASS。`V2-18-13`はその是正専用Taskで、`coastal-honored-400`のrocky cape西端をNW `0.62→0.60`／SW `0.72→0.685`へ拡張して東breakwater armのfoundation toeがcape landへ届くようfixture geometryを是正し、land-water maskを`V2-18-09`の合成規則（composed出力∪macro構図）どおり`active ? composed : macro-background`で再生成した（再生成helper `RegenerateHonoredCoastalMaskExampleTest`はcurrent geometryをbyte一致で再現することで規則一致を保証。mask sha `8b3f04df…`→`60095de7…`、1154 cellがwater→land）。孤立していた2163 cellの東armはmainland（72852 cell）へ併合され、残るnon-mainland land componentはrocky capeの宣言済みsea stack 3個のみ。portfolioの期待を両arm接続へ更新し、`theHonored400EastArmIsAnIsolatedIsland…`を`theHonored400EastArmLandsOnTheMainlandAfterV21813`（両arm connected、landfall cell in mainland、component数=4／mainland=72852をpin）へ置換した。generator挙動・compositor契約・Release formatは不変（fixture＋test＋docsのみ変更）、terrain semantic checksumの意味論・v1 golden・shared pipeline（hydrology／environment／sparse-volume）回帰はfull `./gradlew test`／`build` PASS。coastal容器checksumの変化はADR 0038 D9承認範囲内。**`V2-18-12`（Phase gate）は2026-07-23に実装・検証を完了し、同日の人間承認で完了した**: 新gate test `MacroFoundationPhaseGateV2Test`（`integration.v2.conformance`）がconformance portfolioをPhase gateへ昇格した — gate自身が両portfolio caseをexportしてpublish済みRelease成果物を再測定し、宣言arm集合＝shore-connected arm集合（登録済み非適合の残存はgate失敗。欠陥は新IDへ分離）、EDGE HARD充足＋sealed intentのHARD EDGE constraint集合とblueprint target集合の完全一致（blueprint化途中の脱落検出）、beach↔backshore land連続性、全宣言armのlandfall接続、非空虚性（coastal 4 kind＋backshore実在、band／polygon／arm非空）、land-mass会計（mainland外componentは計画sea stack数・footprint上限内）を固定する。この形状conformanceはrelease qualification保証（Phase gate）であり、production fail-closed契約（preflight／EDGE／owner coverage）と混同しない。あわせてfail-closed export spine契約（`diagnostic-gate-contract-v1`＋production接続kind＝coastal 4種、preflight 3 rule、EDGE evaluator登録、`surface-foundation-owner-gate-v1`＝100%必須）、legacy negative fixture `coastal-fishing-map`のartifact生成前拒否、ADR 0038 D4対応表（60 kind／NORMATIVE 6／PROVISIONAL 54／foundation-eligible 17／`ABYSSAL_PLAIN` modifier／carrier継承）、capability・寸法の無昇格（paper_apply SUPPORTED 4件・1000上限・sealed catalog checksum・Release 2 capability list不変）を再検証した。full clean suite PASS、証拠は [V2-18-12 Phase gate audit](design-v2/audits/v2-18-12-phase-gate.md)。(1) `V2-15-47`／`V2-16-19` Acceptanceへのintent-conformance追記、(2) V2-15 stage gate解除（一律保留を解除し、各production配線leafのper-leaf義務 — 対象kindのcomposition profile確定（PROVISIONAL→確定）＋portfolio case追加・全case適合維持 — へ転換）、(3) V2-18親Phase閉鎖、の3点は**2026-07-23の人間承認（不可分のatomic decision）で同時に有効化された**。V2-17は7 Task登録済みで、offline Phase `V2-15-47`／`V2-16-19`完了後に開始する。実機未確保時は`BLOCKED_EXTERNAL`、未測定Featureは昇格しない | —（V2-18完了。Track A次はV2-17、`V2-15-47`／`V2-16-19`完了待ち） |
| B 画像忠実性 | V2-7 Image fidelity（抽出→draft→明示昇格→競合解決）→ V2-14 wiring follow-up | V2-7完了（7/7）。V2-14は`V2-14-01`〜`03`完了（3/3。親Phase完了宣言は人間承認要） | —（Track B次は親Phase承認待ち。Track Eは`V2-15-10`） |
| C スケール | V2-8 Scale-up（LARGE 3000×3000 offline）→ V2-13 MEDIUM 1024 enablement／measurement | V2-8は`V2-8-02`完了（2/8）、S2決定で`V2-8-03`〜`08`保留。**V2-13は6/6 Task完了（`V2-13-05`は`NOT_APPLICABLE_BY_MEASUREMENT`クローズ、`V2-13-06`は2026-07-22実機evidence人間承認済み）**。同一FAWEホストで32/128/256/512/1024を各2 pass較正し1024を選択、APPLY -93.38%・lifecycle -74.23%、tick/MSPT/GC/memory/watchdog gate PASS。選定buildの1000²／1024²各2 passもfull verify→Undo、Recovery不変（[audit](design-v2/audits/v2-13-06-apply-slice-calibration.md)）。production既定のみ1024 mutations/sliceへ変更し、catalog／寸法／LARGEは不変。親Phase完了宣言は別の人間承認要 | —（V2-13全Task完了。親Phase完了と1024 catalog昇格は別承認） |
| D 地形基盤拡張 | V2-9 Terrain foundation（`V2-6-01`／`V2-6-02`＋`V2-8-01` ScaleAdmission、V2-6／V2-8完了は待たない） | 完了（14/14、[Phase gate audit](design-v2/audits/v2-9-phase-gate.md)） | 完了 |
| E 地形拡張（後続） | V2-10 → V2-15 canonical public wiring → V2-16 deferred／new／preset | V2-10完了。V2-15は`V2-15-01`〜`09`を完了（9/47）。ADR 0036に従う46-kind `CANONICAL_V2` Schema／model／migration、registry-driven dispatch spine、`hydrology-plan`／`environment-fields`／`sparse-volume` shared production pipeline、ADR 0037に従うfoundation→`surface-2_5d` export adapterを並設し、historic `LEGACY_V2`と個別Feature昇格／Paper能力は不変。**2026-07-22 macro foundation監査により、V2-15のproduction export／placement昇格系leafは`V2-18-09`完了と対象kindのcomposition role登録を前提とするstage gateに従う（registry／Schema／codec／offline plan／determinism系leafは継続可）。`V2-18-12`はこのstage gateの解除を「一律保留の解除＋各配線leafのper-leaf義務（対象kindのcomposition profile確定（ADR 0038 D4のPROVISIONAL→確定、変更時はADR amendment）＋intent-conformance portfolio case追加・全case適合維持）への転換」と裁定し、**解除は2026-07-23の人間承認で有効化された**。以後、production export／placement昇格系leafはこのper-leaf義務を各leafのAcceptanceとして負って実行できる** | **`V2-15-10`** |

**2026-07-22 macro foundation／intent conformance監査登録:** 400×400 coastal fixture（`coastal-fishing-map`）のwater／land両baseline実測により、surface production経路のfeature非所有cell（73.0%）が一律`SurfaceBaselineV2`へフォールバックして大域構図（北陸・南海）を破壊し、HARD `LAND_WATER_MASK`（参照PNG不在でもexport成功、export経路にI/O呼出サイトなし）、HARD `EDGE_CLASSIFICATION`（北端land率0.000のまま`placementEligible: true`）、desired＝actual自己参照（residual恒等0）、diagnostic ERRORとexport gateの断絶を確定した（[audit](audits/macro-foundation-conformance-audit-2026-07-22.md)）。対応として**V2-18（Macro foundation and intent conformance、登録時12 Task）をTrack Aへ登録**し、V2-15のproduction昇格系leafへstage gate（`V2-18-09`完了＋composition role登録が前提）を適用した。合計Taskは218から230へ増えた。その後2026-07-23に`V2-18-11`のconformance portfolioが`coastal-honored-400`東armのlandfall非適合を確定し、是正Task`V2-18-13`を追加してV2-18は13 Task・合計231 Taskとなった。`V2-18-13`は同日完了し（rocky cape西端拡張によるfixture geometry是正＋mask再生成で東armをmainlandへ接続、portfolio期待を両arm接続へ更新）、V2-18は12/13完了・残りはPhase gate `V2-18-12`のみとなった。`V2-18-12`は2026-07-23に実装・検証を完了し（conformance portfolioのgate化＋全leaf再検証＋full clean suite、[audit](design-v2/audits/v2-18-12-phase-gate.md)）、同日の人間承認（atomic decision: `V2-15-47`／`V2-16-19` Acceptance追記・V2-15 stage gate解除・親Phase閉鎖）で完了、**V2-18は13/13で閉じた**。fail-closed化はreport-only段階を必ず先行させ、owner coverage gateのfail-closed昇格は`V2-18-09`完了後の`V2-18-10`だけが行う。Scope正本は [Task Index §18](design-v2/task-index.md)。

Track間は依存を明示したTaskだけが待ち、それ以外は並行実行できます。同一ファイルの同時編集は禁止し、`format.v2.release`等の共有領域を変更するTaskは直列にします。Track D／EのPaper apply capability `SUPPORTED`化の前提だったTrack A（V2-6）のPhase gate（`V2-6-19`）と`V2-6-06`〜`V2-6-10`は完了済みで、実際の昇格・接続は`V2-11-01`（2026-07-20完了）が担当しました（`V2-6-19`自体は能力を昇格しません）。V2-9／V2-10 foundation featureはRelease capability未接続のため、共通canonical placement streamを共有していてもPaper能力列は`UNSUPPORTED`のままです。

**2026-07-22 全地形カタログ監査登録:** 既存5 Track制を維持して、Track Bへ`V2-14-03`、Track Eへ`V2-15-01`〜`V2-16-19`、Track A follow-upへ`V2-17-01`〜`V2-17-07`を登録した。合計Taskは144から218へ増えた。`V2-14-03`（docs-only）は2026-07-22に完了し、READMEをCLI抽出E2Eとreference role 6種へ同期、2026-07-22監査をcurrent inventoryとした。`V2-15-01`は現行HEAD再照合、`V2-15-02`はenum／Schema／module／catalog／docsのcurrent-state registryとCI projectionを完了した。`V2-15-03`では14 existing Schema valueの処置を[ADR 0036](adr/0036-canonical-feature-identifier-disposition.md)へ固定し、人間承認を得た。`V2-15-04`はexact allowlistの46-kind canonical Schema／model／codec、14 disposition registry、explicit projection dispatcher、seed semanticsを保持するstrict migration／report／atomic bundle、compatibility fixtureを実装した。`V2-15-05`は既存coastal production経路をcurrent-state照合付きcompile-time dispatch registryへ接続し、未接続・partial routeをartifact生成前にfail closedとした。`V2-15-06`は`hydrology-plan` shared artifact、`V2-15-07`は`environment-fields` shared artifact、`V2-15-08`は`sparse-volume` shared artifactのproduction Application Service／pipelineを追加した。historic Schema／fixture、個別Feature昇格、Paper能力は変更していない。現在のnext Taskは`V2-15-09`である。Paper昇格はV2-17の実機証跡と人間承認を要する。

`V2-6-21`はplacement durability／Undo reservation／R2 command securityのP0指摘で再オープンし、2026-07-20に再完了した。production file storeの全12 write point×before／after failpoint、confirm／apply間restart、同placement Undo reservation、第三者overlap拒否、baseline完全復元、Recovery retry、operator／actor／world policy／completion回帰を固定した。この再完了はsupport catalogを昇格せず、昇格は後続`V2-11-01`が行った。

2026-07-20の全体再監査（V2-6-19再検証）の残指摘は新Taskへ登録した。`V2-11-02`（R2 dimension guard／measurement profile、P0 — 64×64 hard limitの設定上書き穴とR2 disk設定未反映の修正。2026-07-20完了）、`V2-11-03`（Quickstart誤記・roadmap anchor・Schema title／description・inventory-driven検証のdocs／Schema同期、P2）、`V2-11-04`／`V2-11-05`（専用高memory実測ホストでの500／1000再実測。無効化済み`V2-6-16`／`17`は復活させない。2026-07-20完了）、`V2-11-06`（実測寸法のcatalog昇格）、および新Phase `V2-12`（V2正式経路化とv1完全移行: governance ADR→production export経路→CLI／Paper v2 routing→migration tool→default切替／v1 deprecation→v1削除・名称正規化→Phase gate、`V2-12-01`〜`V2-12-07`。2026-07-21に`V2-12-05`のカバレッジ監査結果を受け、ADR 0035 rev.4 D11で`V2-12-08`〜`V2-12-10`を追加し10 Taskとした）。`V2-11-01`は監査で検出した「catalog／testは64×64昇格済み、正本は未着手」の矛盾を、既存昇格を正式成果として確定する方向で解消済みである（2026-07-20）。AGENTS.md §6のv1凍結は`V2-12-01`のADR承認まで有効である。

**2026-07-21 scale／image方針決定:** read-only再設計レビューを受け、(1) LARGE方針はOption S2（LARGE／3072契約の凍結保持、3000／3072のsupported表現禁止、`V2-8-03`〜`08`保留、ADR 0016維持、1024完成後にS3退役を独立migration Phaseで再審査）、(2) 1024実装（`V2-8-02`実行と新Phase V2-13: instrumentation→route／Schema 1024→offline実測→FAWE配置実測→条件付き最適化。catalog昇格は実測後の別承認）、(3) V2-7配線follow-up（新Phase V2-14。`SUPPORTED`昇格は別承認）を承認した。A3（複数Design Package比較）とMATERIALIZE_NEW_WORLDは保留である。

独自Web UIは製品範囲から削除済みです。CLIとPaperを利用者向けUIとし、ブラウザ版ChatGPT／Claudeによる手動JSON作成は外部AI利用方法として維持します。

V2-12-06以降、production CLI／Paper／generator／placementはv2だけです。v1はpackaged legacy read／verify／migrate、immutable golden、custom asset catalogなどADR 0035 D2b／D3が明示した境界に限り残ります。

## 完了済みマイルストーン

| マイルストーン | 状態 | 完了内容 |
|---|---|---|
| M0 Contract | 完了 | record、Schema、strict JSON／YAML、package境界 |
| M1 Terrain Preview | 完了 | seed再現地形、tile margin、8 PNG、500／1000 performance test |
| M2 Portable Release | 完了 | Sponge v3 tile、manifest、checksum、ZIP、strict verify |
| M3 Safe Placement | 完了 | snapshot、apply、verify、rollback、Undo、journal |
| M4 Text to Intent | 完了 | manual JSON、OpenAI、Anthropic、Design Package |
| M5 Image Intent | 完了 | 6 role、画像security、座標正規化、evidence |
| M6 Structures | 完了 | 8 built-in asset、決定論的配置、Release統合 |

## Beta hardening

- [x] Paper request／design／generate／candidate／export／job／version／doctor command
- [x] command単位permissionと権限対応Tab root
- [x] placement領域予約と同時overlap拒否
- [x] actor-bound、一回用、期限付きconfirmation
- [x] snapshot／temporary／rollback／marginのdisk見積もりと永続予約
- [x] snapshot retention dry-run／確認付きcleanup／audit
- [x] recovery status／diagnose／rollback／acceptと保守的分類
- [x] Paper Provider config wiring、環境変数key、model明示、hard range
- [x] CLI common option、stable error code、correlation ID、job操作
- [x] 制限付きcustom asset validate／import／list／info／remove
- [x] User／Admin／仕組み／障害対応／制限／release checklist文書
- [x] WorldEdit 7.3.19でrequest→fixture design→generate→export→plan→apply→verify→Undo smoke
- [x] FAWE単独環境で今回buildの完全Paper smoke（`V2-6-15`）
- [x] 500×500／1000×1000実world apply／Undo計測 Task（`V2-6-16`／`V2-6-17`）を無効化 — 未測定寸法は`SUPPORTED`にしない（再実測`V2-11-04`／`V2-11-05`と実測範囲のcatalog昇格`V2-11-06`は2026-07-20完了。1000超はなお未測定）
- [ ] release checklistの全必須項目を実機で閉じる

未チェック項目を成功済みとは扱いません。公開可否は監査のBeta decisionとrelease checklistで決定します。

## 継続する製品境界

- 独自Web UI／browser UI／frontend／HTTP server
- 巨大都市の完全自動生成
- AIによる全block座標列挙、AI生成Javaコード実行
- entity、高度なblock entity、biome書換え
- 無制限サイズ、任意codeを実行する外部地形plugin、全域dense voxel world

これらはv2でも自動的には範囲へ入りません。変更する場合は新しいADRと脅威分析を必要とします。

### v1資産の退役方針（[ADR 0035](adr/0035-v1-retirement-governance.md)、Accepted 2026-07-20）

v1（Schema、generator `3.0.0-phase6`、Release format 1、placement／Undo、command）は恒久的な製品境界ではなく、V2-12で段階的に退役します。退役の範囲・順序・deprecation windowの正本はADR 0035です。

- ADR 0035は2026-07-20に`Accepted`となり、`V2-12-05`／`V2-12-06`は各々独立した人間承認のもと2026-07-21に完了しました。V2-12-06はD8をneutral archive＋strict read-backでPASSし、R1〜R8だけを退役しました。
- v1 production writer／generator／placement／通常commandは退役済みです。今後のTaskはR1〜R8を復活させず、D2b／D3のlegacy compatibility境界を削除・変更しません。
- **v1 artifactのread／verify／migrateは削除しません**（ADR 0035 D2b）。Release format 1 verifierはmigration専用のlegacy readerへ縮退し、既存のv1資産は「新規作成できないが読める・変換できる」状態を維持します。
- 次はv1退役の対象外として**維持**します（ADR 0035 D3）。
  - v1 golden corpus（R1削除前にimmutable archive＋v2 equivalence testへ変換。fixture dataは恒久維持）
  - version中立の共通基盤（cancellation、error code、executors、disk／world policy、checksum、WorldEdit共通adapter）
  - **custom asset catalog**（`structure/`、`CustomAssetService`、`custom-asset-*` Schema、`asset` command）— v2等価物が存在しないため削除を承認していません。移行する場合は先行カバレッジTaskの新規追加が必要です。
  - `doctor`／`job`／`recovery`／`version`／`help` の運用verb
- 名称は、Java識別子の`V2` suffix除去だけが自由です。Schema `$id`、schemaファイル名、capability名（`surface-2_5d`等）、artifact type／version、module／stage ID、named seed、contract ID、generator version、error code、metric labelはcanonical checksumに影響するため、ADRが明示承認した場合だけ変更します。
- commandは`/lfc <verb>`／`lfc <verb>`を既定とし、`v2 <verb>`を恒久的な明示aliasとして維持します。v1／r2経路と旧permission aliasは削除済みです。
- D8の機械可読証跡は[drain evidence](design-v2/audits/v2-12-06-drain-evidence.json)、R1〜R8とcommand移行の証跡は[retirement evidence](design-v2/audits/v2-12-06-v1-retirement-evidence.md)です。
- `asset`／`doctor`／`job`／`recovery`／`version`／`help`は維持されています（ADR 0035 D10）。
- V2-12完了後も「v1コードが一切ない構成」にはなりません。到達目標は、v2が唯一のproduction writer／generator／placement／通常command経路であり、v1がmigration専用read-only境界と明示的維持対象だけに隔離された構成です。
- 既知かつADRで**明示承認済み**のカバレッジ劣化は2件です。WorldEdit 7.3.19単独runtimeのv2 Paper apply published limitは64×64（1000×1000のevidenceはFAWE 2.15.2単独）であり、provider failure codeはv1の9値からv2の4値へsurface粒度が縮小します。後者の元codeはcause chainへ残ります。

## 現行0.9で未対応

- feature別coast、fjord、delta、volcano、canyon、waterfall generatorのCLI／Paper公開（offline v2は下記のとおり）
- geology、climate、ecology fieldと高度な植生
- cave、overhang、arch、sky island等の局所3D地形（offline v2は下記のとおり）
- Release format 2とv2地形の広範なsupported Paper配置（`V2-11-01`で`surface-2_5d`の4 featureだけを`SUPPORTED`にし、`V2-11-06`で寸法を実測範囲の1000×1000へ昇格したが、他prefixは`EXPERIMENTAL`、foundation featureは`UNSUPPORTED`、寸法evidenceはFAWE 2.15.2単独／WorldEdit単独runtimeは64×64のまま）
- 通常画像・スケッチからの決定論的制約抽出のPaper到達とSUPPORTED昇格（`V2-14-01`でCLI `extract`／`promote`と`request constraint-map`宣言連携を配線し利用者がCLIから到達可能になったが、runtimeは`EXPERIMENTAL`のまま。Paper UIは未接続、`SUPPORTED`昇格は別承認。斜視／multi-view reference roleは`V2-14-02`（2026-07-21完了）でAI提案入力専用roleとして追加済み。座標constraint生成は禁止契約）
- 1001以上のPaper配置（`PlacementDimensionLimitV2`とPaper配置上限は実測1000のまま。1024のFAWE配置は`V2-13-04`が実測完了（両pass `APPLIED`→full verify→`UNDONE`、[evidence](design-v2/audits/v2-13-04-fawe-1024-measurement-evidence.md)）したが、catalog／limit昇格は別承認Task待ちのため依然として1000上限）。1024角offlineは request／Schema／`GenerationBounds`水平上限MEDIUM=1024に加え、`V2-13-03`が特定した hydrology GraphWorkBudget／preview／macro の1_000_000 cell予算を同日follow-up修正で`ScaleDimensionPolicyV2.MEDIUM_MAXIMUM_CELLS`（=1_048_576）へ拡張し、offline E2Eは成立済み（[evidence](design-v2/audits/v2-13-03-offline-budget-measurement-evidence.md)）
- 1024超〜3000×3000のLARGE生成（V2-8-01の契約のみ実装済み・生成不可。2026-07-21のS2決定で`V2-8-03`〜`08`は保留。LARGE契約は凍結保持し、1024完成後にS3退役を独立migration Phaseとして再審査）

これらは各Phaseのgateを満たすまで利用可能、Beta対応、supportedと表現しません。

<a id="terrain-generation-v2"></a>

## Terrain Generation v2（5トラック）

Track A内の既存依存順は `V2-0 → V2-1 → V2-2 → V2-3 → V2-4 → V2-5 → V2-6` です。Track D（V2-9）と旧Track E（V2-10）は完了済みで、そのplan-level成果を公開経路へ接続するTrack E続編は`V2-14-03 → V2-15-01 → V2-15-02 → V2-15-47 → V2-16-01 → V2-16-19`を主依存順とします。V2-17は`V2-15-47`と`V2-16-19`の両方を前提にし、実機証跡なしにPaper `SUPPORTED`へ進みません。Track B（V2-7／V2-14）とTrack C（V2-8／V2-13）はTask単位の依存を正本とします。先行gateを満たさずに後続機能や能力をsupportedへしません。Beta hardeningの未完項目もv2着手を理由に閉じません。

| Track | Phase | 状態 | 目的 | 主な完了判定 |
|---|---|---|---|---|
| A | V2-0 Compatibility spine | 完了 | v1を変えずv2 contract、diagnostic compiler、共通queryを通す | v1 checksum／全block stream不変、11 scenario、query whole／tile決定性 |
| A | V2-1 Constraint maps | 完了 | mask／height guide／zone labelをAIを介さずfieldへcompileする | strict decoder、fixed-point、sidecar、8 preview、1000角memory、whole／tile一致 |
| A | V2-2 Coastal 2.5D | 完了 | beach＋breakwater＋rockycapeを縦に完成する | Azure Coast統合監査、Release 2 capability strict verify、WorldEdit read-back |
| A | V2-3 Hydrology | 完了 | global river／lake／delta／tidal／fjord／waterfall graphを作る | 接続・勾配・流量・分流・海接続、順序／thread決定性 |
| A | V2-4 Environment | 完了（15/15） | geology／climate／ecology／semantic materialを統合する | [Phase gate audit](design-v2/audits/v2-4-phase-gate.md) |
| A | V2-5 Sparse volume | 完了（18/18） | cave／lush cave／overhang／arch／sky island／waterfall／local envを局所3D化する | [Phase gate audit](design-v2/audits/v2-5-phase-gate.md) |
| A | V2-6 Placement | 完了（19/21、`V2-6-16`／`17`無効化） | Release 2を安全に配置・復旧可能にする | [Phase gate audit](design-v2/audits/v2-6-phase-gate.md)（effect envelope、snapshot-all、canonical apply、settle/full verify、WE／FAWE smoke、v1回帰） |
| B | V2-7 Image fidelity | 完了（7/7） | 通常画像・スケッチをdraft＋明示昇格で直接制約へ変換する | 抽出決定性、provenance連鎖、多入力競合解決、source差分metric（[Phase gate](design-v2/audits/v2-7-phase-gate.md)） |
| C | V2-8 Scale-up | `V2-8-02`完了（2/8）、`V2-8-03`〜`08`は保留（2026-07-21 S2決定） | LARGE（3000×3000）のoffline streaming生成を成立させる（契約は凍結保持、supported表現禁止） | 寸法policy統一（完了）、LARGE予算実測、resume／部分再生成、分割export（保留） |
| C | V2-13 MEDIUM 1024 enablement／measurement | 進行中（6/6 Task完了。`V2-13-06`は2026-07-22実機evidence人間承認済み。親Phase完了・1024 catalog昇格はいずれも別の人間承認要） | 実測済み1000を基礎に1024のoffline生成と既存world安全配置を成立させる | `V2-13-06`: apply slice 5候補を同一FAWEホストで各2 pass較正し1024を選択。APPLY -93.38%、lifecycle -74.23%、tick/MSPT/memory/GC/watchdog gate PASS。plan budget 655,360 bytes、最大18 transactionのslice合計11,796,480 bytesを監査。選定buildの1000²/1024²各2 pass、full verify/Undo/Recovery、sealed example byte stability PASS。[audit](design-v2/audits/v2-13-06-apply-slice-calibration.md)／[evidence](design-v2/audits/v2-13-06-apply-slice-calibration-evidence.md)。Taskは人間承認により完了。後続Taskなし |
| B | V2-14 Image fidelity wiring follow-up | 完了（3/3 Task。親Phase完了宣言は人間承認要） | V2-7抽出経路のCLI／Request配線、reference role追加、対外説明のcurrent-state整合 | `V2-14-03`完了: READMEを実装済みE2Eとproduction 6 roleへ同期。監査の「8種」誤記を6種へamend。production／Schema／capability変更なし |
| A | V2-11 Capability promotion | 進行中（6/6 Task完了、Phase gate Task未設定。`V2-6-19`監査で追加、2026-07-20再監査で拡張） | V2-6 gate完了後のPaper apply `SUPPORTED`昇格（`V2-11-01`完了）、dimension guard／measurement profile（`V2-11-02`完了）、docs／Schema同期（`V2-11-03`完了）、500再実測（`V2-11-04`完了）、1000再実測（`V2-11-05`完了）、実測寸法昇格（`V2-11-06`完了） | `V2-11-01`〜`V2-11-06` |
| A | V2-12 V2 formalization | 完了（10/10、Phase gate `V2-12-07` 2026-07-21。follow-up defect `V2-12-11`はLOW） | v2を唯一のproduction経路とし、v1をlegacy compatibility境界へ隔離する | 完了（[V2-12 Phase gate audit](design-v2/audits/v2-12-07-phase-gate.md)） |
| D | V2-9 Terrain foundation | 完了（14/14）、[V2-9 Phase gate audit](design-v2/audits/v2-9-phase-gate.md) | surface/coast/island/marine/volume connection/macro/river graphの基盤を縦に追加する | [Task Index](design-v2/task-index.md) `V2-9-01`〜`V2-9-14` |
| E | V2-10 Deferred terrain families | 完了（11/11）、[V2-10 Phase gate audit](design-v2/audits/v2-10-phase-gate.md) | glacial、karst、advanced marine/river、dry land、lava tube、island/reef、SPRING、OXBOWを段階化した | [Task Index](design-v2/task-index.md) `V2-10-01`〜`V2-10-11` |
| E | V2-15 Canonical catalog／existing-generator wiring | 進行中（9/47。`V2-15-01`〜`09`完了、次は`V2-15-10`） | canonical registry、既存plan／generatorの公開縦経路、internal／alias／subtype整理 | `V2-15-47` Phase gate。公開identityはADR 0036、canonical projectionは`V2-15-04`、production dispatch spineは`V2-15-05`、shared capability pipelineは`V2-15-06`〜`08`、foundation→`surface-2_5d` adapterは`V2-15-09`／ADR 0037で固定済み |
| E | V2-16 Deferred／new terrain and composition | 未着手（0/19、`V2-15-47`待ち） | composition engine、5 preset、deferred／new terrainの重複なし実装 | `V2-16-19` Phase gate。Paper能力は昇格しない |
| A | V2-18 Macro foundation／intent conformance | 完了（13/13。Phase gate `V2-18-12`は2026-07-23の[audit](design-v2/audits/v2-18-12-phase-gate.md) PASS＋人間承認で閉鎖） | surface production経路へ全cellのeffective foundation ownerを与え、HARD入力とdesired／actual分離を実装・検証へ接続する | [ADR 0038](adr/0038-macro-foundation-contract.md)、[macro foundation監査](audits/macro-foundation-conformance-audit-2026-07-22.md)、owner被覆100%のfail-closed gate、conformance portfolioのgate化（`V2-18-12`） |
| A | V2-17 Paper placement evidence／promotion | 未着手（0/7、`V2-15-47`／`V2-16-19`待ち） | 実機measurement harness、runtime別証跡、証拠範囲だけのcapability昇格 | `V2-17-07` Phase gate。hostなしは`BLOCKED_EXTERNAL` |

### Task tracking

2026-07-18のGap監査では、監査開始時taxonomyの73地形名を、専用surface module 16、plan-level standalone volume 7、child-plan限定4、enumのみ5、taxonomyのみ41へ再分類した。v2 Paper適用可能kindは0である。`V2-6-18`へ能力別support/role catalogとfalse-promotion検査を統合し、新しいgeneratorはV2-9/10へ分離した。完了済みV2-2〜V2-5は再openしていない。

V2-0〜V2-5のAcceptance gateは完了済みで、完了状態を戻さない。証拠は [V2-2 Phase gate audit](design-v2/audits/v2-2-phase-gate.md)、[V2-3 Phase gate audit](design-v2/audits/v2-3-phase-gate.md)、[V2-4 Phase gate audit](design-v2/audits/v2-4-phase-gate.md)、[V2-5 Phase gate audit](design-v2/audits/v2-5-phase-gate.md) を正本とする。`V2-4-01`〜`V2-4-06`の完了内容（typed geology、lithology catalog、strata、climate prior transition、wetness／salinity／hydroperiod、snow potential／cover）は後続Taskと統合され、V2-4 offline environment pathの`SUPPORTED` infrastructureになった。

`V2-4-07`（2026-07-17完了）では`model.v2.material.MaterialProfilePlanV2`へ、geology／lithology／strata、water-condition wetness、snow coverを直接checksum結合するbindingと、version固定の閉じたcatalog（`landformcraft.builtin-material-profile`、6 semantic class）、last-write-winsを許さない3段fixed-order resolution rule（`BASE_SUBSTRATE_FROM_LITHOLOGY`→`WETNESS_OVERRIDE`→`SNOW_OVERRIDE`、surface/ceiling/floor hook付き）を実装した。`generator.v2.material.MaterialProfileResolverV2`はhost lithology／wetness／snow coverから6 semantic material classをinteger-onlyで解決し、`core.v2.MaterialProfilePlanCompilerV2`がchecksumをfreezeする。wet-rock/sediment/snow profile、rule precedence、aspect hook、whole/tile/thread/locale/timezone決定性、catalog/rule/cache budget、unknown semantic ID／last-write-wins拒否を`MaterialProfilePlanV2Test`／`MaterialProfileResolverV2Test`／`MaterialProfilePlanCompilerV2Test`で検証した。Minecraft palette、volcanic/canyon固有rule、volume-local materialは対象外で、V2-4親Phase gateは未完了である。

`V2-4-08`（2026-07-17完了）では`model.v2.minecraft.MinecraftPalettePlanV2`と`format.v2.minecraft`へ、semantic material profile checksumへ直接bindingする閉じた18 mapping（6 class × SURFACE/CEILING/FLOOR）、Minecraft 1.21.11／DataVersion 4671／`minecraft-palette-resolver-v1` compatibility、`EnvironmentBlockStateCatalogV2`（coastal allowlist超集合）、offline Sponge writer/inspector連携を実装した（ADR 0018）。全semantic ID解決、palette 127/128 VarInt境界、unknown ID／future resolver／checksum拒否、Sponge→WorldEdit 7.3.19 read-backのcanonical block checksum一致、thread/locale/timezone決定性を`MinecraftPalettePlanV2Test`／`MinecraftPalettePlanCompilerV2Test`／`MinecraftPaletteResolverV2Test`で検証した。Paper apply、biome、v1 `MinecraftBlockPalette`変更は対象外で、V2-4親Phase gateは未完了である。

`V2-4-09`（2026-07-17完了）では`MANGROVE_WETLAND`のregional shapingを`generator.v2.landform.mangrove`へ実装した。`MangroveWetlandPlanV2`／`MangrovePlanCompilerV2`／`MangroveGeneratorV2`／`LandformMangroveModuleV2`（`EXPERIMENTAL`、`generate.landform-mangrove`）がwetland polygon、microRelief／waterloggedShare parameters、tidal channel open-water protection、`SEDIMENT_WET`(4) substrate marker、5 descriptor fieldを提供する。Stage 6へ`MANGROVE_TIDAL_LINK`（VERIFY_ONLY）を追加し、reconciliation scan orderを`kind-feature-constraint-v2`へ更新した。Diagnostic Blueprint配線、catalog binding、filled-channel／dry-wetland／hard-mask／determinism／budgetを`MangroveGeneratorV2Test`／`MangroveBlueprintWiringTest`で検証した。tree/root placement、ecology、new tidal solverは対象外で、V2-4親Phase gateは未完了である。

`V2-4-10`（2026-07-17完了）では`CORAL_REEF`のregional bathymetryを`generator.v2.landform.reef`へ実装した。`CoralReefPlanV2`／`CoralReefPlanCompilerV2`／`CoralReefGeneratorV2`／`LandformCoralReefModuleV2`（`EXPERIMENTAL`、`generate.landform-reef`）がreef ring、crest／outer slope profile、lagoon depth、`REEF_PASS` corridor carve、marine connection、5 descriptor fieldを提供する。Stage 6へ`REEF_LAGOON_PASS`（VERIFY_ONLY）を追加し、reconciliation scan orderを`kind-feature-constraint-v3`へ更新した。Diagnostic Blueprint配線、catalog binding、sealed lagoon／broken pass／hard conflict／determinism／budgetを`CoralReefGeneratorV2Test`／`CoralBlueprintWiringTest`で検証した。coral object／ecology placement、volume reef、Paper fluidは対象外で、V2-4親Phase gateは未完了である。

`V2-4-11`（2026-07-17完了）ではsparse ecology placement基盤を`model.v2.ecology`／`generator.v2.ecology`へ実装した。`EcologyPlanV2`／`EcologyPlanCompilerV2`／`EcologyPlacementResolverV2`（`EXPERIMENTAL`、`ecology-placement-contract-v1`）がhabitat U16 field、閉じたassemblage catalog（mangrove canopy／root・coral colony・alpine shrub／meadow）、preset選定のactive descriptors、cell/feature seed、density／spacing lattice selectorを提供する。dry mangrove／deep-cold coral／unsupported root、whole／tile／seam／thread／candidate順不変、descriptor budget、unknown preset拒否を検証した。dense object grid、cave ecology、entity／block entity、Paper placement、WorldBlueprint配線は対象外で、V2-4親Phase gateは未完了である。

`V2-4-12`（2026-07-17完了）ではvolcanic／canyon向けfeature material overlayを`model.v2.material.feature`／`generator.v2.material.feature`へ実装した。`FeatureMaterialProfilePlanV2`／`FeatureMaterialProfilePlanCompilerV2`／`FeatureMaterialProfileResolverV2`（`EXPERIMENTAL`、`feature-material-profile-contract-v1`）が閉じたcatalog（basalt／tuff／ash・strata／talus／floor sediment、codes 7–12）、fixed-order resolution／conflict rules（canyon wins over volcanic）、base material／geology checksum bindingを提供する。shape generatorは未変更でvolcanic field checksum回帰を確認した。lava tube、cave、Minecraft palette拡張、WorldBlueprint配線は対象外で、V2-4親Phase gateは未完了である。

`V2-4-13`（2026-07-17完了）ではenvironment独立validator／previewを実装した。`EnvironmentValidatorV2`は`EnvironmentFieldSamplerV2`の公開cell snapshotだけを測定し、wrong snowline／mangrove salinity／reef depth／root support／material exposureをHARD metric／issueへ変換する。`EnvironmentDiagnosticPreviewRendererV2`はtemperature／moisture／wetness／salinity／hydroperiod／snow／habitat／material／feature-material／constraint-errorの10固定PNGを1枚ずつrenderし、strict `environment-preview-index-v2.schema.json`／checksum／directory entry read-back後にatomic publishする。validation artifact Schema／example、corruption／determinism／budget／cancelを検証した。Release capability、generator redesign、volume-local fieldsは対象外で、V2-4親Phase gateは未完了である。

`V2-4-14`（2026-07-17完了）ではRelease 2 `environment-fields` capabilityを`hydrology-plan`＋`surface-2_5d`依存で有効化した（ADR 0019）。`ReleaseEnvironmentPublisherV2`／`EnvironmentReleaseCapabilityVerifierV2`がgeology〜feature-material plans、snow Schema、palette checksum binding、validation／10-layer previewをdirectory／ZIPへstrict収容し、prior surface／hydrology回帰、missing／extra／version／palette改変、cancel cleanupを検証した。feature lifecycleの`SUPPORTED`昇格とPaper applyは対象外で、V2-4親Phase gateは未完了である。次は`V2-4-15`である。

`V2-4-15`（2026-07-17完了）ではsnowy mountains、mangrove wetland、coral reef、volcanic／canyon material、environment Releaseを統合監査した。`EnvironmentPhaseGateV2Test`が5 scenario、plan checksum、scenario／module／stage順、1／4 thread、locale／timezone、1000×1000 stage peak／sparse descriptor budgetを固定し、全V2-4 positive／corruption／tampering／cancel test、v1／Release 1／V2-2／V2-3回帰、clean test／buildを通過した。geology／climate／water／snow infrastructure、ALPINE／GLACIAL mountain、MANGROVE_WETLAND、CORAL_REEF、VOLCANIC_ARCHIPELAGO、Release 2 `environment-fields`をoffline `SUPPORTED`へ昇格し、V2-4 Phase gateを閉じた。WATERFALLとchild-plan kind、cave-local environment、公開CLI／Paperは対象外のままである。次は`V2-5-01`である。

`V2-5-01`（2026-07-17完了）では`model.v2.volume`と`generator.v2.volume.sdf`へ、VolumePlan SDF primitive契約（`volume-sdf-primitive-contract-v1`）とinteger-only kernel（`volume-sdf-fixed-v1`／`volume-sdf-q-v1`）を実装した。sphere／ellipsoid／capsule／plane／rounded box／swept splineのfixed-point signed distance、conservative AABB、canonical plan checksum、Schema／exampleを追加し、boundary／symmetry／translation、primitive順／locale／timezone／thread決定性、zero radius／future kernel／overflow拒否を`VolumeSdfKernelV2Test`で検証した。ordered CSG、AABB index、voxel cache、feature generator、`sparse-volume` capabilityは対象外で、V2-5親Phase gateは未完了である。次は`V2-5-02`である。

`V2-5-02`（2026-07-17完了）では`model.v2.volume.VolumeCsgPlanV2`と`generator.v2.volume.csg.VolumeCsgEvaluatorV2`へ、ordered CSG契約（`volume-csg-contract-v1`／`volume-csg-ordered-v1`）を実装した。`ADD_SOLID`／`CARVE_SOLID`／`ADD_FLUID`と`INTERSECTION_WITH_PRIMITIVE` mask、SDF primitive plan checksum binding、明示ordinal順、stable operator IDをfreezeし、add→carveとcarve→addの差、CARVEのfluid非所有、ambiguous ordinal／unknown dependency／self-cycle／depth／CPU budget拒否、thread／locale決定性、Schema／example round-tripを`VolumeCsgEvaluatorV2Test`で検証した。AABB index、voxel cache、feature generator、`sparse-volume`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-03`である。

`V2-5-03`（2026-07-17完了）では`model.v2.volume.VolumeAabbIndexPlanV2`と`generator.v2.volume.index`へ、AABB spatial index契約（`volume-aabb-index-contract-v1`／`volume-aabb-index-v1`）を実装した。CSG plan checksum binding、operatorごとのconservative AABB（intersection maskは交差AABB、空ならprimaryへfallback）、stable bulk build、XZ/Y halo query、候補のoperator ordinal昇順、Schema／exampleを追加し、brute-force oracle一致、input順／thread不変、overflow／future kernel／checksum拒否を`VolumeAabbIndexV2Test`で検証した。voxel cache、feature generator、`sparse-volume`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-04`である。

`V2-5-04`（2026-07-17完了）では`model.v2.volume.VolumeTileCachePlanV2`と`generator.v2.volume.cache`へ、3D tile-cache契約（`volume-tile-cache-contract-v1`／`volume-tile-cache-v1`）を実装した。chunk key、XYZ halo、bounded LRU、solid／fluid column intervals、`retained + concurrency×peak + cache` admission、1000×1000×512 dense allocation detector、cancel-safe fill、Schema／exampleを追加し、hit／evict／edge Y、cache有無の同checksum、thread／locale決定性、oversized chunk／support拒否を`VolumeTileCacheV2Test`で検証した。TerrainQuery、feature generator、`sparse-volume`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-05`である。

`V2-5-05`（2026-07-17完了）では`TerrainQuery.queryKernelVersion()`と`generator.v2.volume.query.VolumeTerrainQueryV2`／`VolumeTerrainCompositionKernelV2`へ、volume-aware query契約（`terrain-query-volume-v1`）を実装した。base heightfield＋ordered CSG composition、`solidIntervals`／`fluidIntervals`／`surfaceBelow`／`ceilingAbove`、base-only pass-throughを追加し、add／carve／fluid、XYZ seam／whole／tile／thread／locale決定性、interval budget／owner conflict／oob拒否、`V1TerrainAdapterTest`／`V1CompatibilityGoldenTest`回帰を`VolumeTerrainQueryV2Test`で検証した。feature generator、`sparse-volume`、Paperは対象外で、V2-5親Phase gateは未完了である。次は`V2-5-06`である。

`V2-5-06`（2026-07-17完了）では`model.v2.volume.CaveNetworkPlanV2`と`generator.v2.volume.cave`へ、`CAVE_NETWORK`契約（`cave-network-contract-v1`／`cave-network-v1`）を実装した。stable tunnel／chamber graph、entrance reachability、chamber／tunnel SDF→ordered `CARVE_SOLID` CSG、minimum roof／surface breakthrough metrics、AABB、Schema／exampleを追加し、connectivity／isolated chamber／thin roof／breakthrough拒否、thread／locale決定性を`CaveNetworkGeneratorV2Test`で検証した。lush／underground lake／sea cave／materialは対象外で、V2-5親Phase gateは未完了である。次は`V2-5-07`である。

`V2-5-07`（2026-07-17完了）では`model.v2.volume.LushCavePlanV2`と`generator.v2.volume.cave.lush`へ、`LUSH_CAVE`契約（`lush-cave-contract-v1`／`lush-cave-v1`）を実装した。host `CAVE_NETWORK`へのHARD `WITHIN`／`REACHABLE_FROM`、lush chamber carve（SDF→`CARVE_SOLID`）、wet floor／wall／ceiling classification、`LUSH_SUBTERRANEAN` ecology hooks、Schema／exampleを追加し、orphan／too-dry／unreachable／containment／thin roof／breakthrough拒否、thread／locale決定性を`LushCaveGeneratorV2Test`で検証した。full ecology placement／underground lake／lighting／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-08`である。

`V2-5-08`（2026-07-17完了）では`model.v2.volume.UndergroundLakePlanV2`と`generator.v2.volume.water`へ、`UNDERGROUND_LAKE`契約（`underground-lake-contract-v1`／`underground-lake-v1`）を実装した。host cave chamberへのHARD `WITHIN`／`REACHABLE_FROM`、basin `CARVE_SOLID`→単一`ADD_FLUID`（water-plane mask）、rim／air cavity／contained fluid、offline CSG read-back、Schema／exampleを追加し、orphan／unreachable／leak／double fluid／carve-as-fluid拒否、thread／locale決定性を`UndergroundLakeGeneratorV2Test`で検証した。sea connection／Paper fluid／lush material／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-09`である。

`V2-5-09`（2026-07-17完了）では`model.v2.volume.SeaCavePlanV2`と`generator.v2.volume.seacave`へ、`SEA_CAVE`契約（`sea-cave-contract-v1`／`sea-cave-v1`）を実装した。cliff host `CARVES_FLANK_OF`、marine `EMPTIES_INTO`、capsule carve＋静的`ADD_FLUID`、sea opening／fluid continuity／roof metrics、Schema／exampleを追加し、landlocked／unsupported host／leaking inland／land-water conflict拒否、thread／locale決定性を`SeaCaveGeneratorV2Test`で検証した。dynamic tide／wave／Paper／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-10`である。

`V2-5-10`（2026-07-17完了）では`model.v2.volume.OverhangPlanV2`と`generator.v2.volume.overhang`へ、`OVERHANG`契約（`overhang-contract-v1`／`overhang-v1`）を実装した。host cliff `SUPPORTS_FROM`、seaward `ADD_SOLID` lobe＋underside `CARVE_SOLID` recess、support／roof／projection／clearance／seaward opening metrics、Schema／exampleを追加し、floating slab／thin roof／blocked corridor／short projection拒否、thread／locale決定性を`OverhangGeneratorV2Test`で検証した。natural arch／sea cave／Paper gravity／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-11`である。

`V2-5-11`（2026-07-17完了）では`model.v2.volume.NaturalArchPlanV2`と`generator.v2.volume.arch`へ、`NATURAL_ARCH`契約（`natural-arch-contract-v1`／`natural-arch-v1`）を実装した。two-pier `ADD_SOLID` mass＋through `CARVE_SOLID` opening、pier／crown／span／clearance metrics、Schema／exampleを追加し、one-pier／thin crown／closed opening／short span拒否、thread／locale決定性を`NaturalArchGeneratorV2Test`で検証した。bridge asset／sky island／material／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-12`である。

`V2-5-12`（2026-07-17完了）では`model.v2.volume.SkyIslandGroupPlanV2`と`generator.v2.volume.skyisland`へ、`SKY_ISLAND_GROUP`契約（`sky-island-group-contract-v1`／`sky-island-group-v1`）を実装した。ordered multipoint `ADD_SOLID` lobes＋underside `CARVE_SOLID`、ground clearance／inter-island gap／top-edge-underside classes、Schema／exampleを追加し、merged／touching ground／out-of-Y／dense air fill拒否、thread／locale／component-order決定性を`SkyIslandGroupGeneratorV2Test`で検証した。ecology／material／Paper／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-13`である。

`V2-5-13`（2026-07-17完了）では`model.v2.volume.WaterfallVolumePlanV2`と`generator.v2.volume.waterfall`へ、V2-3 fall geometry checksumへ`BOUND_TO_FALL`で結合するwaterfall volume契約（`waterfall-volume-contract-v1`／`waterfall-volume-v1`）を実装した。behind／pool／column shaft `CARVE_SOLID`、falling columnとplunge-poolの静的`ADD_FLUID`、lip→column→pool continuity／behind clearance metrics、Schema／exampleを追加し、offset column／missing behind／missing pool／graph checksum mismatch拒否とthread／locale決定性を`WaterfallVolumeGeneratorV2Test`で検証した。dynamic fluid／Paper settle／new waterfall graph／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-14`である。

`V2-5-14`（2026-07-17完了）では`model.v2.environment.local.VolumeLocalEnvironmentPlanV2`と`generator.v2.environment.local`へ、post-volume local environment／material／sparse placement契約（`volume-local-environment-contract-v1`／`volume-local-environment-v1`）を実装した。surface class／wetness／drip／shade、lush moss／root／pool、cave／sea-cave wet rock、sky-island top／edge／underside profile、Schema／exampleを追加し、moss on dry ceiling／root without support／wrong underside／unknown profile拒否とthread／locale決定性を`VolumeLocalEnvironmentResolverV2Test`で検証した。new volume shapes／lighting／Paper／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-15`である。

`V2-5-15`（2026-07-17完了）では`validation.v2.volume`と`preview.v2`へ、descriptor-only volume validator（`volume-validator-v1`）とstrict 5-layer diagnostic preview（`volume-diagnostic-palette-v1`）を実装した。isolated cave／thin roof／fluid leak／floating overhang／broken arch／merged sky island／fall discontinuity検出、Schema／example、atomic publish／strict read-back／cancel cleanupを`VolumeValidatorV2Test`／`VolumeDiagnosticPreviewRendererV2Test`で検証した。dense validation grid／schematic／Release／`SUPPORTED`は対象外で、V2-5親Phase gateは未完了である。次は`V2-5-16`である。

`V2-5-16`（2026-07-18完了）ではoffline tile pathをvolume合成へ拡張した。writer／inspector／WorldEdit reader（`OfflineTileSchematicWriterV2`／`SpongeV3TileInspectorV2`／`WorldEditOfflineTileReaderV2`）は既に`minY..maxY`全域をX-fast／Z／Yで走査する汎用3D exporterであり意味・format・checksum・budgetを凍結したまま、新規`format.v2.tile.VolumeTileBlockResolverV2`のみを追加した。これは`terrain-query-volume-v1` kernelのvolume合成`TerrainQuery`（V2-5-05）をcanonical block stateへ橋渡しし、air cavity→`minecraft:air`／fluid(WATER)→`minecraft:water`／independent solid→semantic material（`EnvironmentBlockStateCatalogV2` allowlist経由）で、NONE material solidと非water fluidを拒否しv1 adapterと分離する。cave（carve→air）／floating solid（sky island・overhang topology）／fluid pool／airを含むvolume tileがexport→strict inspector read-back→WorldEdit 7.3.19 read-backでresolver semantic checksumと全XYZ一致すること、whole／tile分割・thread／order・locale／timezone不変、palette 127/128 VarInt境界を跨ぐ3D read-back、truncated／corrupt byte／checksum改変拒否を`OfflineVolumeTileReadBackV2Test`／`WorldEditOfflineVolumeTileReaderV2Test`で検証し、`SchemaContractTest`へ`examples/v2/volume/offline-volume-tile-artifact-v2.json`を追加した。出力Sponge v3は一般仕様features（Version 3／DataVersion 4671／Offset `[0,0,0]`／general VarInt／proprietary tag無し）だけを使うためoffline FAWE readerでも同じfileを読める。v1 golden／V2-2 export回帰は不変である。Paper apply、running-server FAWE smoke、`sparse-volume` Release capabilityは対象外で、V2-5親Phase gateは未完了である。次は`V2-5-17`である。

`V2-5-17`（2026-07-18完了）ではRelease format 2へ`sparse-volume` capabilityを`environment-fields`＋`hydrology-plan`＋`surface-2_5d`依存で追加した。`ReleaseArtifactCatalogV2`へcapability list（natural order `[environment-fields, hydrology-plan, sparse-volume, surface-2_5d]`）、6 artifact type、dispatch、依存規則を登録し、`SparseVolumeReleaseCapabilityVerifierV2`／`ReleaseSparseVolumePublisherV2`／`ReleaseSparseVolumeVerifierV2`／`SparseVolumeReleaseSourceV2`／`ReleaseSparseVolumeArtifactsV2`を実装した。`volume/`配下へSDF primitive plan／ordered CSG plan／AABB index plan／volume validation／3D volume tile（`volume-offline-tile-artifact-v2`／`volume-sponge-schematic-v3`の専用type）を収容し、CSG→SDF／AABB→CSGのchecksum binding、validation `sourcePlanChecksum`＝CSG＋hard-pass、volume tileのstrict Sponge v3 read-backと`sourceBlueprintChecksum` bindingをstrict verifyする。既存の`surface-2_5d`／`hydrology-plan`／`environment-fields` releaseはmissing/extra/version/binding/tile checksum改変／future capability／依存downgrade拒否とdirectory／ZIP parity／cancel cleanupを含めて回帰不変で、`ReleaseSparseVolumePublisherVerifierV2Test`と共有fixture `EnvironmentReleaseFixtureV2`（`ReleaseEnvironmentPublisherVerifierV2Test`と同一）で検証した。exampleは`examples/v2/release-sparse-volume/README.md`である。format coreの意味は不変で、`sparse-volume`とvolume featureの`SUPPORTED`昇格はV2-5親Phase統合監査（`V2-5-18`）まで保留する。Paper placement、feature lifecycle最終昇格は対象外である。次は`V2-5-18`である。

`V2-5-18`（2026-07-18完了）ではV2-5全体を統合監査し、V2-5 Phase gateを閉じた。`VolumePhaseGateV2Test`が9 volume plan compilerのlifecycle、`sparse-volume` capability依存順（`[environment-fields, hydrology-plan, sparse-volume, surface-2_5d]`）、13 volume plan exampleのstrict read安定性（正逆読取順／1・4 thread／locale／timezone）、共有volume scene（cave carve＋floating solid＋fluid）のwhole／exclusive-owner tile dispatch／window限定queryのcanonical block stream一致（3D whole/tile/XYZ seam）、`1000×1000×512` dense allocation拒否と1000角streaming admission境界を固定した。full clean suiteで全V2-5 positive／corruption／tampering／cancel、v1 golden、Release 1、V2-2〜V2-4 capability回帰、clean buildを通過した。sparse-volume infrastructure（SDF／CSG／AABB index／tile cache／volume query）、CAVE_NETWORK／LUSH_CAVE／UNDERGROUND_LAKE／SEA_CAVE／OVERHANG／NATURAL_ARCH／SKY_ISLAND_GROUP／waterfall volume／post-volume local environment、volume validator／5-layer preview、offline 3D tile export、Release 2 `sparse-volume`をoffline `SUPPORTED`へ昇格し、V2-3-15以来deferredだった`WATERFALL` kind／moduleも`SUPPORTED`とした。VOLUME_GUIDE intent kind（CAVE_NETWORK等）のdiagnostic-only境界、child-plan kindの`EXPERIMENTAL`、公開CLI／Paper／placement非接続は維持する。証拠は [V2-5 Phase gate audit](design-v2/audits/v2-5-phase-gate.md) を正本とする。次は`V2-6-01`である。

`V2-6-01`（2026-07-18完了）ではRelease 2 placement plan／journal契約を`model.v2.placement`へv1と分離して固定した（ADR 0020）。`PlacementPlanV2`（`release-2-placement-contract-v1`）はtarget／bounds／`MINIMUM_CORNER` anchor、Release format 2 manifest checksum binding、canonical capability set、`tile-xN-zN` tile order、envelope checksum参照、reservation／confirmation binding slot、operation IDを持ち、`PlacementJournalV2`（`release-2-placement-journal-v1`）はformat 2 journal statesを定義する。`PlacementPlanCompilerV2`は`PLANNED`／`PENDING`だけをsealし、Schema／example／strict round-trip、unknown state／version／capability、target mismatch、path／checksum／future version拒否、locale／timezone／thread決定性、journal entry budget、v1 journal codec不変を`PlacementPlanCompilerV2Test`／`PlacementPlanV2Test`で検証した。envelope算出、reservation、snapshot、apply、Undo／Recoveryは対象外で、V2-6親Phase gateは未完了である。次は`V2-6-02`である。

`V2-6-02`（2026-07-18完了）ではmutation／effect envelopeを`model.v2.placement.PlacementEnvelopePlanV2`と`core.v2.placement.envelope.PlacementEnvelopeCompilerV2`へ実装した（ADR 0021）。per-tile mutation AABB、version固定physics policy（FLUID／GRAVITY／NEIGHBOR support radius）、union effect envelope、overflow-safe world bounds、disk／volume admission、mutation／effect checksum provenance、`PlacementPlanV2` envelope ref bindingを提供する。solid／air／fluid／gravity／neighbor fixture、independent oracleによるunder-approx検出、tile／order／thread／locale／timezone決定性、world border／budget／unknown physics拒否を`PlacementEnvelopeCompilerV2Test`で検証した。reservation、snapshot、apply、settleは対象外で、V2-6親Phase gateは未完了である。次は`V2-6-03`である。

`V2-6-03`（2026-07-18完了）ではregion／disk reservationとbound confirmationを`model.v2.placement.PlacementReservationPlanV2`／`PlacementSafetyStateV2`と`core.v2.placement.reservation`へ実装した（ADR 0022）。`FilePlacementSafetyStoreV2`はatomic multi-region／disk lease、overlap拒否、injectable disk probe、expiry prune、restart rebuild、consumed confirmation ledgerを提供し、`PlacementConfirmationBinderV2`はRelease／envelope／reservation／operation／actor／expiry／nonceへ結合した一回使用token（TTL 10分、平文非永続）を発行・検証する。`PlacementReservationConfirmCompilerV2`は`CONFIRMATION_ISSUED` journalまで進め、失敗時にleaseを全解放する。overlap／race／expiry／replay／disk shortage／actor／checksum mismatchとv1 reservation回帰を`PlacementReservationConfirmCompilerV2Test`で検証した。snapshot、apply、settle、v1 reservation意味変更は対象外で、V2-6親Phase gateは未完了である。次は`V2-6-04`である。

`V2-6-04`（2026-07-18完了）ではsnapshot-allを`core.v2.placement.snapshot`へ実装した。`PlacementWorldGatewayV2`（`release-2-placement-world-gateway-v1`）はcanonical X→Z→Y block-state streamのread契約とapply契約を分離し、snapshot-allはapplyを決して呼ばない。`PlacementSnapshotAllCompilerV2`はCONFIRMATION_ISSUEDのplan／envelope／reservation／lease bindingを検証後、全effect envelopeをcanonical tile-index順に`release-2-placement-snapshot-file-v1`（palette＋VarInt）へstagingし、2回走査のstream hash一致でworld drift（TOCTOU）を拒否、全fileのstrict read-backとsealed `PlacementSnapshotPlanV2` index（`placement-snapshot-plan-v2.schema.json`、plan／envelope／reservation／confirmation checksum binding）のread-back後にのみatomic publishして`SNAPSHOTTING`（全tile PENDING）→`SNAPSHOT_COMPLETE`（apply-ready、全tile SNAPSHOTTED）へ進める。disk leaseを上限とするhard byte cap、事前admission、disk shortage／palette budget拒否、失敗・cancel時のstaging全削除（canonical partialなし）、`cleanupAbandoned`／`loadPublished`のrestart経路、published tamper（byte／truncation／index／extra file）拒否、thread／repeat決定性、apply gateway未呼出を`PlacementSnapshotAllCompilerV2Test`で検証した。block apply、settle、rollback execution、Undo UXは対象外で、V2-6親Phase gateは未完了である。次は`V2-6-05`である。

`V2-6-05`（2026-07-18完了）ではfluid／gravity／neighbor containment preflightを`core.v2.placement.safety`へ実装した（ADR 0023）。`PlacementBlockPhysicsCatalogV2`（閉じたphysics分類＋UNSUPPORTED denylist）、`PlacementContainmentPolicyV2`（envelope physics radii一致必須）、`PlacementContainmentPreflightV2`が`SNAPSHOT_COMPLETE`後にpost-apply predicted worldをcanonical X→Z→Y順で走査し、fluid closure／boundary seal、gravity support、neighbor radius、physics-class宣言を証明する。`PlacementContainmentEvidenceV2`は`CONTAINED`だけをsealしplan／envelope／snapshotへchecksum bindingする。contained／uncontained water／lava／sand／gravel／neighbor、unknown／unsupported／understated／gap／budget拒否、locale／timezone／thread決定性を`PlacementContainmentPreflightV2Test`で検証した。apply、settle、事後rollback代替は対象外で、V2-6親Phase gateは未完了である。次は`V2-6-06`である。

`V2-6-06`（2026-07-18完了）ではRelease 2 canonical apply transactionを`core.v2.placement.apply`へ実装した（ADR 0024）。`StrictPlacementApplyPrerequisiteVerifierV2`がRelease directory、段階別plan checksum chain、reservation ownership、consumed confirmation、published snapshot、containment evidence、source bindingを最初のscheduler submission前にstrict再検証し、`PlacementApplyTransactionServiceV2`がfeature-neutralなfinal block streamをcanonical tile順、X→Z→Y exact coverage、明示`SOLID → AIR_CARVE → FLUID`、overlay ordinal昇順でbounded scheduler sliceへ分割する。`PaperPlacementWorldGatewayV2`／`WorldEditBlockMutationAccessV2`はPaper main-thread実行とWorldEdit resource closeをreceiptで証明し、observer timeout／cancel後も受理済みoperationをreconcileする。journalはatomic `APPLYING` publishとcanonical `APPLIED` tile prefix checkpointだけを行い、submission後の失敗は`RECOVERY_REQUIRED`とする。settle／effect envelope full verify、rollback、Undo／Recovery、public command接続、production `SUPPORTED`は対象外で、V2-6親Phase gateは未完了である。次は`V2-6-07`である。

`V2-6-07`（2026-07-18完了）ではbounded settleとeffect envelope全体のexact verifyを`core.v2.placement.verify`へ実装した（ADR 0025）。`PlacementSettleVerifyPolicyV2`（`release-2-placement-settle-verify-policy-v1`）がsettle tick／quiescence／timeout、verify slice／queue、effect外update拒否、sampled verify拒否をversion固定し、`PlacementSettleVerifyServiceV2`がapply完了journal（`APPLYING`かつ全tile `APPLIED`）だけから`SETTLING → VERIFYING →` terminal `APPLIED`（全tile `VERIFIED`）へ進める。`PlacementExpectedBlockResolverV2`はsnapshot baseline＋canonical solid→air→fluid overlayからexpected streamを構築し、gatewayの`advanceSettleTick`／`readVerifySlice`でscheduler-slicedにexact比較する。成功時だけ`PlacementVerifyEvidenceV2`（`VERIFIED`）をplan／envelope／snapshot／apply journalへchecksum bindingしてsealし、surface foundation／marine underwater／surface-volume entrance／underground fluid／overlay continuity metricsを記録する。delayed update／timeout／mismatch／out-of-envelope／cancel／shutdown／slice・queue budgetは`RECOVERY_REQUIRED`へ分類する。tile checkpoint aloneを成功扱いせず、sample verifyへ弱めない。rollback／Undo／Recovery、public command、production `SUPPORTED`は対象外で、V2-6親Phase gateは未完了である。次は`V2-6-08`である。

`V2-6-08`（2026-07-19完了）ではRelease 2 rollbackを`core.v2.placement.rollback`へ実装した（ADR 0026）。`PlacementRollbackServiceV2`は永続化済み`RECOVERY_REQUIRED` journal（全tile `SNAPSHOTTED`／`APPLIED`）だけを入力に、最初のworld mutation前にplan／envelope／reservation／snapshot binding、durable reservation ownership、`loadPublished`によるpublished snapshotのstrict再検証、全snapshot fileのstrict decodeとsealed index checksum照合、union effect envelopeの被覆完全性を検査し、missing／tampered snapshotとcoverage gapをmutationゼロで拒否する。復元はsnapshot済みeffect envelopeを逆canonical tile-index順のbounded `PlacementRestoreSliceV2`（`release-2-placement-restore-slice-v1`、scheduler受理・main-thread・resource close receipt必須）で書き戻し、tile完了ごとに`ROLLING_BACK` journalへcanonical `RESTORED` suffixをcheckpointする（journal invariant追加）。全tile復元後にbounded rollback settle（envelope外update拒否）とunion effect envelope全体のsnapshot baselineとのexact stream verifyを行い、一致時だけterminal `ROLLED_BACK`（全tile `RESTORED`）をsealしてreservation leaseを解放する。published snapshotは削除しない。apply・settle・verify各failure pointからの復元（solid／air／fluid／overlay content）、restore失敗／receipt不正／late completion／shutdown／cancel、逆順restore orderのthread／repeat不変、bounded slice／slice budget、missing・tampered snapshot拒否、v1 rollback回帰を`PlacementRollbackServiceV2Test`で検証した。user Undoは`V2-6-09`、startup Recoveryは`V2-6-10`、snapshot外変更対応・production `SUPPORTED`は対象外で、V2-6親Phase gateは未完了である。

`V2-6-09`（2026-07-19完了）ではRelease 2 Undoを`core.v2.placement.undo`へ実装した（ADR 0027）。`PlacementUndoPrepareCompilerV2`はterminal `APPLIED`（全tile `VERIFIED`）からUNDO reservation／actor-bound confirmation／sealed `PlacementUndoPlanV2`を用意し、journalは`APPLIED`のまま（confirmation準備メッセージ）でworldをmutationしない。`PlacementUndoServiceV2`はconfirmation verify／consume、actor照合、expected applied streamとのcurrent-world drift preflight（`WORLD_DRIFT`はmutationゼロ・force禁止）、published snapshot strict再検証の後、`UNDOING`（VERIFIED prefix＋RESTORED suffix）で逆順`PlacementRestoreSliceV2` restore→bounded settle→snapshot baseline exact verify→terminal `UNDONE`＋reservation解放を行い、snapshotは`KEEP_SNAPSHOTS_FOR_CLEANUP`で保持する。`PlacementUndoApplicationServiceV2`／`PaperPlacementUndoServiceV2.isRelease2Path()`でv1 Undoと識別し、commandへ任意注入できる。actor／replay／drift／missing snapshot／cancel→`RECOVERY_REQUIRED`、逆順restore、codec／journal loadを`PlacementUndoServiceV2Test`で検証した。startup Recovery、production `SUPPORTED`は対象外で、V2-6親Phase gateは未完了である。

`V2-6-10`（2026-07-19完了）ではRelease 2 startup Recoveryを`core.v2.recovery`へ実装した（ADR 0028）。`PlacementRecoveryServiceV2.diagnose`はjournal／artifact／world証拠だけを読み取り専用で分類し、pre-mutation状態は`NO_WORLD_MUTATION`（lease解放のみ）、terminalは`ALREADY_TERMINAL`、mutated状態はpublished snapshotのstrict再検証・durable lease所有・binding照合・union effect envelope全体のbounded exact world scan後にのみ`SAFE_TO_ROLLBACK`とし、canonical block source（manifest checksum／capability set／overlay ordinal照合済み）から再構築したexpected applied streamと完全一致した場合だけ`SAFE_TO_ACCEPT`を加える。証拠の欠損・改変・不整合は`MANUAL_INTERVENTION_REQUIRED`（自動actionなし）へ落とす。prepareはactor-bound一回用confirmation（`RECOVERY_ROLLBACK`／`RECOVERY_ACCEPT`、TTL 10分）付きsealed `PlacementRecoveryPlanV2`（`release-2-placement-recovery-plan-v1`、Schema／example／codec追加）を発行し、rollback executeは中断journal（`APPLYING`〜`UNDOING`、復元進捗付き`RECOVERY_REQUIRED`）をVERIFIED→APPLIED／RESTORED→SNAPSHOTTEDの決定論的late reconciliationで`RECOVERY_REQUIRED`へresealしてV2-6-08 rollbackへ委譲、accept executeは新たなfull exact scanが再一致した時だけterminal `APPLIED`（全tile `VERIFIED`）をsealしてleaseを解放する。cleanup retentionはterminal `ROLLED_BACK`／`UNDONE`限定のdry-run bound削除（exact file set／journal checksum再検証、file／byte budget）とした。`PlacementRecoveryApplicationServiceV2`／`PaperPlacementRecoveryServiceV2.isRelease2Path()`でv1 recoveryと識別しcommandへ任意注入できる。各persisted state分類、missing／tampered snapshot、lease欠損、source binding不一致、scan budget、drift拒否、replay／actor／expiry、journal永続化失敗（disk full相当）、中断rollback再整合、cleanup dry-run／stale／budget、repeat不変を`PlacementRecoveryServiceV2Test`で検証した。自動accept、v1 recovery意味変更、external backup、production `SUPPORTED`は対象外で、V2-6親Phase gateは未完了である。

`V2-6-11`（2026-07-19完了）ではOpenAI／Anthropic／import／fixture／manual constraint／reference-image soft draftを`ai.spi.v2`＋`TerrainDesignApplicationServiceV2`へ接続した（ADR 0029）。`ProviderCapabilityCatalogV2`／`DesignCapabilityNegotiatorV2`がintent contract version 2とcapabilityを明示照合し、未知version・未宣言model・capability mismatchをhard rejectしてv1へfallbackしない。HTTP adapterはTerrainIntent v2 Schema subsetを送りローカル再検証し、Design Package v2（`terrain-intent-v2.json`／`audit-v2.json`／任意`image-draft-evidence-v2.json`／exact checksums）をatomic publishする。soft draftは`UNCONFIRMED`／`CONFIRMED_SOFT`だけを許しHARD昇格を拒否する。同一Intentのprovider間canonical checksum一致、path／secret拒否、v1 default不変をcontract testで検証した。CLI／Paper既定`design`はv1のまま。次は`V2-6-12`である。

`V2-6-12`（2026-07-19完了）ではRelease 2 cross-capability hardeningを閉じた（ADR 0030）。`ReleaseCapabilityDependencyMatrixV2`が5 valid prefixの正本となり、`ReleaseArtifactCatalogV2`／`PlacementPlanV2`が同じ集合を参照する。`ReleaseCrossVersionReaderPolicyV2`（format 2／manifest 1のみ）、`ReleaseArtifactLimitsCatalogV2`、`ReleasePlacementEligibilityVerifierV2`（directory／ZIP placement gate）、`ReleasePlacementInputContractV2`（foundation／bathymetry→共有 canonical overlay stream）を実装した。全valid prefixのdirectory／ZIP eligibility、missing／extra／unknown／version／checksum／path／ZIP bomb拒否、locale／timezone不変、Release 1 allowlist非共有を`ReleaseCrossCapabilityHardeningV2Test`で検証した。world mutation・新artifact type・format 3は含まない。次は`V2-6-13`である。

`V2-6-13`（2026-07-19完了）では運用metrics／diagnostics／retentionを`core.v2.operations`へ実装した（ADR 0031）。closed-label `OperationalMetricsSnapshotV2`、redacted `OperationalDiagnosticsReportV2`、append-only `operations-audit-v2.jsonl`、actor-bound `Release2RetentionServiceV2`（recovery cleanup dry-run／confirm／audit）、Paper `/lfc ops metrics|diagnose`と`PaperOperationalOperationsServiceV2`を追加した。自動削除default・external telemetry・Web UIは含まない。次は`V2-6-14`である。

`V2-6-14`（2026-07-19完了）はWorldEdit 7.3.19単独profileのRelease 2 smokeである。`scripts/smoke/v2-6-14-run.sh`で`/lfc r2 plan→confirm→execute→undo`を実行し、`APPLIED`後`UNDONE`とJAR／WE／plan／envelope／snapshot／verify checksumを[evidence](design-v2/audits/v2-6-14-worldedit-smoke-evidence.md)へ記録した。Bukkit live read-backのdefault property差（`water[level=0]`）は`WorldEditBlockMutationAccessV2.toReleaseCanonical`でcatalog identifierへ正規化した。FAWE・500／1000・support昇格は含まない。完了数は16/21、次は`V2-6-15`である。

`V2-6-20`（2026-07-19完了）はstrict verify済みRelease format 2 directory／ZIPからbounded／restartable canonical block sourceを構築した（ADR 0033）。`ReleaseCoreVerifierV2.openVerified`／`VerifiedReleaseViewV2`がZIP stagingをsource closeまで所有し、`VerifiedReleaseCanonicalBlockSourceV2`がsurface系final tileまたはsparse-volumeのvolume-composed final tileを選択する。cursorごとにtarget schematicのbyte length／artifact checksum／metadata semantic checksumを再検査し、1 tileだけをbounded decodeしてrelease-local X最速→Z→Yをworld targetへ変換する。directory／ZIP parity、cursor reopen、surface ordinal 0〜2／volume ordinal 3〜5、post-verify drift、extra、cancel、ZIP cleanup、既存Release tamper corpusを検証した。Release format／Schema／v1は不変である。次は`V2-6-21`であり、完了後にのみ`V2-6-14`を再開する。

`V2-6-21`（2026-07-19完了）はRelease 2のproduction application lifecycleを組み立てた（ADR 0034）。strict Release sourceからplan→effect envelope→reservation-bound confirm→snapshot-all→containment→canonical apply→bounded settle/full verifyを実行し、失敗時のreverse rollback、operation-bound Undo、conservative Recovery、restart inspectionを同一operation stateへbindする。Paperでは専用`/lfc r2` command、WorldEdit public API gateway、bounded worker ownership／shutdownを追加し、v1 commandは変更していない。fake gateway integrationで安全順序、confirm binding、snapshot前mutationゼロ、containment reject時mutationゼロ、verify failure rollback、restart／shutdownを検証した。新Release artifact／Schema／example formatは追加せず、既存placement契約を運用状態へ再利用する。support catalogは昇格せず、次は`V2-6-14`の実機smoke再開である。

`V2-6-15`（2026-07-19完了）はFAWE 2.15.2単独profileのRelease 2 smokeである。`build.gradle.kts`の`runFaweServer`（`runDirectory=run-fawe`、Paper build 132）と`scripts/smoke/v2-6-15-run.sh`で`/lfc r2 plan→confirm→execute→undo`を実行し、`APPLIED`後`UNDONE`とJAR／FAWE `2.15.2+e9ed0d1`／plan／envelope／snapshot／verify checksumを[evidence](design-v2/audits/v2-6-15-fawe-smoke-evidence.md)へ記録した。WorldEdit plugin併用なし。canonical expected streamは`V2-6-14`と同値。500／1000・support昇格は含まない。完了数は17/21である。

`V2-6-16`／`V2-6-17`（2026-07-19無効化）は500×500／1000×1000 Paper実測Taskである。IDは残すが実行しない。典型開発ホストではcontainment／settle／full verifyの壁時計とメモリが許容外となり、未測定寸法をcatalog候補へ上げない方針へ切り替えた。`V2-6-18`でsmoke規模の実測上限（64×64）と能力catalogを固定し、500／1000 Paper applyを`SUPPORTED`にしない方針を実装した。

`V2-6-18`（2026-07-19完了）はFinal supported catalogである。`FeatureSupportCatalogV2`／`BuiltInFeatureSupportCatalogV2`（71 entry・全FeatureKind＋synthetic）、13能力×role、placement hard limit **64×64**、presets／deferred diagnostics、taxonomy↔module consistency verifier、Schema／sealed example、false-promotion corpusを固定した。Paper能力列と未測定500／1000は`SUPPORTED`にしない。完了数は18/21、次は`V2-6-19`である。

`V2-6-19`（2026-07-19完了）はRelease candidate auditとV2-6 Phase gateである。`PlacementPhaseGateV2Test`が、catalog全entryのPaper 5能力列（paper_apply／post_apply_validation／snapshot／rollback／restart_recovery）非昇格とhard limit 64×64／未測定500・1000拒否、sealed catalog exampleとbuilt-inのchecksum一致、Release 2 capability list・5 valid prefix不変、12 sealed placement example＋catalog exampleの正逆読取順／1・4 thread／tr-TR locale／Chatham timezone安定性、共有release fixtureのplan→confirm→snapshot-all→containment→apply→settle→full verify→Undo全lifecycleのrepeat／executor数／locale／timezone同一block-state map（snapshot-all前mutationゼロ、terminal `UNDONE`でbaseline復元）を固定し、full clean test／build（909 tests）でv1 golden／Release 1／V2-2〜V2-5／V2-9／V2-10回帰を通過した。監査でlocale依存の`toLowerCase()` 6箇所（4 preview index codec、settle-verify continuity detail、provider secret-header検査）を発見し、checksum不変の`Locale.ROOT`最小修正として同Task内で修正した。能力昇格は行わず、`paper_apply`昇格とTrack D／E接続は新Task `V2-11-01` へ登録した。Beta hardening未完項目は別trackのまま残り、release candidateは未承認である。証拠は [V2-6 Phase gate audit](design-v2/audits/v2-6-phase-gate.md) を正本とする。V2-6は完了（19/21、`V2-6-16`／`17`無効化）である。

`V2-11-01`（2026-07-20完了）はV2-6 Phase gate後のPaper capability昇格である。2026-07-20全体再監査が検出した「catalog／testは64×64昇格済み、正本（roadmap／task-index）は未着手」という矛盾を、既存昇格を正式成果として確定する方向で解消した。`BuiltInFeatureSupportCatalogV2`の`PROMOTION_DECISION_ID`（`v2-11-01-paper-capability-promotion`）と`PAPER_SMOKE_EVIDENCED_CAPABILITIES`（`surface-2_5d`のみ）に基づき、`paper_apply`／`post_apply_validation`／`snapshot`／`rollback`／`restart_recovery`を`SANDY_BEACH`／`BREAKWATER_HARBOR`／`HARBOR_BASIN`／`ROCKY_CAPE`の4 entryだけ`SUPPORTED`とし、runtimeを`paper-1.21.11+worldedit-7.3.19|fawe-2.15.2`、evidenceを`V2-6-14`／`V2-6-15` smokeと [V2-6 Phase gate audit](design-v2/audits/v2-6-phase-gate.md) へbindした。`hydrology-plan`／`environment-fields`／`sparse-volume`の21 entryはprefixごとの実機smokeが無いため5列とも`EXPERIMENTAL`、Release capabilityを持たないV2-9／V2-10 foundation entryは共通canonical placement streamを共有していても`UNSUPPORTED`のままとし、この方針をcatalogの`unsupportedDiagnostics`へ明記した。`FeatureSupportCatalogConsistencyVerifierV2`がsmoke-evidenced prefix／宣言runtime／smoke evidence link無しの`SUPPORTED`とRelease capability無しのPaper主張を失敗させ、false-promotion corpus（`FeatureSupportCatalogV2Test`）が偽装evidence付き`MEANDERING_RIVER`昇格、`PLAIN`昇格、child-plan standalone昇格、500寸法limitを拒否する。`PlacementPhaseGateV2Test`はhard limit 64×64と500／1000拒否、sealed example（`examples/v2/catalog/feature-support-catalog-v2.json`、71 entry）とbuilt-inのchecksum一致を維持する。寸法昇格は行わず、500／1000は`V2-11-04`／`V2-11-05`実測後に`V2-11-06`が扱う。次は`V2-11-02`である。

`V2-11-02`（2026-07-20完了）はR2 placement dimension guardとmeasurement profileである。2026-07-20全体再監査の判定#5（catalog hard limit 64×64が設定で最大10,000まで上書き可能、R2経路へ一部disk設定未反映）を閉じた。`Release2MeasuredDimensionGateV2.production(...)`が`placement.release2.measured-candidate-max-*`をFeature Support Catalogの`SMOKE_MEASURED_MAXIMUM`（64）へクランプし、64超の設定値を起動時に拒否する。新しい`Release2PlacementDimensionPolicyV2`が唯一のadmission pointとなり、上限超のlayoutはworld mutation・journal書込・reservation取得のいずれよりも前に拒否される（`Release2PlacementApplicationServiceV2.prepare`の`VerifiedReleaseCanonicalBlockSource`読取直後）。再実測専用の`Release2MeasurementProfileV2`は既定無効で、明示config flag＋宣言済み隔離world＋CONSOLE／RCON operatorの3条件が揃った時だけ`V2-11-04`／`V2-11-05`用に上限超を許可し、有効時はstartup警告を出す。この許可は能力昇格ではなく、published `PlacementDimensionLimitV2`は64×64のまま（昇格は`V2-11-06`）である。あわせて`disk.minimum-free-bytes`／`disk.safety-margin-bytes`を`Release2DiskBudgetV2`としてR2へ初めて反映し（従来は固定1 MiB floor）、reservation floorを両者の和として`FilePlacementSafetyStoreV2`のreservation／snapshot／Undo admissionへ通した。`FilePlacementSafetyStoreV2.rebuild`はplan target箱ではなくsealed effect envelopeのtile AABBとenvelope disk estimateを基準に再構築するよう修正し（target基準ではfluid／gravity containment halo分が未予約になる）、consumed confirmation setはanti-replayのため保持する回帰を固定した。新規上限hard-codeは追加せず、catalogの`SMOKE_MEASURED_MAXIMUM`／`CATALOG_BUDGET_MAXIMUM`だけを正本とする。次は`V2-11-03`である。

`V2-11-03`（2026-07-20完了）はdocs／Schema／example consistency syncである。2026-07-20全体再監査のP2指摘を、実装を正として文書・Schema側へ同期した。Quickstart §2が生成する`sandy-coast-001`を§3が`rocky-coast-001`として配置していた誤記を修正し、confirmation tokenの実装（`ConsoleConfirmationFileStore`）に合わせてCONSOLE／RCONではtokenをchatへ出さず、確認コマンド全文をowner-onlyの`data/confirmations/<key>.command`（10分有効・1回用、消費後破棄）へ保存してpathだけを表示する運用を追記した。`roadmap.md#terrain-generation-v2`への参照3件（architecture／limitations／terrain-design-guide）は見出し変更で解決しなくなっていたため、roadmapへ明示anchorを置いて復旧し、`DocsLinkConsistencyTest`が全Markdownの相対link・anchorを機械検査するようにした。Schemaは`custom-asset-catalog-entry`／`custom-asset-metadata`へ`title`を、残り139件へ`description`を追加し（validation semanticsは不変）、`SchemaContractTest`がtitle／description presenceを固定する。監査で唯一共通検証から漏れていた`feature-support-catalog-v2.schema.json`は、漏れていた間にdriftして5 Paper能力列が`const: UNSUPPORTED`のままとなり`V2-11-01`昇格後のsealed exampleと125件矛盾していた。schemaを`supportLevel` enumへ同期（昇格evidenceの強制は従来どおり`FeatureSupportCatalogConsistencyVerifierV2`が正本）したうえで`StructuredDataValidator`のbundleへ加え、`FeatureSupportCatalogCodecV2.read`が共通strict検証を通すようにした。inventory-driven検査として、schemaはdisk＝bundle＝packaged resourceの集合一致を、exampleは`examples/`配下178 documentがsource／docsから必ず参照されることを固定し、参照の無かった14 example（azure-coast results 3、custom-asset metadata、mountain-stream request、diagnostic scenarios 3、hydrology skeleton 2、environment validation artifact、operations 3）を`SchemaContractTest`のstrict読取corpusへ加えた。契約version、canonical checksum、昇格範囲（`surface-2_5d`×64×64）は変更していない。次は`V2-11-04`である。

`V2-11-04`（2026-07-20完了）は500×500 Paper再実測である。無効化済み`V2-6-16`の後継として、Gradle `runServer`を使わず`run-fawe/`のpaperclip単独JVM＋RCON＋server PID単独telemetryで、**FAWE 2.15.2のみ**（WorldEdit plugin禁止）のmeasurement profile（ceiling 500×500）上にsolid surface Release `v2-11-04-measure-500`を配置した。Pass1／Pass2ともplan→confirm→snapshot-all→apply→settle→full effect-envelope verify→`APPLIED`→Undo→`UNDONE`を完走（wall ≈ 621／627 s、peak RSS ≈ 6.08 GiB、Xmx 6G）。Undo baseline安定化のためenvelopeをstone-sealし、Paper `forceload`はblock column座標で指定する（chunk index誤解だとfillがunloadになる）。Pass間は稼働中store削除を避けPaper再起動後にplacementを掃除した。plan→SIGKILL→restartのrecovery観察とv1 help／doctor smokeも記録した。この計測自体はcatalogを昇格せず、昇格は`V2-11-06`が行った。証拠は [audit](design-v2/audits/v2-11-04-fawe-500-measurement.md)／[evidence](design-v2/audits/v2-11-04-fawe-500-measurement-evidence.md)。

`V2-11-05`（2026-07-20完了）は1000×1000 Paper再実測である。無効化済み`V2-6-17`の後継として、`V2-11-04`と同一のFAWE 2.15.2／`run-fawe`単独JVM＋RCON＋PID telemetry手法でsolid surface Release `v2-11-05-measure-1000`（64 tiles）をPass1／Pass2とも`APPLIED`→full effect-envelope verify→`UNDONE`まで完走した（wall ≈ 6342／6436 s、peak RSS ≈ 6.02 GiB、Xmx 8G／Xms 2G）。Paperの単一`forceload`上限256 chunksを避けてstrip keep-loadし、confirm完了はjournalの`SNAPSHOT_COMPLETE`を正とした。MEDIUM envelope（〜2M overrides）で`Map.copyOf`→`MapN`がconfirm／Undo準備を長時間ブロックする欠陥を実測で検出し、`PlacementExpectedBlockResolverV2`／Undo／Rollbackを`Collections.unmodifiableMap`へ修正した。disk admission超過なし、plan→SIGKILL recoveryとv1 help／doctorも記録。この計測自体はcatalogを昇格せず、昇格は`V2-11-06`が行った。証拠は [audit](design-v2/audits/v2-11-05-fawe-1000-measurement.md)／[evidence](design-v2/audits/v2-11-05-fawe-1000-measurement-evidence.md)。次は`V2-11-06`である。

`V2-11-06`（2026-07-20完了）は実測寸法のcatalog昇格である。`V2-11-04`（500×500）／`V2-11-05`（1000×1000）のFAWE 2.15.2実測evidenceだけを根拠に、published `PlacementDimensionLimitV2`を64×64から**1000×1000**（`PlacementDimensionLimitV2.MEASURED_MAXIMUM`）へ引き上げ、sealed example（`examples/v2/catalog/feature-support-catalog-v2.json`）とschema（`maximum` 64→1000）を同期した。昇格したのは寸法だけで、feature集合（`surface-2_5d`のSANDY_BEACH／BREAKWATER_HARBOR／HARBOR_BASIN／ROCKY_CAPEの4 entry×5 Paper能力列）とcontract version、Release formatは`V2-11-01`から不変である。上位寸法のevidenceはFAWE単独であるため、`Release2MeasuredDimensionGateV2.measuredCeilingFor(...)`がruntime別のproduction ceilingを返し（FAWE 2.15.2は1000、WorldEdit 7.3.19は従来どおり64）、`Landformcraft`はplugin起動時に検出したintegrationをgateへ渡す。配布configの`measured-candidate-max-*`既定値は保守的に64のままで、引き上げは運用者の明示設定である。`FeatureSupportCatalogConsistencyVerifierV2`は「published limit＝実測最大」「500／1000をadmit」「1001以上を拒否」「smoke寸法超を公開する場合、昇格entryのevidenceRefが`V2-11-04`／`V2-11-05` auditを含む」「runtime別ceilingがevidence分割と一致」を検証し、false-promotion corpus（`FeatureSupportCatalogV2Test`）は1000超のcatalog宣言、measurement link欠落の昇格、既存の偽装evidence昇格を拒否する。`PlacementPhaseGateV2Test`は3072等のLARGE寸法拒否とsealed example checksum一致を維持する。1000超（LARGE／V2-8 streaming）とWorldEdit単独runtimeでの64超は未実測のままであり、`SUPPORTED`にしない。次は`V2-12-01`である。

`V2-12-01`（2026-07-20**完了**）はv1退役のgovernanceを[ADR 0035](adr/0035-v1-retirement-governance.md)へ正本化した。コード変更・Schema変更・catalog変更はゼロである。ADRはD1（凍結解除にはADR `Accepted`とD2前提Taskの両方が必要）、D2（削除可能なv1資産をR1〜R8＝generator `3.0.0-phase6`／Release format 1／v1 placement・Undo・recovery／design・generation orchestration／v1 provider adapter／v1 command／v1専用Schema・example／V2-0互換spineに限定し、各行へv2カバレッジと前提Taskを対応付け）、D3（維持対象K1〜K5。**custom asset catalogはv2等価物が無いため削除対象から明示除外**し、これによりGateの停止条件「v2未カバー機能の削除対象化」を成立させない）、D4（Java識別子の`V2` suffix除去は自由。Schema `$id`／schemaファイル名／capability名／artifact type・version／module・stage ID／named seed／contract ID／generator version／error code／metric labelはcanonical checksum影響のため凍結し、本ADRが明示承認した識別子変更はpermission node `landformcraft.r2.*`→`landformcraft.v2.*`とcommand名の2件のみ）、D5（`/lfc r2`→`/lfc v2 <verb>`→`/lfc <verb>`既定→v1経路・alias削除の4段階と、tagged release・30日・未承認劣化ゼロ・人間承認の4条件からなるdeprecation window）、D6（既知の唯一のカバレッジ劣化＝WorldEdit単独runtimeの64×64を明示承認し、範囲を寸法に限定）、D7（削除順序）、D8（v1 operational stateのdrain gate）、D9（migration toolの品質条件10項目）を決定した。rev.2（同日、第1回レビュー反映）でD2をD2a（writer削除）／D2b（legacy reader維持）／D2c（構造問題）へ分割し、Release format 1 verifierを削除対象から外してmigration専用readerへ縮退した。またdeprecation window条件1の代替判定を`V2-12-07`から`V2-12-05`へ移して`V2-12-06`開始条件の循環を解消し、削除順序を静的依存実測に基づきR6→**R4→R5**→R3→R2→R1+R8→R7へ修正した（`core`のv1 orchestrationが`ai.spi`をimportするため）。v1 goldenはR1削除前にimmutable archive＋v2 equivalence testへ変換する方針を確定した。rev.3（同日、第2回レビュー反映）でD0（正本関係）とD10（command inventory）を追加し、Task Index側の矛盾（§1の無条件v1凍結、`V2-12-06`のverifier／golden削除記載、`V2-12-07`の「v2単独構成」）を同期した。AGENTS.md §6へ「v1凍結の解除条件」節、roadmap「継続する製品境界」へv1退役方針、task-execution-guide §2へADR参照を追加した。ADR 0035は2026-07-20にrev.3で `Accepted` となり、D1の第1条件が成立した。ただし第2条件（対象行のD2a前提Task完了）は未成立であり、`V2-12-02`〜`V2-12-04`が未完のため**v1凍結は現時点でも全Taskで有効**である。承認はv1削除の開始許可ではなく、governance境界の発効を意味する。次は`V2-12-02`である。

`V2-12-02`（2026-07-20**完了**）はv2のproduction Release 2 export経路を成立させた。新しい`core.v2.export`パッケージの`Release2ExportApplicationServiceV2`が、CLI／Paper adapterから呼べる唯一の正式export APIである。入力はsealed `GenerationRequestV2`とdesign stageが公開した`TerrainIntentV2`（`Release2ExportRequestV2.fromDesign(...)`で`TerrainDesignApplicationServiceV2`の出力から直接組める）だけで、経路は request → Blueprint compile → coastal generation／transition composition → constraint field sidecar → field-only coastal validation → 固定11 diagnostic preview → `TilePlanV2`幾何のSponge v3 tile → `ReleaseSurfacePublisherV2`（staging → strict read-back → atomic publish）である。publish後に`ReleaseSurfaceVerifierV2`でdirectoryとZIPのmanifest一致を確認し、`ReleasePlacementEligibilityVerifierV2`を通過した場合だけ`Release2ExportResultV2`をsealする。tile幾何を`TilePlanV2`正本に合わせたため、publish済みReleaseは`V2-6-20`の`VerifiedReleaseCanonicalBlockSourceV2`でそのまま開けて`PlacementPlanCompilerV2`のplacement planをcompileでき、`requirePlanMatches`が通る（world mutationは行わない）。coastal featureが所有しないcellのland-waterと地表高は推測せず、呼び出し側が`SurfaceBaselineV2`として明示する。`ExportBudgetV2`はtile数とdense descriptor working setをartifactを1本も書く前にadmitする。新capability、新artifact format、v1変更、CLI／Paper command routingは含まない（routingは`V2-12-03`）。証拠は`Release2ExportApplicationServiceV2Test`（E2E＋placement plan、design→export接続、determinism、mid-pipeline cancel cleanup、tile／resident budget拒否）とfull test／buildである。次は`V2-12-03`である。

`V2-12-03`（2026-07-20**完了**）はCLIとPaperへv2の正式command経路を実装した。`core.v2.command`にBukkit非依存の共有routing契約（`V2CommandVerbV2`のverb表、`V2CommandRouterV2`、`V2CommandRouteV2`、`V2CommandErrorCodeV2`、offline verbのbackendである`V2WorkflowServiceV2`）を置き、`/lfc v2 <verb>`と`lfc v2 <verb>`が同一の文法・arity・permission nodeを共有する。ADR 0035 D5のとおり`/lfc r2 <op>`は同一semanticのdeprecated aliasとしてrouterがcanonical形へ翻訳し、実行時にdeprecation警告を出す（placement lifecycleの実装は`handleRelease2`の1本のまま）。verbは request／design／generate／export／preview（offline、CLI＋Paper）と place／status／undo／recover（Paper専用）で、CLIからworld系verbを呼ぶと安定code`V2_PAPER_ONLY`で拒否される。`generate`は`export`のZIP無し版、baselineは`<land|water> <land-y> <water-y>`として明示必須である（`V2-12-02`の非推測方針）。Paper側は`PaperV2WorkflowServiceV2`が`data/v2/`配下へpathをsandbox化（絶対path・`..`・symlink拒否）し、生成はbounded executorで実行してmain threadを塞がない。permissionはADR 0035 D4の明示承認どおり`landformcraft.v2.*`へ改名し、旧`landformcraft.r2.*`をdeprecation window中の同権限として受理する（削除は`V2-12-06`）。tab completionはpermission・surface・world policyを尊重し、全失敗経路に安定error codeと`v2CorrelationId`が付く。v1 command（routing・出力・permission）は未変更である。64×64のcoastal fixture`harbor-cove-64`をexampleへ追加した（Azureのnormalized geometryを保持し、block単位parameterだけを再調整）。自動testは`V2CommandRouterV2Test`／`LandformCraftCliV2Test`／`V2CommandPathE2EV2Test`（fake gateway E2E: request→export→64×64 place→status→undo→`UNDONE`）で、full test／buildが通る。実機smokeは`scripts/smoke/v2-12-03-run.sh`でPaper 1.21.11＋WorldEdit 7.3.19単独・64×64を実行し、production `lfc v2 export`が作ったReleaseを`/lfc v2 place plan→confirm→execute`で`APPLIED`（settleとeffect envelope全体のexact verify成功）、`/lfc v2 status`と`/lfc r2 status`（deprecation警告付き）、`/lfc v2 undo plan→execute`で`UNDONE`まで通した（[evidence](design-v2/audits/v2-12-03-v2-command-path-evidence.md)）。同auditの§4に本実行で判明したrunnerの欠陥3件（fill guardのfalse positive、RCON応答を証拠化していなかった点、deprecation警告の二重スラッシュ）と修正を記録した。v1 command不変のlive証拠は取得できておらず自動testで担保している。catalogの能力・寸法昇格は行っていない。次は`V2-12-04`である。

`V2-12-04`（2026-07-20**完了**）はv1→v2 migration toolを実装した。`core.v2.migration`の`LegacyMigrationApplicationServiceV2`が、operatorの明示した1資産（v1 `terrain-intent.json`／v1 design package／Release format 1 directory・ZIP）をstrict readし、Release 2 design packageと`migration-report-v2.json`からなるmigration bundleへ変換する。CLIは`lfc v2 migrate inspect`（dry-run）と`lfc v2 migrate apply <kind> <source> <output-root> <migration-id> <strict|accept-lossy>`で、`migrate`はoperator workstationのlegacy pathを読むためCLI専用（Paperからは安定code`V2_CLI_ONLY`）である。**v2 intentへ写るのは`schemaVersion`と`theme`だけである。** `TerrainIntentV2.Feature`はnormalized geometryを必須とし、v1のzoneは`preferredArea`と`areaShare`しか持たないため、topology／seaSides／landRatio／relief／coastline／water／zone／structureをv2 featureやconstraintへ変換することは位置・形状の発明になる。これらはreportへ1件ずつ理由付きで列挙し、intentには入れない。Release 1のtile schematic／structures／assets／previewも、v2 Blueprintのmodule・stage・field descriptorを欠くため非対応elementとして報告し、移行後のintentから`lfc v2 export`で作り直す。結果として実質すべてのv1資産がlossyであり、`apply`は`strict`（非対応が1件でもあれば拒否）か`accept-lossy`の明示を必須とする。ADR 0035 D9の10条件（非破壊、dry-run、非overwrite、determinism、report、version＋checksum記録、部分publish禁止、unknown version／未定義fieldの拒否、corrupted fail closed、結果のRelease 2 strict verify）を実装で満たし、D2b／R7の先行整備としてv1 contract schema 8件のimmutable copyを`src/main/resources/legacy/v1/contracts/`へ置き`StructuredDataValidator`をfallback解決へ変更した（active copyは削除せず、削除は`V2-12-06`）。`LegacyMigrationPackagedJarV2Test`がactive v1 schemaとv1 orchestration classを隠すClassLoader下でのpackaged-JAR migrationを検証し、D2b Acceptance 6を満たす。v1 Schema・generator・Release format 1・v1 placement／Undo・v1 commandは不変で、catalogの能力・寸法昇格も行っていない。**未解決事項**: `docs/design-v2/migration-plan.md` §6「自動変換できるもの」は推測値を`SOFT`＋provenanceとして書き出す方針で、本実装の保守方針と差異がある。現行`TerrainIntentV2`ではgeometryにstrengthが無く§6は契約変更なしには実装できないため、Task Index Scope／Gate／AGENTS.md §6に従った。方針決定が必要である（同§へ記録済み）。次は`V2-12-05`である。

`V2-12-05`（2026-07-21**停止／STOPPED**）はCLI／Paperの既定経路をv2へ切替える前提として、ADR 0035 D10 command inventory（Paper／CLIの全verbを、削除するverbと維持するverbの両方について分類した完全版）とv1→v2カバレッジ監査を作成した（[監査](design-v2/audits/v2-12-05-v1-to-v2-coverage-audit.md)）。D2a-R5が要求するprovider同等性検査は、設定key（`ProviderSettings`）、retry（`TerrainDesignPolicy.maxAttempts`）、quota（`ProviderQuota`）、audit記録（`DesignAuditV2`）がv2で同等以上であることを確認した。しかしADR 0035 D6が明示承認している劣化はWorldEdit単独runtimeの**寸法のみ**であり、その範囲外の劣化を4件検出した。**F1**（重大）: v1のrequest authoring — `request create`、`request bounds`、WorldEdit selectionからのbounds取込、chat 1件を取り込む`request prompt`、`request list` — にv2等価物が無く、v2の`request`は既存`generation-request-v2.json`のvalidate／infoだけである。backendの`createRequest`／`setBounds`／`setPrompt`／`requestList`は削除対象R4の`PaperWorkflowService`にのみ存在する。**F2**（中）: v2側に`Release2RetentionServiceV2`等の実装はあるがcommandへ未接続で、v1の`cleanup`、CLI `journal-verify`、CLI read-only `recovery`に相当するv2 verbが無い。**F3**（重大）: v1の非同期job→candidate→token確認付きexport lifecycleと`job status|cancel`にv2等価物が無く、v2の`generate`／`export`は同期単発である。このためD10表が`job`へ求める「v1／v2 dispatch」のdispatch先が存在せず、Scope項目「D10維持verbのv2 backend接続」を`job`について履行できない。**F4**（軽微・要判断）: `ProviderFailureCode` 9値が`DesignFailureCodeV2` 4値へcollapseされ、認証失敗とrequest不正、timeoutとserver error、rate limitとtoken budget超過が運用者に見えるcodeから区別できない。`git tag`は0件のためD5条件1の代替判定「v1経路にしか存在しない利用可能機能はゼロ」が必須だが、F1／F3により**不成立**である。ADR 0035 D3-K3の「`V2-12-05`で他の未カバー機能が判明した場合は停止し、本ADRをamendする」とD6の同旨規定に従い、default routing切替、`/lfc v1`／`lfc --v1` opt-in、v1 deprecation表示、user／admin／README／Quickstart更新、CHANGELOGは**実施していない**。コード変更はゼロで、v1・v2の既存挙動はすべて不変である。2026-07-21に**案A（不足機能をv2へ実装する）が承認され、[ADR 0035](adr/0035-v1-retirement-governance.md) rev.4 D11として正本化した**。F1は`V2-12-08`（v2 request authoring）、F3は`V2-12-09`（v2 job／candidate／export lifecycle）、F2は`V2-12-10`（v2運用verbの接続）で解消する。F4のみD6へ明示承認として追記し、`DesignFailureCodeV2`は拡張しない（凍結識別子の変更費用が運用上の影響に見合わないため。元codeは例外のcause chainに残り管理者ログで追跡できる）。rev.4はD2a R4の追加条件へ`V2-12-08`＋`V2-12-09`完了を、R3の追加条件へ`V2-12-10`完了を加えた（削除対象集合R1〜R8は不変）。`V2-12-05`は`V2-12-08`〜`V2-12-10`完了後に再開し、カバレッジ監査を最初からやり直す。`V2-12-06`へは進めない。次は`V2-12-08`である。

`V2-12-08`（2026-07-21**完了**）は`V2-12-05`監査のF1（v1にはあるrequest authoringがv2に無い）を、ADR 0035 rev.4 D11の方針どおり**劣化を承認せずv2実装で**解消した。新しいBukkit非依存の`core.v2.command.V2RequestStoreV2`がv2 requestのcreate／bounds／constraint map宣言／prompt／listを担う。`GenerationRequestV2`契約は不変で、各操作は現在のrequestを読んで1 fieldだけ差し替え、`LandformV2DataCodec`経由で書き戻すため、strict schema検証とatomic publishを毎回通り中途状態が残らない。verbは`v2 request create <id>`（既定128×128／y -32..160／water 62／seed 0／tileSize 64。既存IDは上書きせず失敗）、`bounds <id> <w> <l> <min-y> <max-y> <water>`（waterは新範囲へclamp）、**Paper専用**の`selection <id>`（現在のWorldEdit選択範囲からwidth／length／min-y／max-yを取り込み、保存済みwater levelはclampして維持。CLIからは`V2_PAPER_ONLY`）、`constraint-map <id> <slug> <file> <sha256> <w> <l>`、`prompt <id>`（Paperは次のchat 1件を取込。5分失効・`cancel`取消）と`prompt <id> <text...>`（CLI）、`list`である。`validate`／`info`の意味とpath引数は不変で、保存先はPaperが`data/v2/requests/`、CLIが`<--data-dir>/v2/requests/`、request IDはlowercase portable slugのみでpathを受け取らない。`constraint-map`を含めたのは、`surface-2_5d` exportが「宣言済みconstraint map sourceちょうど1件」をfail closedで要求するためで、宣言verbが無いとauthoringしたrequestは`v2 export`へ到達できずGateを満たせない。canonical categorical形（U8 grayscale・north-west原点・east/south軸・pixel中心・回転／反転なし・全面crop・`0=water` `1=land`・no-data禁止）へ固定し、それ以外の形式は推測せず手書きJSONのままとした。routerは1つのoperation tokenがsurfaceごとに異なるarityを持つ場合（`prompt`）にcaller surface→arityの順で解決するよう拡張し、各surfaceが自分のusageを受け取る。permissionは`landformcraft.v2.request.create`／`.edit`を追加した（read系は既存の`.request`）。promptのcredential filterはstore側にあるため、v1 Paperのchat取込より厳しい。証拠は`V2RequestStoreV2Test`（16件）、`V2CommandRouterV2Test`追加6件、`LandformCraftCliV2Test`追加6件（create→bounds→constraint-map→prompt→listと、**authoringしたrequestが`v2 export`で`placementEligible: true`まで到達するE2E**）とfull test／buildである。v1 request store、v1 command、v1 golden、Release format、catalogは不変で、能力・寸法昇格は行っていない。Scope明確化2点をTask Index Gateへ記録した（Paperの選択範囲取込は`bounds selection`ではなく`selection` operationとしたこと、非Scope「image入力経路」はV2-7の画像抽出pipelineを指しrequest levelのsource宣言を含まないと解釈したこと）。次は`V2-12-09`である。

`V2-12-09`（2026-07-21**完了**）は`V2-12-05`監査のF3（v1の非同期job→candidate→確認付きexportにv2等価物が無い）を、ADR 0035 rev.4 D11のとおりv2実装で解消した。新package `core.v2.job`／`format.v2.job`／`model.v2.job`の`ExportJobServiceV2`が`Release2ExportApplicationServiceV2`を**包む**形で非同期lifecycleを提供するため、既存の単発同期`v2 export`／`v2 generate`は一切変更していない。cancelはAGENTS.md §11のとおり連動する。`cancel`はexport pipelineが参照するcancel tokenを立て、`CompletableFuture`をinterrupt付きでcancelし、終端stateを永続化する。publisherはcommit point（atomic move）直前までtokenを観測するため、**取り消したjobはReleaseを公開しない**（testで公開物ゼロを確認）。stateは`QUEUED`→`RUNNING`→`PUBLISHED`／`CANCELLED`／`FAILED`で、遷移ごとにsnapshotを書くため再起動後も`job status`が答えられ、終端jobへの再cancelは冪等である。新Schema `generation-job-v2.schema.json`とexample `examples/v2/job/export-job-v2.json`を追加し、v1 `generation-job.schema.json`は凍結のまま拡張も再利用もしていない。candidate列挙はRelease format 2 manifestがrequest identityを持たず、format変更が本Taskの停止条件であるため、job snapshotの`requestId`を正本にして「そのrequestの公開済みjob」をjob ID順で返す形にした。`ExportPlanStoreV2`はv1の`export plan`→`export create`に相当するreservation-bound confirmation tokenを提供し、plan時にfree diskをadmitし、SHA-256 hashだけを保持（平文token非保持）、constant-time比較、単回使用・10分失効・actor束縛・未確認plan 256件上限を課し、plan後にrequestかintentが編集されるとdigest不一致で拒否する。verbは`v2 export plan`／`v2 export create`（Paper専用。planは実行中processに存在し確認したjobも同processで走るため。v1の同verbもPaper専用だった）と`v2 job status|cancel|list`／`v2 candidate list|info`（両surface。job storeが永続的なためCLIからも読めるが、CLIの`job cancel`は別processのworkerに届かずdurable snapshotを`CANCELLED`にするだけで、v1 CLIと同じ性質である）。上位verbの`/lfc job`／`/lfc candidate`をv1／v2 dispatchへ変更することはしていない。既定routingの切替は`V2-12-05`のScopeであり、本Taskはそのdispatch先となるv2 backendを用意した（ADR 0035 D10の`job`行が要求していた欠落はこれで解消した）。routerは1つのverbが直接形とoperation形を併せ持つ場合をoperation名かつarity一致で判定するよう拡張したため、`plan`という名前のfile引数も直接形へ正しくroutingされる。証拠は`ExportJobServiceV2Test`（11件）、`ExportPlanStoreV2Test`（7件）、`V2CommandRouterV2Test`追加5件、`LandformCraftCliV2Test`追加4件、`SchemaContractTest`のsealed example読取とfull test／buildである。v1 job store、v1 command、v1 golden、Release format 2、placement contract、catalogは不変で、能力・寸法昇格は行っていない。次は`V2-12-10`である。

`V2-12-10`（2026-07-21**完了**）は`V2-12-05`監査のF2（v1の`cleanup`／`journal-verify`／`recovery` read-onlyに相当するv2運用機能がcommandへ未接続）を、ADR 0035 rev.4 D11のとおり接続で解消した。retention policyそのものは変更していない。**retention（Paper、v1 `cleanup`後継）**: 既存の`core.v2.operations.Release2RetentionServiceV2`を`/lfc v2 retention plan|execute|status`へ接続した。`plan <placement-id>`はplacementのapplied journalからplan＋journalを読み、recovery plannerがretention window内と判定したsnapshotのbyte総量を示す1回限りの確認token（10分失効、actor束縛）を発行、`execute`がtokenを消費して削除、`status`がjournal stateとsnapshot byte使用量を表示する。削除対象はrecovery cleanup planが列挙したwindow外terminal snapshotだけで、この判定は`V2-6-13`と同一である。本番配線として、placement統合が有効なとき`Release2PlacementApplicationServiceV2.retentionCleanupPort()`（＝`RetentionCleanupPortV2.from(recovery)`）を`Release2RetentionServiceV2`へ渡すよう`Landformcraft`を変更した（従来のdeferred portはthrowするだけだった。統合が無ければ削除対象も無いためdeferredのまま）。retentionは実行中serverのplacement stateを読みsnapshot state（world blockではない）を削除するためPaper専用で、新permission `landformcraft.v2.retention`を要する。**journal-verify（CLI、v1同verb後継）**と**recovery inspect（CLI read-only、v1 `recovery status|diagnose`のread-only視点）**は、それぞれv2 placement journalのstrict検証と、journal state（`RECOVERY_REQUIRED`含む）／recovery plan classificationのread-only表示を行う。両者は明示artifact fileだけを読みworldを変更しないためCLI専用（Paperからは`V2_CLI_ONLY`）で、v2 recoveryの実行系`recover diagnose|plan|execute`（Paper専用）とはverb token（`recover`／`recovery`）で区別される。CLIに`recovery list`（全placement列挙）は設けていない。offline store走査はv2の明示path・非推測方針に反するため、Paper起動時のrestart-state点検が列挙相当を担う。証拠は`Release2RetentionExpiryV2Test`（token失効で削除ゼロ、削除対象がrecovery scopeのbyte総量のみ）、`V2CommandRouterV2Test`追加3件、`LandformCraftCliV2Test`追加5件（actor束縛・wrong-token拒否は既存`OperationalOperationsV2Test`が担保）とfull test／buildである。v1 `cleanup`／`journal-verify`／`recovery`、v1 command、v1 golden、Release format、placement contract、catalogは不変で、能力・寸法昇格は行っていない。これで`V2-12-08`〜`V2-12-10`が揃い、`V2-12-05`の再開条件（ADR 0035 rev.4 D11）が成立した。次は`V2-12-05`の再開である（本Taskからは開始しない）。

`V2-12-05`（2026-07-21**完了、人間承認 nankotsu029**）は、`V2-12-08`〜`V2-12-10`完了後にカバレッジ監査を最初から再実行し、F1〜F3の解消とF4のADR 0035 D6承認範囲内を[再監査](design-v2/audits/v2-12-05-v1-to-v2-coverage-audit-rerun.md)で確認してから既定経路をv2へ切替えた。Paperは`/lfc <verb>`＝`/lfc v2 <verb>`、CLIは`lfc <verb>`＝`lfc v2 <verb>`で、v1は`/lfc v1 <verb>`／位置非依存global option `lfc --v1 <verb>`の明示opt-inとなり、実行時にdeprecation警告を出す。`/lfc r2`は同一semanticのdeprecated aliasとして残る。`git tag`は0件のため、D5条件1の代替「v1経路にしか存在しない退役対象機能はゼロ」をD10 command inventoryで確認した。version中立verbとD2b legacy readerは維持し、v1 Schema、generator `3.0.0-phase6`、Release format 1、placement／Undo、golden、catalog、Release 2 contractは不変である。routing／opt-in／deprecation／help回帰、v1 golden、full test／buildを通し、README、Quick Start、User／Admin／Commands／Operations、CHANGELOG、Task Indexを同期した。次は`V2-12-06`だが、本Taskからは開始しない。`V2-12-06`には独立した人間承認とADR 0035 D8 drain gateが必要である。

`V2-12-06`（2026-07-21**完了、人間承認 nankotsu029**）は、ADR 0035 rev.5の未公開project限定例外により30日条件だけを免除し、D8、R1〜R8、D2b／D3、識別子凍結、test gateを維持してv1退役を実行した。削除前preflightはWorldEdit／FAWEの2 runtime rootをneutral archiveへbyte-exact保存し、strict read-back／inventory SHA-256／unresolved 0でPASS。v1 production writer／generator／placement／commandとV2-0 compatibility spineをD7順で削除し、v1 Schema／fixtureはactive inventoryから`src/main/resources/legacy/v1/`へ移設した。D2bのread／verify／migrate、immutable golden、custom asset、version中立基盤・運用verbは維持。`/lfc r2`／`/lfc v1`／`--v1`／`landformcraft.r2.*`は削除し、default v2と恒久的な`v2`明示aliasだけを残した。[retirement evidence](design-v2/audits/v2-12-06-v1-retirement-evidence.md)と[drain JSON](design-v2/audits/v2-12-06-drain-evidence.json)を保存し、targeted testとfull `./gradlew test`（985件、6 skip）が通過した。次は`V2-12-07`だが、本Taskからは開始しない。

`V2-12-07`（2026-07-21**完了**）はV2-12全体を統合監査しPhase gateを閉じた。監査対象は[ADR 0035](adr/0035-v1-retirement-governance.md) Consequencesが定義する**v2 production-only構成**（v2が唯一のproduction writer／generator／placement／通常command経路であり、v1はD2b migration専用read境界とD3維持資産だけに隔離された構成。「v1コードが一切ない構成」ではない）である。新規executable gate `MigrationPhaseGateV2Test`（6件）が、(1) production-only構成境界（D2a writer不在、D2b/D3維持資産存在、R7 v1 schema 10種のactive inventory排除＋packaged legacy copy存在、`landformcraft.r2.`／`--v1`削除、router root=`v2`・r2 root拒否）、(2) 8 production verb（request／design／generate／export／preview／place／status／undo）のv2 routingと`landformcraft.v2.*` permission、(3) production offline E2E（request→generate/export→strict preview→placement-eligible、`V2WorkflowServiceV2`）、(4) capability false-promotion不在（`paper_apply` SUPPORTED 4件、`PlacementDimensionLimitV2.measured()`、sealed catalog checksum一致、Release capability list `[environment-fields, hydrology-plan, sparse-volume, surface-2_5d]`不変）、(5) K1 v1 golden→D2b packaged reader migration回帰（deterministic PUBLISHED）、(6) portfolio determinism（正逆順／4 thread／tr-TR locale／Chatham timezone）を固定した。world placement／Undo／**Recovery** lifecycle、tamper corpus、fault injection、cross-runtime determinismは既存test（`V2CommandPathE2EV2Test`／`Release2PlacementApplicationServiceV2Test`／`PlacementRecoveryServiceV2Test`／`PlacementPhaseGateV2Test`／capability verifier suites）がfull clean suiteで再実行し担保した。`./gradlew clean test build`は**991件・6 skip・0 failure／error（175 test class）でBUILD SUCCESSFUL**（985＋新規gate 6）。v1 golden equivalence（K1 archive→migration path）、Release 1／design package read／verify（D2b）、4 Release 2 capability verifier、placement lifecycle portfolioは回帰不変で、Release format／capability／catalog／placement contract／凍結識別子は不変、能力・寸法昇格は行っていない。監査で**pre-existing・LOW・Scope外のtest-hygiene defect 1件**を検出した（`PlacementUndoServiceV2Test`／`PlacementRecoveryServiceV2Test`がassertion無しでtracked example `placement-undo-plan-v2.json`／`placement-recovery-plan-v2.json`へ書き込み、`confirmationHash`が`UUID.randomUUID()`由来security nonceのsha256で非決定的なため毎回working treeがdirtyになる。V2-6-09/10由来でV2-12とは無関係、失敗テストなし、v2単独運用に無影響）。gate criterion（未解決critical/high無し・v2単独運用成立）に該当しないためgateを閉じ、churnした2 exampleはcommitせず、defectを`V2-12-11`（P2/LOW）として登録した。Beta hardening blockerは免除せず、release decisionは[beta-release-checklist](beta-release-checklist.md)のとおり「Beta blockerが残るため未リリース」を維持する。証拠は [V2-12 Phase gate audit](design-v2/audits/v2-12-07-phase-gate.md) を正本とする。これでV2-12親Phaseは`完了`である。Track Aの残りはfollow-up defect `V2-12-11`だけで、本Taskからは開始しない。

`V2-12-11`（2026-07-21**完了**）は`V2-12-07` gateが検出したpre-existing・LOWのtest-hygiene defectを解消した。`PlacementUndoServiceV2Test`と`PlacementRecoveryServiceV2Test`のcodec round-trip testが、sealed example（`examples/v2/placement/placement-undo-plan-v2.json`／`placement-recovery-plan-v2.json`）を毎回`UUID.randomUUID()`由来の非決定的confirmation nonceで上書きし、`./gradlew test`のたびにworking treeを汚していた問題である。両testは既存のseam（`request.plaintextToken()`が非nullなら固定nonceを採用、nullなら従来どおりrandom）を使い、round-trip test**だけ**が固定placeholder nonceでexampleを決定論的に再生成するようにした（`PlacementUndoServiceV2Test`へ`afterApplied(root, twoTiles, plaintextToken)` overload、`PlacementRecoveryServiceV2Test`へ`prepareRequest`/`prepareAccept`のtoken overloadを追加。他の全scenarioはrandom token経路を維持）。production側の`PlacementConfirmationBinderV2.newPlaintextToken`（`UUID.randomUUID()`）はsecurity要件のため不変。2 exampleを固定nonceで再生成し、連続runでbyte-stable（churnなし）を確認した。`./gradlew clean test build`が通り、`PlacementPhaseGateV2Test`／`SchemaContractTest`のexample読取回帰は不変で、v1資産・Release format・capability・catalog・placement contract・default routingは不変である。これでV2-12のfollow-upも完了し、Track Aに未着手Taskは残らない。

`V2-7-01`（2026-07-17完了）では`format.v2.constraint.extract`へ`image-land-water-extract-v1`のinteger-only抽出core、`ExtractedMaskDraftV2`（semantic checksum付きdraft）、trusted ceiling付きadmission、行単位cancelを実装し、golden分類・決定性（repeat/thread/locale/timezone）・全拒否経路を`ImageLandWaterExtractorV2Test`で検証した。CLI/Paper/Requestへは未接続で、hard constraint化には明示昇格（V2-7-04）を必須とする。

`V2-7-02`（2026-07-21完了）では抽出専用のsecure入力封筒`SecureImageExtractionEnvelopeV2`を`format.v2.constraint.extract`へ追加した。retired `ReferenceImageProcessor`と同等のportable path／symlink／hardlink alias／magic／extension／byte／pixel／aspect／frame（APNG `acTL`＋`getNumImages`）／EXIF orientation検査、TOCTOU（size／mtime／file key）、trusted ceiling付きdecode／working budget（8 MiB／4M px／4096／aspect 32／ARGB working 64 MiB）、行・読込単位cancel、raw file SHA-256→`SanitizedArgbImageV2`→`ExtractedMaskDraftV2`のchecksum連鎖を実装し、実PNG／JPEGからdraftまでのstrict経路を`SecureImageExtractionEnvelopeV2Test`で検証した。Request Schema・draft artifact・CLI／Paper／AI Providerへは未接続のまま`EXPERIMENTAL`である。

`V2-7-03`（2026-07-21完了）ではdraft artifactとconfidence previewを追加した。`ExtractedMaskDraftArtifactPublisherV2`が`classes.u8`／`confidence.u8`＋`extracted-mask-draft-v2.json`をstaging→strict read-back→atomic publishし、`ExtractedMaskDraftPreviewRendererV2`が固定palette（`extracted-mask-draft-palette-v1`）のclass／confidence／unknown PNGを1枚ずつrenderしてstrict preview indexを公開する。Schema／example／`SecureImageExtractionEnvelopeV2Test`系に加え`ExtractedMaskDraftArtifactPublisherV2Test`／`ExtractedMaskDraftPreviewRendererV2Test`でround-trip・改変拒否・cancel cleanup・決定性を検証した。昇格・Release capability・CLI／Paper／Requestへは未接続のまま`EXPERIMENTAL`である。

`V2-7-04`（2026-07-21完了）ではdraft→数値constraint PNGへの明示昇格を追加した。`ExtractedMaskPromotionServiceV2`がconfidence閾値とUNKNOWN処理（`REJECT`／`MAP_TO_WATER`／`MAP_TO_LAND`／`MAP_TO_NODATA`）の明示指定を必須とし、U8 grayscale `land-water.png`をstagingして`SecureConstraintMapSourceLoader`＋`NumericPngDecoder`でV2-1再検証したのち、`extracted-mask-promotion-v2.json` provenanceと合わせてatomic publishする。source checksum → draft semantic checksum → map SHA-256 → record canonical checksumの連鎖を固定し、`ExtractedMaskPromotionServiceV2Test`でround-trip・閾値境界・改変拒否・cancel cleanup・決定性を検証した。自動／暗黙昇格・AI補完・CLI／Paper／Request／Release capabilityへは未接続のまま`EXPERIMENTAL`である。

`V2-7-05`（2026-07-21完了）では輝度ベースのheight guide draft抽出と明示昇格連携を追加した。`ImageHeightGuideExtractorV2`（`image-height-guide-extract-v1`）がinteger-only輝度（BT.601風重み）からU8 sample（0..254 clamp）＋confidenceを作り、alpha < 128をno-dataとする。sample空間は`luminance-u8-requires-explicit-height-value-meaning-v1`として宣言し、V2-1の3種`HeightValueMeaning`を抽出時に推測しない。`ExtractedHeightGuidePromotionServiceV2`が意味／scale／offsetの明示指定とconfidence閾値を必須とし、`height-guide.png`をV2-1 strict decoderと`CanonicalConstraintRasterV2` residual一貫性検査の後にatomic publishする。Schema／example／`ImageHeightGuideExtractorV2Test`／`ExtractedHeightGuidePromotionServiceV2Test`でgolden・no-data・clamp・3意味昇格・決定性を検証した。CLI／Paper／Request未接続、`EXPERIMENTAL`のまま。次は`V2-7-06`である。

`V2-7-06`（2026-07-21完了）では固定palette距離量子化によるzone／sketch label draft抽出と明示昇格連携を追加した。`ImageZoneLabelExtractorV2`（`image-zone-label-extract-v1`）がsketch palette v1（shore／upland／wetland／rock）への整数平方ユークリッド距離でlabel index＋confidenceを作り、ambiguous帯・遠色・alpha < 128をUNKNOWNとする（k-means禁止）。sample空間は`fixed-palette-distance-quantize-requires-explicit-categorical-encoding-v1`として宣言する。`ExtractedZoneLabelPromotionServiceV2`がconfidence閾値とnoDataを明示し、`zone-labels.png`をV2-1 categorical decoderと`ZONE_LABEL_MAP` canonical経路の後にatomic publishする。Schema／example／`ImageZoneLabelExtractorV2Test`／`ExtractedZoneLabelPromotionServiceV2Test`でpalette golden・ambiguous・budget・決定性・昇格を検証した。CLI／Paper／Request未接続、`EXPERIMENTAL`のまま。次は`V2-7-07`である。

`V2-7-07`（2026-07-21完了）では多入力競合解決とPhase gateを閉じた。`MultiSourceReconciliationServiceV2`（`image-fidelity-multisource-reconcile-v1`）が`image-constraint-priority-v1`優先順位（manual hard→hard map→manual soft→soft map→image draft→prompt soft→preset）でprompt vs 画像・hard vs draftを解決し、HARD/HARDおよび同rank SOFT peerはfail closed（last-write-wins禁止）。source-to-result差分metricとresult／conflict／source-diff previewをstrict artifactとして公開した。`ImageFidelityPhaseGateV2Test`と[V2-7 Phase gate audit](design-v2/audits/v2-7-phase-gate.md)でportfolioを統合監査し、抽出経路を**SUPPORTED候補**として記録した。runtimeは`EXPERIMENTAL`・CLI／Paper／Request未接続・Release capability追加なしのまま。Track B（V2-7）は完了である。

`V2-8-01`（2026-07-17完了）では`model.v2.scale`のscale class（SMALL/MEDIUM/LARGE≤3072）、version凍結defaultsを持つ`ScaleProfileV2`、canonical row-major `TilePlanV2`、`core.v2.scale.ScaleAdmissionV2`（`scale-admission-v1`）を実装し、3000×3000=576 tileのgolden分解・決定性・全failure codeを`TilePlanV2Test`/`ScaleAdmissionV2Test`で検証した。既存の分散寸法検査とv1・全checksumは不変で、LARGE生成は未実装である。次は`V2-8-02`である。

`V2-9-01`（2026-07-18完了）ではsurface foundationの共通2.5D field／ownership／transition契約を`model.v2.foundation.SurfaceFoundationPlanV2`（`surface-foundation-field-contract-v1`）へ固定した。依存は方針Aどおり`V2-6-02`と`V2-8-01`（`ScaleAdmissionV2`）のみとし、`V2-8-02`は前提にしない。`SurfaceFoundationPlanCompilerV2`がScaleProfile admission・named seed namespace（`terrain.v2.foundation`）・5 field binding（surface-class／elevation／residual／owner-index／transition-weight）・owner／interaction／budgetをcompileし、`SurfaceFoundationMergeCompilerV2`が2 synthetic ownerのwhole／tile merge同checksumとownerless／tie／undeclared overlap／band 0..32外／seed collision拒否を検証する。Schema／example／strict codecを追加し、kindの`SUPPORTED`昇格・形状generator・WorldBlueprint埋込・Paper・LARGE enableは対象外である。次は`V2-9-02`である。

`V2-9-02`（2026-07-18完了）では`PLAIN`と`HILL_RANGE`のvertical sliceを`EXPERIMENTAL`で実装した。Intent／Schemaへtyped parameterを追加し、`PlainPlanV2`／`HillRangePlanV2`、integer-only generator（microrelief＋groundwater handoff、closed ridge／saddle budget＋plain transition）、`FoundationPlainHillSliceCompilerV2`、validation artifact／preview index、sealed plan exportを提供する。dedicated moduleは存在するが既存WorldBlueprint checksum不変のためグローバルcatalogへは未登録（diagnostic binding）。`BACKSHORE_PLAINS`はIDを維持し`BackshorePlainsAliasV2`でPLAIN候補へ写像するだけである。transition corruptionとwhole／tile checksumを`FoundationPlainHillSliceCompilerV2Test`で検証し、`SUPPORTED`昇格はしていない。次は`V2-9-03`である。

`V2-9-03`（2026-07-18完了）では`MOUNTAIN_RANGE`と`VALLEY`のvertical sliceを`EXPERIMENTAL`で実装した。ridge／peak／saddle／spur／pass／foothillはderived component ID（FeatureKind化なし）、valleyはV／U cross-section＋floor／shoulder／connection role、`FoundationMountainValleySliceCompilerV2`がmerge／validation／preview／sealed exportを閉じる。ALPINE／GLACIAL／FJORD specialized seed・module・`MountainParameters`／`FjordParameters`は未変更。peak／pass budget・floor owner conflict・valley connection拒否とwhole／tile checksumを`FoundationMountainValleySliceCompilerV2Test`で検証し、`SUPPORTED`昇格はしていない。次は`V2-9-04`である。

`V2-9-04`（2026-07-18完了）ではpublic `FeatureKind.RIVER`と`RiverPlanV2`（source→mouth reach graph、bank／bed／discharge／floodplain handoff）を`EXPERIMENTAL`で追加した。`FoundationRiverSliceCompilerV2`がvalidation／preview／sealed exportを閉じ、orphan／self-loop graphを拒否する。`MEANDERING_RIVER`／`HydrologyRiverModuleV2`／`MeanderingRiverPlanV2`のserializationとSUPPORTED catalog bindingは不変で、`MeanderingRiverSubtypeBridgeV2`はsuggested parameters写像のみを提供する。`SUPPORTED`昇格とWorldBlueprint埋込はしていない。次は`V2-9-05`である。

`V2-9-05`（2026-07-18完了）では`FLOODPLAIN`／`MARSH`を独立ownershipの`EXPERIMENTAL` hydrologic surfaceとして追加した。`FloodplainPlanV2`／`MarshPlanV2`と`FoundationFloodplainMarshSliceCompilerV2`がriver adjacency・groundwater／hydroperiod・wetness・open-water／fluid・solid ownership・floodplain↔marsh transitionのvertical sliceを閉じる。river graphは別途compileし、surface mergeはfloodplain+marshのみ。`MANGROVE_WETLAND`／mangrove module／seedsは未変更。dry marsh・filled channel・river disconnect・groundwater／hydroperiod conflictを拒否し、`SUPPORTED`昇格はしていない。

`V2-9-07`（2026-07-18完了）では`SINGLE_ISLAND`／`ARCHIPELAGO`／`VOLCANIC_CONE`のEXPERIMENTAL foundation vertical sliceを追加した。island mass／shore／drainage／apron、group spacing／dominance／saddle、cone／crater／radial drainageを閉じる。`VolcanicIslandConeAdapterV2`はsuggested paramsのみ。dedicated moduleはcatalog未登録（diagnostic binding）。既存`VOLCANIC_ARCHIPELAGO` checksum／hook／moduleは未変更。`SUPPORTED`昇格はしていない。

`V2-9-08`（2026-07-18完了）では`OCEAN_BASIN`／`CONTINENTAL_SHELF`／`CONTINENTAL_SLOPE`のEXPERIMENTAL bathymetry foundation sliceを追加した。depth／slope／coast-distance／ownership／fluid-column-hint fieldsと3 vertical slices、`FoundationBathymetryTransectCompilerV2`によるcoast→basin断面、whole／tile depth checksum、streaming underwater column export（dense 3D water配列なし）を閉じる。dedicated moduleはcatalog未登録（diagnostic binding）。`ROCKY_COAST`／coral／hydrology SUPPORTED経路は未変更。`SUPPORTED`昇格はしていない。

`V2-9-09`（2026-07-18完了）では`SUBMARINE_CANYON`のEXPERIMENTAL foundation vertical sliceを追加した。shelf head／slope crossing／basin outletのHARD relation、centerline floor depth＝host bathymetry＋additional carve、V／U cross-section corridor、whole／tile checksum、streaming underwater column exportを閉じる。surface `CANYON`／`CanyonPlanV2`／`LandformCanyonModuleV2`は未変更。dedicated moduleはcatalog未登録（diagnostic binding）。`SUPPORTED`昇格はしていない。

`V2-9-10`（2026-07-18完了）では`CAVE_ENTRANCE`のEXPERIMENTAL surface-volume connectorを追加した。`CaveEntranceParameters`、`CaveEntrancePlanV2`／`FoundationCaveEntranceSliceCompilerV2`、HARD `ENTRANCE_OF`（frozen `CaveNetworkPlanV2` checksum bind）＋`SUPPORTED_BY`（`MOUNTAIN_RANGE`／`VALLEY`）、opening／approach carve、roof／flood／owner／reachability validation、seamless query／export checksumを閉じる。cave-network plan checksumはin-place変更しない。dedicated moduleはcatalog未登録（diagnostic binding）。`SUPPORTED`昇格はしていない。

`V2-9-11`（2026-07-18完了）では`UNDERGROUND_RIVER`と`FLOODED_CAVE` fluid-region hookのEXPERIMENTAL flooded-volume connectionを追加した。`UndergroundRiverParameters`、`UndergroundRiverPlanV2`／`FoundationUndergroundRiverSliceCompilerV2`、HARD `WITHIN`（frozen cave checksum）＋lake chamber／`fluidBodyId`単一owner bind、carve→`ADD_FLUID` ordering、reachability／down-gradient／leak／air-pocket validation、whole／tile／scene export checksumを閉じる。cave-network／underground-lake example checksumはin-place変更しない。dedicated moduleはcatalog未登録（diagnostic binding）。`SUPPORTED`昇格はしていない。

`V2-9-12`（2026-07-18完了）ではmacro land-water topologyを`MACRO_CONSTRAINT`契約として追加した。manual `LAND_WATER` mask＋`ZONE_LABEL`から`MacroLandWaterTopologyPlanV2`をcompileし、bay／isthmus／strait／enclosed-basin等をFeatureKindなしで表す。adjacency／containment／min-width検証、disconnected strait／collapsed isthmus／nested basin／ambiguous boundary／budget拒否、whole／tile／thread同一checksumによるfreeze handoffを閉じる。画像draftの暗黙hard昇格と8個のshape generatorは非Scope。次は`V2-9-13`である。

`V2-9-13`（2026-07-18完了）では一般`RiverPlanV2`へgraph role／reach class／modifier／child ownershipを拡張し、`WATERFALL_CHAIN` COMPOSITE_PRESET（`WaterfallChainPlanV2`）を追加した。HEADWATER／STREAM／TRIBUTARY／CONFLUENCE／BIFURCATION／DISTRIBUTARY／RAPIDSとSANDBAR／RIVER_ISLAND／PLUNGE_POOLを同じgeneral IRで再現し、split／merge flow conservation・elevation continuity・modifier order・budget／orphan拒否を閉じる。専用waterfall-chain generatorとestuary／braided sliceは非Scope。既存meandering／delta／waterfall fixturesは不変。`SUPPORTED`昇格はしていない。次は`V2-9-14`である。

`V2-9-06`（2026-07-18完了）では`ROCKY_COAST`／`SEA_CLIFF`のEXPERIMENTAL foundation vertical sliceを追加した。rock shelf／exposure／channel／talus handoffとcliff face／talus／notch、coast transition、`hostSupportAabb`経由のsea-cave／overhang host handoffを閉じる。dedicated moduleはcatalog未登録（diagnostic binding）。`ROCKY_CAPE` checksumとSeaCave／Overhang compilerは未変更。`SUPPORTED`昇格はしていない。

`V2-9-14`（2026-07-18完了）ではV2-9全体を統合監査し、V2-9 Phase gateを閉じた。`FoundationPhaseGateV2Test`が19 foundation kindのdiagnostic binding維持（false-promotionなし）、18 dedicated moduleの`EXPERIMENTAL`、Release 2 capability list不変（`[environment-fields, hydrology-plan, sparse-volume, surface-2_5d]`）、22 foundation plan exampleのstrict read安定性（正逆読取順／1・4 thread／locale／timezone）、plain-hill代表scenarioの再compile同一checksumとwhole／tile merge一致、1000角2 owner mergeのScaleProfile admission＋whole／tile一致とoversized profile拒否を固定し、full clean suiteで全V2-9 positive／corruption／cancel、v1 golden、Release 1、V2-2〜V2-6回帰、clean buildを通過した。surface foundation contractと全V2-9 vertical slice（surface／hydrologic surface／river graph／coast／island／marine bathymetry／surface-volume connection／macro topology）のoffline plan-level generate／validation／previewを`SUPPORTED`、intent_compile／standalone_usage／exportを`PARTIAL`（diagnostic binding・sealed canonical JSONのみ、Release capability未定義）、Paper以降を`UNSUPPORTED`のまま共通canonical stream互換（V2-6 evidence継承経路）として記録した。WorldBlueprint checksum、module catalog、既存SUPPORTED経路、LARGE制限は不変である。証拠は [V2-9 Phase gate audit](design-v2/audits/v2-9-phase-gate.md) を正本とする。

`V2-10-01`（2026-07-18完了）では`VALLEY_GLACIER`／`ICE_CAP`／`ICE_SHEET`のEXPERIMENTAL glacial-ice foundation vertical sliceを追加した。共通`GlacialIcePlanV2`契約（flow／thickness／bed contact／meltwater／cold climate＋snow profile、bounded sparse `ADD_SOLID`）と`IceFjordPlanV2` COMPOSITE_PRESET（`FJORD + VALLEY_GLACIER + cold/snow`、FeatureKindなし・専用generatorなし）を閉じる。dedicated moduleはcatalog未登録（diagnostic binding）。warm climate／unsupported bed／meltwater hint corruptionとwhole／tile／thread／locale決定性を検証し、`SUPPORTED`昇格とdense ice voxel正本はしていない。

`V2-10-03`（2026-07-18完了）では`SINKHOLE`／`KARST_SPRING`のEXPERIMENTAL karst hydrology graph vertical slice（typed graph freeze、`KarstHydrologyGraphPlanV2`、loss/spring static balance、host `CAVE_NETWORK` checksum bind、`CenotePlanV2` COMPOSITE_PRESET）を追加した。`KARST_CAVE_SYSTEM`は`CAVE_NETWORK`上のgraph node roleでありFeatureKind化していない。`CENOTE`もFeatureKind化せず`FLOODED_CAVE` hook経由のpresetである。dedicated moduleはcatalog未登録（diagnostic binding）。sealed `cave-network-plan-v2.json` checksumは変更していない。

`V2-10-04`（2026-07-18完了）では`ABYSSAL_PLAIN`／`SEAMOUNT`のEXPERIMENTAL additional-marine foundation vertical slice（`AbyssalPlainPlanV2`／`SeamountPlanV2`、`FoundationAdditionalMarineSliceCompilerV2`、HARD `WITHIN`→`OCEAN_BASIN` host bind、basin containment／depth-relief／slope／transition／whole-tile export metrics）を追加した。`OCEAN_TRENCH`／`MID_OCEAN_RIDGE`／`SUBMARINE_VOLCANO`はplate-scale／ownership evidence不足のためcatalog評価のみ（FeatureKind化・generatorなし）。dedicated moduleはcatalog未登録（diagnostic binding）。sealed `ocean-basin-plan-v2.json` checksum（`51b0acafa65953d09bacc3734e8ef18c5b43af29135f00999d2abe7cf5d0eb22`）とcontinental shelf／slope／submarine canyon sealed plansは不変。次は`V2-10-05`である。

`V2-10-05`（2026-07-18完了）はadvanced river／lakeの**contract／分割決定のみ**である。7候補をownership分類し、最初の実装対象を`SPRING`（`V2-10-10`）と`OXBOW_LAKE`（`V2-10-11`）へ固定した。`RIVER_TERRACE`／`ALLUVIAL_FAN`／`ESTUARY`／`BRAIDED_RIVER`／`DAM_RESERVOIR`はdeferred。`AdvancedRiverLakeSplitContractV2`＋schema＋sealed exampleで決定をfreezeし、7候補のFeatureKind／generatorは導入していない。V2-10 Task数は9→11（gate `V2-10-09`は01〜08＋10＋11依存）。次は`V2-10-06`である。

`V2-10-06`（2026-07-18完了）では`ESCARPMENT`／`PLATEAU`のEXPERIMENTAL foundation vertical slice（`EscarpmentPlanV2`／`PlateauPlanV2`、`FoundationEscarpmentPlateauSliceCompilerV2`、HARD `OVERLAPS`＋`PRIORITY_BLEND` transition、long scarp／cap-floor-talus／material handoff／whole-tile export metrics）を追加した。`MESA`／`BUTTE`は`PlateauProfile`のみ（FeatureKind化なし）。dune/badlands/salt-flat群は`DryLandModifierContractV2`へ分類（FeatureKind／generatorなし）。surface `CANYON`は不変。dedicated moduleはcatalog未登録（diagnostic binding）。sealed `plain-plan-v2.json` checksum（`1748e6ca5953465e06af039af50df9d1b48c44f0ee41b2724f2a4bc4da55af6b`）は不変。

`V2-10-07`（2026-07-18完了）では`LAVA_TUBE`のEXPERIMENTAL foundation vertical slice（`LavaTubePlanV2`、`FoundationLavaTubeSliceCompilerV2`、HARD `WITHIN`→`VOLCANIC_CONE` checksum bind、HARD `ORIGINATES_AT`→`VOLCANIC_CALDERA`／`LAVA_FLOW_FIELD` provenance、`VolumeSdfPrimitiveV2.SweptSpline`＋`CARVE_SOLID` only、roof/support/entrance plan fields、host／continuity／roof／fluid-conflict／AABB／whole-tile export metrics）を追加した。tube internalsはFeatureKind化しない。dynamic lava／dense voxelなし。dedicated moduleはcatalog未登録（diagnostic binding）。sealed `volcanic-cone-plan-v2.json` checksum（`835388b9c778e95917891361fbe764e219204410fc2ab7b91f3b6cd045b8deae`）は不変。

`V2-10-08`（2026-07-18完了）では`BARRIER_ISLAND`／`ATOLL` COMPOSITE_PRESET（`BarrierIslandPlanV2`／`AtollPlanV2`、`FoundationAdvancedIslandReefSliceCompilerV2`、`AdvancedIslandReefCatalogContractV2`）とadvanced island/reef catalog分類を追加した。`SINGLE_ISLAND`＋`LAGOON` shore-parallel barrier、`CORAL_REEF`＋`LAGOON`＋`REEF_PASS` atoll（coral fixture relations再利用）、catalog候補5種（`FLOATING_REEF` deferred）。FeatureKind／dedicated moduleは導入しない。sealed `single-island-plan-v2.json` checksum（`a38974fcd83d8d3dd16366b1411c69fa94e41e35a6ed125df0301a4fb5982d73`）とdiagnostic coral-reef／volcanic fixturesは不変。次は`V2-10-10`である（`V2-10-09` gateは10/11完了待ち）。

`V2-10-10`（2026-07-18完了）ではsurface `SPRING` EXPERIMENTAL graph-node slice（`SpringPlanV2`、`FoundationSpringSliceCompilerV2`、`LandformSpringModuleV2`）を追加した。HARD `SUPPORTED_BY` surface host、`EMPTIES_INTO` general `RIVER`、`RiverPlanV2` SOURCE+HEADWATER bind、`HydrologyPlanV2.NodeKind.SPRING` id、outflow/source continuity metrics、validator／preview／export。`KARST_SPRING`／karst graph／sealed river fixturesは不変。moduleはcatalog未登録（diagnostic binding only）。sealed `karst-spring-plan-v2.json`／`river-plan-v2.json`／`river-graph-roles-plan-v2.json`／`advanced-river-lake-split-contract-v2.json` checksumは不変。次は`V2-10-11`である（`V2-10-09` gateは11完了待ち）。

`V2-10-11`（2026-07-18完了）では`OXBOW_LAKE` EXPERIMENTAL reach-cutoff basin slice（`OxbowLakePlanV2`、`FoundationOxbowLakeSliceCompilerV2`、`LandformOxbowLakeModuleV2`）を追加した。HARD `ORIGINATES_AT` parent `RIVER`／`MEANDERING_RIVER`、HARD `SUPPORTED_BY` floodplain/plain host、FIXED `CLOSED` stagnant basin（no spillway）、cutoff reach bind、wetland handoff metrics、validator／preview／export。open-spill `LAKE`／`LakePlanV2` semantics／sealed river-split fixturesは不変。moduleはcatalog未登録（diagnostic binding only）。sealed `open-spill-lake.terrain-intent-v2.json` SHA-256／`river-plan-v2.json`／`river-graph-roles-plan-v2.json`／`advanced-river-lake-split-contract-v2.json` checksumは不変。次は`V2-10-09` Phase gateである。

`V2-10-02`（2026-07-18完了）では`MORAINE_FIELD`／`OUTWASH_PLAIN`のEXPERIMENTAL glacial-deposition vertical sliceと`PermafrostPlainProfileV2`（`PLAIN`＋cold climate、FeatureKindなし）を追加した。moraine/outwashはglacial parentをgeometry checksumでbindし、integer raster exportとwhole／tile決定性を検証する。dedicated moduleはcatalog未登録（diagnostic binding）。`PERMAFROST_PLAIN` FeatureKind化、`SUPPORTED`昇格、V2-10-01 sealed example checksumのin-place変更はしていない。

`V2-10-09`（2026-07-18完了）はV2-10のPhase gateである。`DeferredTerrainPhaseGateV2Test`が14 V2-10 FeatureKindのdiagnostic binding維持（false-promotionなし）、12 dedicated moduleの`EXPERIMENTAL`・catalog未登録、17 profile/preset/role/deferred候補名のFeatureKind不在、Release capability list `[environment-fields, hydrology-plan, sparse-volume, surface-2_5d]`不変、21 V2-10 sealed exampleの正逆順／1・4 thread／tr-TR locale／Chatham timezone安定性、plateau-escarpment代表scenarioの再compile＋whole／tile export一致、protected host checksum不変、1000角ScaleProfile admission＋SMALL越え拒否を固定し、full suiteでv1／Release 1／V2-2〜V2-6／V2-9回帰を通過した。offline plan-levelのgenerate／validationを全completed sliceで`SUPPORTED`、previewはpreview index証拠のあるsliceのみ`SUPPORTED`（`MORAINE_FIELD`／`OUTWASH_PLAIN`は`EXPERIMENTAL`のまま）、intent／standalone／exportは`PARTIAL`、Paper以降は`UNSUPPORTED`とし、preset／profile／graph role／deferred候補は昇格していない。証拠は [V2-10 Phase gate audit](design-v2/audits/v2-10-phase-gate.md) を参照する。Track Eは全11 Task完了で終了した。

`V2-14-03`（2026-07-22**完了**）はdocs-onlyのREADME current-state同期である。登録時に「reference role 8種」と誤記していた正本を、productionの`GenerationRequestV2.ReferenceImageRole` **6種**へamendした。`README.md`から「抽出coreのみ…昇格は未実装」と旧v1風role列挙（`HEIGHT_REFERENCE`／`ZONE_REFERENCE`）を除去し、CLI `extract`→`promote`→`request constraint-map`→`v2 export` E2Eと6 roleへ同期、2026-07-18 Gap auditを履歴、2026-07-22監査をcurrent inventoryとしてリンクした。production source／Schema／capability差分は0件。次は`V2-15-01`である。

`V2-15-01`（2026-07-22**完了**）は[全地形カタログ監査](audits/terrain-catalog-full-audit-2026-07-22.md)の現行HEAD再照合である。HEAD `23cc424`でFeatureKind 60＝Schema 60、sealed catalog 71（public 60＋synthetic 11）、dedicated binding 16、production export／Paper `SUPPORTED` coastal 4、排他的4区分4／45／8／48、target案`60 - 14 + 11 = 57`、Task graph V2-15／16／17＝47／19／7を再確認した。`SchemaContractTest`、`FeatureSupportCatalogV2Test`、V2-9／10／volume Phase gate、production command/export E2E、docs link testを通し、新しい矛盾とfalse promotionは検出していない。production source／Schema／example／capability差分は0件で、57-kind案は未採用、公開identifier dispositionは`V2-15-03`の人間承認待ちである。次は`V2-15-02`であり、本Taskからは開始しない。

`V2-15-02`（2026-07-22**完了**）は[Current Feature State Machine Registry](design-v2/current-feature-state-machine-registry.md)を追加し、60 FeatureKindについてenum、Schema、module binding、sealed support catalogのcurrent stateをpure read-only projectionへ集約した。CI testはsourceの欠落／未知／catalog欠落／module binding不整合をfail-closedで検出し、文書projectionのSHA-256も照合する。現行値とcatalog checksum、Schema、example、generator／export／Paper capabilityは不変である。次は`V2-15-03`（公開identifier disposition ADR、人間承認必須）であり、本Taskからは開始しない。

`V2-15-03`（2026-07-22**完了、人間承認済み**）は、既存14 Schema valueのcanonical dispositionと互換移行policyを[ADR 0036](adr/0036-canonical-feature-identifier-disposition.md)としてAcceptedにした。14値のexact mapping、V2-15-04が追加できるtyped-field allowlist、明示的`LEGACY_V2`／`CANONICAL_V2` projection、migrated documentのcanonical `legacySeedBinding` semantics、1 release cycle、単調lifecycleとstateを戻さない運用rollbackを固定した。本Taskはdocs／ADRのみで、Schema、example、production／test source、checksum、capabilityを変更していない。次は`V2-15-04`であり、本Taskからは開始していない。

`V2-15-04`（2026-07-22**完了**）は、[Canonical Feature Target Registry](design-v2/canonical-feature-target-registry.md)と`schemas/terrain-intent-v2-canonical.schema.json`を追加した。46 top-level kind、7 parent discriminator／typed payload行、6 nested child token、strict `legacySeedBinding`だけを受理する。historic v2はexplicit `LEGACY_V2` reader、targetはtop-level `CANONICAL_V2`で分離し、generic dispatcherは欠落を推測しない。strict migratorは14 mapping、owner relation、seed tupleを検証し、version付き旧新checksum reportとtargetをstrict read-back後にatomic publishする。compatibility fixture対、14 mapping、missing／unsupported owner、idempotence、checksum参加、lifecycle単調性をtestした。legacy Schema／fixture／checksum、generator／Release／Paper capabilityは不変。次は`V2-15-05`であり、本Taskからは開始していない。

`V2-15-05`（2026-07-22**完了**）は、`production-dispatch-registry-v1`とcompile-time-only `ProductionExportPipelineV2`を追加し、`Release2ExportApplicationServiceV2`の既存coastal直結をregistry選択へ置き換えた。V2-15-02 current-state projectionとmodule bindingを起動時に照合し、production-connected coastal 4件だけにgenerator／validator／preview／exportの完全なhandler chainを与える。既存Azure Coastの`BACKSHORE_PLAINS`はcontract-onlyで、単独実行や能力昇格を許さない。未接続kind、missing／duplicate／unknown／partial route、source-state昇格、複数未合成pipelineはartifact生成前にfail closedで拒否する。registry checksum（当時）と代表plan checksum `cda5fc94b4147006c859503c964d4da9152aaad744980e2759acd3e396f3756c`はroute順／thread／locale／timezoneに依存しない。既存coastal Releaseの`surface-2_5d`、strict directory／ZIP verify、cancel cleanup、budget admissionは回帰し、Schema／example／support level／Paper能力は不変。次は`V2-15-06`であり、本Taskからは開始していない。

`V2-15-06`（2026-07-22**完了**）は、`hydrology-plan` shared artifactのproduction Application Serviceを追加した。`HydrologyPlanExportPipelineV2`（`v2.production.hydrology-plan.shared`）がcoastal surface生成の上にempty-graph routing／reconciliation／field-only validation／固定12 previewを載せ、`Release2HydrologyExportApplicationServiceV2`が`ReleaseHydrologyPublisherV2`経由で`["hydrology-plan","surface-2_5d"]`をstrict directory／ZIP verifyする。dispatchは`select(intent, capabilities)`でcapability明示選択し、個別hydrology Feature planを含むIntentはartifact生成前に拒否する。当時のregistry checksumとsurface／hydrology plan checksumはorder／thread／locale／timezoneに依存しない。surface-only経路、Schema／example／support level／Paper能力、個別Feature昇格は不変。次は`V2-15-07`であり、本Taskからは開始していない。

`V2-15-07`（2026-07-22**完了**）は、`environment-fields` shared artifactのproduction Application Serviceを追加した。`EnvironmentFieldsExportPipelineV2`（`v2.production.environment-fields.shared`）がshared hydrology chainの上にgeology／climate／water／snow／material／palette／ecology／feature-material／field-only validation／固定10 previewを載せ、`Release2EnvironmentExportApplicationServiceV2`が`ReleaseEnvironmentPublisherV2`経由で`["environment-fields","hydrology-plan","surface-2_5d"]`をstrict directory／ZIP verifyする。hydrology dependency（plan／routing／surface）を同一Releaseでstrict verifyし、個別environment Feature planを含むIntentはartifact生成前に拒否する。registry checksum `15a7a21481031a3c6e78122c2336c3f7c0fe228fb43b9d0fe42252ac374565fc`、surface plan checksum `cda5fc94b4147006c859503c964d4da9152aaad744980e2759acd3e396f3756c`、hydrology plan checksum `b592c5b77f9e74d1ab26dd663b7945a0321cda8a2cdbf1c4bce2e8e09730ee3d`、environment plan checksum `40303ba4cb75354b7d7df06d2fc87b393ce3e497d8f2d0d22477aeedfa463caf`はorder／thread／locale／timezoneに依存しない。surface／hydrology経路、Schema／example／support level／Paper能力、個別Feature昇格は不変。次は`V2-15-08`であり、本Taskからは開始していない。

`V2-15-08`（2026-07-22**完了**）は、`sparse-volume` shared artifactのproduction Application Serviceを追加した。`SparseVolumeExportPipelineV2`（`v2.production.sparse-volume.shared`）がshared environment chainの上にbounded identity SDF、ordinal 0 `ADD_FLUID` ordered CSG、CSG-bound AABB index、hard-pass volume validation、`TilePlanV2`幾何のstreaming 3D volume tileを載せる。operatorはcanonical bedrock floorに作用して結果を変えず、個別volume Featureを推測せずにproduction ordered-CSG kernelを実行する。`Release2SparseVolumeExportApplicationServiceV2`は既存publisher／verifier経由で`["environment-fields","hydrology-plan","sparse-volume","surface-2_5d"]`のdirectory／ZIP、SDF→CSG→AABB／validation binding、volume Sponge v3 semantic checksum、placement eligibilityをstrict verifyする。registry checksum `bec55cada3e479a182b027c873b9ed50a81d283bea51527b479e739999342afc`、volume dispatch plan checksum `b01959e3a4a032869ef95e5d447367d9e667d26503c37b77bb8d491224aa9a08`はorder／thread／locale／timezoneに依存しない。前回full-suiteで顕在化した既存job cancel raceはschedule前登録とsnapshot遷移の直列化で修正し、terminal `CANCELLED`、Future interrupt、未公開Release cleanupを回帰した。全域dense voxel、新Schema／artifact format／capability、個別volume Feature昇格、CLI既定切替、Paper能力変更はない。次は`V2-15-09`であり、本Taskからは開始していない。

`V2-15-09`（2026-07-22**完了**）は、V2-9／V2-10 foundation 2.5D surfaceを既存`surface-2_5d`へ写像するadapterを追加した。[ADR 0037](adr/0037-foundation-surface-to-surface-2_5d-mapping.md)がDR-2推奨どおり新capabilityを作らず、ADR 0013のcoastal-named exact setへ投影することをAcceptedにした。`FoundationSurfaceExportAdapterV2`（`v2.production.surface-2_5d.foundation-adapter`）がplain／hill merge→land-water／height→`CoastalValidatorV2`／固定11 preview→streaming tileの`SurfaceReleaseSourceV2`を組み立て、`Release2FoundationSurfaceExportApplicationServiceV2`が`ReleaseSurfacePublisherV2`経由でdirectory／ZIP strict verifyとplacement eligibilityを通す。`ProductionDispatchRegistryV2`へ第二`["surface-2_5d"]` pipelineは登録せず、coastal production routeとcapability keyを衝突させない。plain-hill-64 fixtureのblueprint checksum `571de2635d5adbcd202cf71df7ad3cb455cccb3a74eb1750d50decac88d98ffc`、manifest checksum `9736c96f1e3834a46c9467510efd159f7e583a2c2d81ee5ca63ca04f85fe9f03`は1／4 worker・tr-TR／Chathamで不変。cancel時は未公開Releaseとsurface stagingを残さない。個別Feature昇格、Schema／artifact type追加、CLI既定、Paper能力、macro owner coverage fail-closedは非Scope（V2-18／後続wiringへ）。次は`V2-15-10`であり、本Taskからは開始していない。

V2-2〜V2-18の詳細なTask Scope、非Scope、test、決定性／memory／security条件、Acceptance、停止条件は [Task Index](design-v2/task-index.md) を正本とし、共通実行規則は [Task Execution Guide](design-v2/task-execution-guide.md)、モデル・Effort・レビュー体制は [Model Assignment](design-v2/model-assignment.md) を参照する。

| Track | Phase | Task | 状態 | 次 | 親Phase gate |
|---|---|---:|---|---|---|
| A | V2-2 | 12 | 完了（12/12） | 完了 | `V2-2-12` |
| A | V2-3 | 15 | 完了（15/15） | 完了 | `V2-3-15` |
| A | V2-4 | 15 | 完了（15/15） | 完了 | `V2-4-15` |
| A | V2-5 | 18 | 完了（18/18） | 完了 | `V2-5-18` |
| A | V2-6 | 21 | 完了（19/21、`V2-6-16`／`17`無効化） | 完了 | `V2-6-19`（完了） |
| A | V2-11 | 6 | 進行中（6/6 Task完了） | — | — |
| A | V2-12 | 11 | 完了（11/11、gate closed、follow-up `V2-12-11`完了） | 完了 | `V2-12-07`（完了） |
| B | V2-7 | 7 | 完了（7/7） | 完了 | `V2-7-07` |
| C | V2-8 | 8 | 進行中（2/8、`V2-8-03`〜`08`はS2決定でHOLD） | —（再開は独立審査） | `V2-8-08` |
| C | V2-13 | 6 | 進行中（6/6 Task完了、親Phase完了は人間承認待ち） | — | — |
| B | V2-14 | 3 | 完了（3/3、親Phase完了宣言は人間承認待ち） | — | — |
| D | V2-9 | 14 | 完了（14/14） | 完了 | `V2-9-14` |
| E | V2-10 | 11 | 完了（11/11） | 完了 | `V2-10-09` |
| E | V2-15 | 47 | 進行中（9/47） | `V2-15-10` | `V2-15-47` |
| E | V2-16 | 19 | 未着手（`V2-15-47`待ち） | `V2-16-01`（待ち） | `V2-16-19` |
| A | V2-18 | 13 | 完了（13/13、Phase gate `V2-18-12` 2026-07-23人間承認） | 完了 | `V2-18-12` |
| A | V2-17 | 7 | 未着手（`V2-15-47`／`V2-16-19`待ち） | `V2-17-01`（待ち） | `V2-17-07` |

個別Taskが完了しても親Phaseを完了にしない。各Phase末尾の統合監査Taskが、全前提、Phase fixture、capability strict verify、determinism／memory／security portfolio、v1回帰、clean buildを確認した場合だけPhase gateを閉じる。WorldEdit／FAWE smokeは必要環境がなければ`BLOCKED_EXTERNAL`のまま残し、mockやoffline testで完了扱いにしない。`V2-6-16`／`V2-6-17`（500／1000実測）は無効化済みであり、未測定寸法を`SUPPORTED`へ上げない。再実測`V2-11-04`／`V2-11-05`と、その実測evidenceの範囲だけを昇格する`V2-11-06`は2026-07-20完了済みである。1000超の寸法はなお未測定であり`SUPPORTED`にしない。

## v2共通gate

- 単一`SUPPORTED`を能力判定へ使わず、13能力ごとにcontract/test/evidenceを要求する。child-plan、plan-level volume、offline export、Paper applyを相互に波及させない。
- v1→v2は新artifactを作る明示操作とし、欠落した形状や接続を推測してhard constraintへしない。画像draftのhard化も明示昇格のみとする。
- whole／tile、tile順序、thread数、module登録順、locale、timezone、対応runtimeでfield／metric／final block checksumを一致させる。
- Release format 1のlegacy read／verify／migrate、immutable golden、version中立基盤を維持し、format 2はversion別のstrict publisher／verifierへ隔離する。退役済みv1 production placement／Undoは復活させない。
- v2 Paper applyはoffline generation、preview、Release 2 self-verify、corruption validationが完了するまで有効化しない。
- LARGEは各予算gateの実測なしにsupportedへしない。
- Schema、ADR、production docs、examplesは各Phaseの実装と同時に更新し、設計例だけを実装済みと表現しない。
