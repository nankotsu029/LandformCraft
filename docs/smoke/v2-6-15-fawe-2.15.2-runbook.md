# V2-6-15 FAWE 2.15.2 standalone smoke runbook

Release 2 end-to-end placement smoke for **Paper 1.21.11 + FastAsyncWorldEdit-Paper 2.15.2 only**（WorldEdit plugin 禁止）。

Version lock: [ADR 0032](../adr/0032-fawe-2.15.2-standalone-smoke-profile.md)。
Evidence: [v2-6-15-fawe-smoke-evidence.md](../design-v2/audits/v2-6-15-fawe-smoke-evidence.md)。

## 0. Preconditions

```bash
./gradlew clean shadowJar
# FAWE jar must already be in run-fawe/plugins/; WorldEdit jars must be absent
# LandformCraft is injected by runFaweServer via -add-plugin (remove stale LandformCraft*.jar)
scripts/smoke/v2-6-15-probe.sh
```

Profile:

- directory: `run-fawe/`
- plugins: FAWE 2.15.2 only in `plugins/`（LandformCraft via Gradle `-add-plugin`）
- Java 21
- **起動:** `./gradlew runFaweServer`（`runDirectory=run-fawe`、Paper build 132）
- Do **not** use `java -jar versions/*/paper-*.jar`（`joptsimple` 欠落）
- 前提: `V2-6-14` COMPLETE かつ R2 Paper path（`V2-6-21`）注入済み

## 1. Boot

```bash
./gradlew runFaweServer
# or automated:
bash scripts/smoke/v2-6-15-run.sh
```

Confirm log:

- `Initialized 2 plugins`
- `FastAsyncWorldEdit (2.15.2+...)`, `LandformCraft (...)`
- no WorldEdit plugin line
- server reaches `Done`

## 2. Smoke sequence

WE runbook と同型（Release 2）:

1. `/lfc version`／`/lfc doctor`
2. Release 2 fixture verify（directory＋ZIP）
3. plan→reservation-bound confirm→snapshot-all
4. containment preflight（fluid／gravity）
5. apply→settle→full verify→APPLIED
6. Undo prepare→confirm→UNDONE
7. optional recovery／restart journal path
8. record peak queue／memory／disk、gateway close／async completion
9. compare canonical checksum with declared fixture expectation
10. no secret／world artifact commit

## 3. Evidence file

[evidence](../design-v2/audits/v2-6-15-fawe-smoke-evidence.md)へmachine-verifiable値を記入する。全Gateを満たした場合だけTaskを完了へ変更し、roadmap Nextを`V2-6-18`へ進める（`V2-6-16`／`17`は無効化）。

## 4. Non-claims

- Do not reuse 2026-07-14 FAWE init-only log as acceptance
- Do not claim 500／1000 measurement
- Do not mark Beta checklist FAWE apply smoke complete unless this Task’s evidence is attached
