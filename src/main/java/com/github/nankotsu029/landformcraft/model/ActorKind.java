package com.github.nankotsu029.landformcraft.model;

/** Stable operator identity categories shared by CLI, Paper, and recovery audit records. */
public enum ActorKind {
    PLAYER,
    CONSOLE,
    SYSTEM
}
