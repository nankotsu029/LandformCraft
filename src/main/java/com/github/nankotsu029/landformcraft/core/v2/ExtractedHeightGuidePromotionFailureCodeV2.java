package com.github.nankotsu029.landformcraft.core.v2;

/** Stable machine-readable failures for explicit height-guide promotion. */
public enum ExtractedHeightGuidePromotionFailureCodeV2 {
    INVALID_OPTIONS,
    PIXEL_BUDGET_EXCEEDED,
    DECODE_VERIFY_FAILED,
    RESIDUAL_INCONSISTENT,
    ARTIFACT_TAMPERED,
    CANCELLED
}
