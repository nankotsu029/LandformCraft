package com.github.nankotsu029.landformcraft.core.v2;

/** Stable machine-readable failures for explicit zone-label promotion. */
public enum ExtractedZoneLabelPromotionFailureCodeV2 {
    INVALID_OPTIONS,
    UNRESOLVED_UNKNOWN,
    PIXEL_BUDGET_EXCEEDED,
    DECODE_VERIFY_FAILED,
    ARTIFACT_TAMPERED,
    CANCELLED
}
