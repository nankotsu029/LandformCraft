package com.github.nankotsu029.landformcraft.core.v2;

/** Stable machine-readable failures for multi-source reconciliation publication. */
public enum MultiSourceReconciliationFailureCodeV2 {
    INVALID_OPTIONS,
    DIMENSION_MISMATCH,
    PIXEL_BUDGET_EXCEEDED,
    ARTIFACT_TAMPERED,
    CANCELLED
}
